# Episode Report: MongoDB Gone

**Agent:** v6  
**Episode ID:** ep_20260422_020205_mongodb_gone  
**Date:** 2026-04-22T02:02:06.973070+00:00  
**Duration:** 290.0s  

---

## Scenario

**Category:** container  
**Blast radius:** global  
**Description:** Kill MongoDB — the 5G core subscriber data store. UDR loses its backend, new PDU sessions cannot be created, and subscriber queries fail. Existing sessions may survive briefly.

## Faults Injected

- **container_kill** on `mongo`

## Baseline (Pre-Fault)

Stack phase before injection: **ready**
All containers running at baseline.

## Fault Propagation Verification

**Verdict:** ✅ `confirmed`

- **Wait:** 0s
- **Actual elapsed:** 0.0s
- **Nodes with significant deltas:** 1
- **Nodes with any drift:** 4

## Symptoms Observed

Symptoms detected: **Yes**  
Observation iterations: 1

## Anomaly Screening (Phase 0)

**ANOMALY DETECTED.** Overall anomaly score: 0.96 (threshold: 0.70, trained on 211 healthy snapshots). The current metric pattern is statistically different from the learned healthy baseline. Something in the network has changed.

The following metrics deviate from their learned-healthy baseline. Treat each as a semantic observation (meaning + numbers), not a number alone — the KB's interpretation is the authoritative reading.

- **`derived.pcscf_sip_error_ratio`** (P-CSCF SIP error response ratio) — current **1.00 ratio** vs learned baseline **0.00 ratio** (HIGH, spike)
    - **What it measures:** Proportion of SIP responses that are errors. Zero is the healthy
baseline; any sustained non-zero value means P-CSCF or something
downstream is rejecting requests.
    - **Spike means:** Errors flowing back — downstream CSCFs or HSS rejecting.
    - **Healthy typical range:** 0–0 ratio
    - **Healthy invariant:** Zero in healthy operation.

- **`normalized.upf.gtp_outdatapktn3upf_per_ue`** (GTP-U downlink rate per UE (N3)) — current **0.03 packets_per_second** vs learned baseline **3.34 packets_per_second** (MEDIUM, drop)
    - **What it measures:** Health of the downlink user-plane path UPF → gNB. Typically mirrors
uplink rate during calls/data; asymmetry indicates directional
faults (e.g., UPF receiving from internet but not forwarding).
    - **Drop means:** No traffic leaving UPF toward RAN.
    - **Healthy typical range:** 0–10 packets_per_second
    - **Healthy invariant:** Per-UE — constant regardless of UE pool size. Roughly symmetric with uplink during healthy calls.

- **`normalized.upf.gtp_indatapktn3upf_per_ue`** (GTP-U uplink rate per UE (N3)) — current **0.13 packets_per_second** vs learned baseline **3.42 packets_per_second** (MEDIUM, drop)
    - **What it measures:** Health of the uplink user-plane path gNB → UPF. Drops to near-zero
during RAN or N3 outage; stays nonzero during active calls or data
sessions. Decoupled from SIP signaling (signals data plane, not
control plane).
    - **Drop means:** Data plane dead on uplink — UPF receiving no packets from gNB.
    - **Healthy typical range:** 0–10 packets_per_second
    - **Healthy invariant:** Per-UE — constant regardless of UE pool size. Rises during active VoNR calls (~100 pps for G.711 voice) and data transfer.

- **`normalized.smf.bearers_per_ue`** (Active QoS bearers per UE) — current **2.00 count** vs learned baseline **2.61 count** (LOW, shift)
    - **What it measures:** Per-UE count of active QoS bearers. Baseline reflects default
bearers; increments during VoNR calls indicate dedicated voice
bearers being set up. Drop during an active call = dedicated
bearer torn down unexpectedly (voice will fail).
    - **Shift means:** Expected during VoNR calls (1 extra bearer per active call).
    - **Healthy typical range:** 2–3.5 count
    - **Healthy invariant:** At rest: equals configured default bearers (typically 2 per UE).
During active VoNR call: +1 per caller. The per-UE ratio is the
invariant; absolute count scales with UE pool.

- **`normalized.pcscf.dialogs_per_ue`** (Active SIP dialogs per registered UE at P-CSCF) — current **0.00 count** vs learned baseline **0.57 count** (LOW, drop)
    - **What it measures:** How many calls per user are currently in progress at the P-CSCF.
Going to zero from a non-zero baseline means calls have ended
(normal) OR call setup is failing system-wide (degradation).
Together with rcv_requests_* it discriminates the two.
    - **Drop means:** No active calls. Normal at rest; suspicious if prior state had calls AND INVITE rate is also zero.
    - **Healthy typical range:** 0–1 count
    - **Healthy invariant:** Per-UE — scale-independent. 0 at rest, ~1 per active VoNR call.

- **`derived.upf_activity_during_calls`** — current **1.00** vs learned baseline **0.47** (LOW, spike). *(No KB context available — interpret from the metric name.)*


## Event Aggregation (Phase 1)

No events fired during this episode. Either no metric KB triggers matched, or the episode encountered no meaningful state transitions.

## Correlation Analysis (Phase 2)

No events fired — correlation engine had nothing to work with.

## Network Analysis (Phase 3)

**Summary:** The MongoDB container has exited, causing PCF to fail, which in turn leads to P-CSCF rejecting all SIP requests and a complete failure of the user data plane.

**Layer status:**

| Layer | Rating | Note |
|---|---|---|
| **infrastructure** | 🔴 RED | A critical database component (MongoDB) is down. This is the likely root cause of the cascading failures in the Core and IMS layers. |
| **ran** | 🟡 YELLOW | The RAN itself appears to be operational, but there is no user plane traffic flowing to or from the core. This is a symptom of the core network failure, not a root cause in the RAN. |
| **core** | 🔴 RED | The PCF is unresponsive, likely due to the MongoDB outage. This is preventing the establishment of QoS flows necessary for VoNR calls. The SMF and AMF are also affected, but the primary symptom is the PCF failure's impact on the IMS. |
| **ims** | 🔴 RED | The IMS is completely non-functional for call setup. The P-CSCF is rejecting all incoming SIP requests, which is a direct result of the PCF failure in the core network. |

**INFRASTRUCTURE evidence:**
- container 'mongo' has exited

**RAN evidence:**
- normalized.upf.gtp_indatapktn3upf_per_ue near zero
- normalized.upf.gtp_outdatapktn3upf_per_ue near zero

**CORE evidence:**
- inactive links to MongoDB from PCF and UDR
- pcscf is reporting high httpclient connection failures

**IMS evidence:**
- derived.pcscf_sip_error_ratio is 1.00
- normalized.pcscf.dialogs_per_ue is 0.00

**Ranked hypotheses:**

- **`h1`** (fit=0.90, nf=pcf, specificity=specific):
    - **Statement:** The 'mongo' database container has exited, causing the PCF (Policy Control Function) to become unresponsive. This leads to failures on the Rx interface, causing the P-CSCF to reject all SIP INVITE requests for new VoNR calls.
    - **Falsification probes:**
        - Check the logs for the 'pcf' container for any database connection errors.
        - Check the logs for the 'pcscf' container to confirm errors related to the Rx interface or PCF.
        - Restart the 'mongo' container and observe if the 'pcscf_sip_error_ratio' returns to zero.
- **`h2`** (fit=0.60, nf=pcf, specificity=moderate):
    - **Statement:** A network partition exists between the P-CSCF and the PCF, preventing the P-CSCF from obtaining QoS policy authorization for SIP sessions.
    - **Falsification probes:**
        - Execute 'measure_rtt' from the 'pcscf' container to the 'pcf' container's IP address to check for connectivity.
        - Inspect the network policies and firewall rules on the Docker network connecting the IMS and core components.


## Falsification Plans (Phase 4)

**2 falsification plan(s) — one per hypothesis:**

### Plan for `h1` (target: `pcf`)

**Hypothesis:** The 'mongo' database container has exited, causing the PCF (Policy Control Function) to become unresponsive. This leads to failures on the Rx interface, causing the P-CSCF to reject all SIP INVITE requests for new VoNR calls.

**Probes (3):**
1. **`get_network_status`** — Check status of 'mongo' container
    - *Expected if hypothesis holds:* The 'mongo' container has status 'exited'.
    - *Falsifying observation:* The 'mongo' container is 'running'. This would indicate the database is up and not the root cause of a PCF failure.
2. **`read_container_logs`** — container='pcf', grep='mongo|database|connection refused'
    - *Expected if hypothesis holds:* PCF logs show errors attempting to connect to the MongoDB database.
    - *Falsifying observation:* No database connection errors in PCF logs. This would suggest the PCF is either not failing or failing for a reason other than its database.
3. **`read_container_logs`** — container='pcscf', grep='Rx|policy|PCF'
    - *Expected if hypothesis holds:* P-CSCF logs show errors related to the Rx interface or inability to get policy from PCF.
    - *Falsifying observation:* No such errors in P-CSCF logs. If P-CSCF is rejecting SIP requests but not logging failures about PCF, the cause of rejection is likely elsewhere.

*Notes:* This plan directly tests the causal chain asserted in the hypothesis: the database is down (probe 1), which causes the PCF to fail (probe 2), which in turn causes the P-CSCF to fail (probe 3).

### Plan for `h2` (target: `pcf`)

**Hypothesis:** A network partition exists between the P-CSCF and the PCF, preventing the P-CSCF from obtaining QoS policy authorization for SIP sessions.

**Probes (3):**
1. **`measure_rtt`** — from='pcscf', to_ip='pcf_ip'
    - *Expected if hypothesis holds:* 100% packet loss or extremely high RTT, indicating a partition.
    - *Falsifying observation:* Clean, low RTT (<5ms). This would directly disprove a network partition between the two components.
2. **`measure_rtt`** — from='pcf', to_ip='pcscf_ip'
    - *Expected if hypothesis holds:* 100% packet loss (symmetric partition).
    - *Falsifying observation:* Clean, low RTT (<5ms). This triangulates the first probe and confirms bi-directional connectivity, disproving a partition.
3. **`get_network_status`** — Check status of 'pcf' and 'mongo' containers
    - *Expected if hypothesis holds:* Both 'pcf' and 'mongo' containers are 'running', but isolated.
    - *Falsifying observation:* Either 'pcf' or 'mongo' is in an 'exited' state. This would falsify the 'partition' hypothesis and support the 'container down' hypothesis (h1).

*Notes:* This plan focuses on verifying the network-level isolation claimed in the hypothesis. Probes 1 and 2 test the primary link from both directions for triangulation. Probe 3 helps disambiguate this hypothesis from h1 by checking container health.


## Parallel Investigators (Phase 5)

**2 sub-Investigator verdict(s):** **2 DISPROVEN**

### `h1` — ❌ **DISPROVEN**

**Hypothesis:** The 'mongo' database container has exited, causing the PCF (Policy Control Function) to become unresponsive. This leads to failures on the Rx interface, causing the P-CSCF to reject all SIP INVITE requests for new VoNR calls.

**Reasoning:** Probe 1 confirmed that the 'mongo' container has exited, which is consistent with the hypothesis. However, Probes 2 and 3, which checked the logs of the PCF and P-CSCF containers respectively, found no corresponding error messages. The absence of logs in PCF about database connection failures and the absence of logs in P-CSCF about Rx interface or PCF-related failures contradicts the central causal chain of the hypothesis.

**Probes executed (3):**
- **Check status of 'mongo' container** ✓ CONSISTENT
    - *Tool:* `default_api.get_network_status()`
    - *Observation:* [EVIDENCE: get_network_status() -> "{\n  \"phase\": \"down\", ... \"containers\": {\n    \"mongo\": \"exited\", ... }\n}"]
    - *Comment:* The tool output confirms that the 'mongo' container has indeed exited, which aligns with the initial premise of the hypothesis.
- **container='pcf', grep='mongo|database|connection refused'** ✗ CONTRADICTS
    - *Tool:* `default_api.read_container_logs(container='pcf', grep='mongo|database|connection refused', since_seconds=300)`
    - *Observation:* [EVIDENCE: read_container_logs("pcf", "mongo|database|connection refused", 300) -> "(no log output)"]
    - *Comment:* Contrary to expectations, the PCF logs show no errors related to database connectivity. This finding weakens the hypothesized link between the exited mongo container and a PCF failure.
- **container='pcscf', grep='Rx|policy|PCF'** ✗ CONTRADICTS
    - *Tool:* `default_api.read_container_logs(container='pcscf', grep='Rx|policy|PCF', since_seconds=300)`
    - *Observation:* [EVIDENCE: read_container_logs("pcscf", "Rx|policy|PCF", 300) -> "(no log output)"]
    - *Comment:* The P-CSCF logs contain no errors concerning the Rx interface or its interaction with the PCF. This contradicts the final step in the hypothesis's causal chain, which states that P-CSCF is rejecting calls due to PCF unresponsiveness.

**Alternative suspects:** pcscf

### `h2` — ❌ **DISPROVEN**

**Hypothesis:** A network partition exists between the P-CSCF and the PCF, preventing the P-CSCF from obtaining QoS policy authorization for SIP sessions.

**Reasoning:** The core claim of a network partition is directly contradicted by strong evidence. Probes measuring round-trip-time between the P-CSCF and PCF in both directions show perfect connectivity with 0% packet loss and sub-millisecond latency. Furthermore, the 'mongo' container, a dependency for the PCF, was found to be in an 'exited' state, which points to a component failure rather than network isolation as the root cause.

**Probes executed (3):**
- **from='pcscf', to_ip='pcf_ip'** ✗ CONTRADICTS
    - *Tool:* `measure_rtt("pcscf", "172.22.0.27")`
    - *Observation:* [EVIDENCE: measure_rtt("pcscf", "172.22.0.27") -> "3 packets transmitted, 3 received, 0% packet loss, time 2043ms\nrtt min/avg/max/mdev = 0.087/0.127/0.176/0.036 ms"]
    - *Comment:* The RTT is clean and shows no signs of a network partition. This directly contradicts the hypothesis.
- **from='pcf', to_ip='pcscf_ip'** ✗ CONTRADICTS
    - *Tool:* `measure_rtt("pcf", "172.22.0.21")`
    - *Observation:* [EVIDENCE: measure_rtt("pcf", "172.22.0.21") -> "3 packets transmitted, 3 received, 0% packet loss, time 2054ms\nrtt min/avg/max/mdev = 0.073/0.079/0.091/0.008 ms"]
    - *Comment:* This confirms bidirectional connectivity is healthy, further disproving the network partition hypothesis.
- **Check status of 'pcf' and 'mongo' containers** ✗ CONTRADICTS
    - *Tool:* `get_network_status()`
    - *Observation:* [EVIDENCE: get_network_status() -> "{\"phase\": \"down\", \"down_or_absent\": [\"mongo\"], \"containers\": {\"mongo\": \"exited\", \"pcf\": \"running\"}}"]
    - *Comment:* The 'mongo' container has exited. This falsifies the 'partition' hypothesis (which assumes running but isolated components) and strongly supports the 'container down' hypothesis (h1).

**Alternative suspects:** pcf, mongo


## Evidence Validation (Phase 6)

**Overall verdict:** clean
**Overall confidence:** high

**Per sub-Investigator:**

| Agent | Tool Calls | Citations | Verdict | Confidence |
|---|---|---|---|---|
| `InvestigatorAgent_h1` | 3 | 3/3 | clean | high |
| `InvestigatorAgent_h2` | 4 | 3/3 | clean | high |


## Ground Truth

**Failure domain:** data_layer  
**Protocol impact:** multiple  
**Affected components:** mongo  
**Severity:** degraded

## Agent Diagnosis

**Model:** v6-adk/gemini-2.5-flash+gemini-2.5-pro  
**Time to diagnosis:** 137.3s

### Prompt to RCA Agent

```
The 5G SA + IMS stack is experiencing issues. Investigate and diagnose the root cause.
```

**Diagnosis:**

> ### causes
> - **summary**: The initial hypotheses, which blamed a PCF failure on either a database outage or a network partition, have both been disproven, leaving the true root cause undetermined.
> - **timeline**:
>     1. No events were provided by the correlation engine. The primary symptom is the `pcscf_sip_error_ratio` metric being 1, indicating total call failure.
> - **root_cause**: The root cause is currently unknown. The initial set of hypotheses from the Network Analyst was invalidated by the investigation.
> - **affected_components**:
>     - `mongo`: Suspected Cause (This container has exited, but the expected downstream impact on PCF was not observed in logs.)
>     - `pcf`: Suspected Cause (This component was central to the initial hypotheses, but logs did not confirm its failure mode.)
>     - `pcscf`: Symptomatic (This component is rejecting all SIP requests, but the reason is not what was hypothesized.)
> - **recommendation**: The initial hypotheses have been ruled out. A manual investigation should begin by examining the logs for the `pcscf` to find the real reason it is rejecting SIP requests, as the expected Rx/PCF errors were not present. Concurrently, investigate the `mongo` container logs to determine why it exited, and the `pcf` logs to understand why it did not report any database connection errors.
> - **confidence**: low
> - **explanation**: The investigation disproved both initial hypotheses. The primary hypothesis, that an exited MongoDB container caused the PCF to fail and the P-CSCF to reject calls, was contradicted when the logs for PCF and P-CSCF showed no database connection or Rx interface errors. The secondary hypothesis of a network partition between the P-CSCF and PCF was disproven by connectivity probes which showed perfect network health. Although we confirmed the `mongo` container is down, its causal link to the P-CSCF failure could not be established, meaning the original analysis was incorrect.

### Scoring Breakdown

**Overall score: 42%**

**Scorer assessment:** The agent correctly identified the severity and type of fault (component down, total service failure) and correctly placed the affected component in its ontology layer. However, it failed to identify the actual root cause, explicitly disproving the correct hypothesis, and only weakly identified the correct component as a 'suspected cause' while denying its causal link to the observed symptoms. Its low confidence was well-calibrated given the incorrect diagnosis.

| Dimension | Result | Rationale |
|-----------|--------|-----------|
| Root cause correct | No | The agent explicitly states that the root cause is 'unknown' and that the hypothesis blaming a database outage (MongoDB) was 'disproven' because its causal link to the observed P-CSCF failure 'could not be established.' This directly contradicts the simulated failure where MongoDB being down is the root cause. |
| Component overlap | 10% | The agent identifies 'mongo' as a 'Suspected Cause' and notes that the 'container has exited.' However, it also lists 'pcf' as a 'Suspected Cause' and, more critically, explicitly states in the explanation that the causal link between the exited 'mongo' container and the observed P-CSCF failure 'could not be established.' This undermines its identification of 'mongo' as the primary affected component responsible for the observed problem, resulting in a very low partial score. |
| Severity correct | Yes | The agent correctly identifies a 'total call failure' based on the 'pcscf_sip_error_ratio' being 1, which accurately reflects the complete outage caused by the MongoDB failure. |
| Fault type identified | Yes | The agent identifies that the 'mongo' container 'has exited,' which correctly describes the fault type as a component being down/unreachable. It also identifies a complete service failure. |
| Layer accuracy | Yes | The agent's intermediate network analysis correctly rates the 'infrastructure' layer as 'red' and provides 'container 'mongo' has exited' as evidence, accurately attributing the MongoDB failure to its correct ontology layer. |
| Confidence calibrated | Yes | The agent states 'confidence: low' for a diagnosis that failed to identify the root cause and explicitly disproved the correct hypothesis. This low confidence is appropriate given the incorrect diagnosis. |

**Ranking:** The agent explicitly states the root cause is 'unknown' and does not provide a ranked list of potential root causes.


### Token Usage

| Metric | Count |
|--------|-------|
| Input tokens | 118,935 |
| Output tokens | 3,794 |
| Thinking tokens | 8,467 |
| **Total tokens** | **131,196** |

**Per-phase breakdown:**

| Phase | Tokens | Tool Calls | LLM Calls |
|-------|--------|------------|-----------|
| AnomalyScreener | 0 | 0 | 0 |
| EventAggregator | 0 | 0 | 0 |
| CorrelationAnalyzer | 0 | 0 | 0 |
| NetworkAnalystAgent | 46,920 | 4 | 5 |
| InstructionGeneratorAgent | 12,620 | 1 | 2 |
| InvestigatorAgent_h1 | 27,972 | 3 | 4 |
| InvestigatorAgent_h2 | 39,172 | 4 | 5 |
| EvidenceValidator | 0 | 0 | 0 |
| SynthesisAgent | 4,512 | 0 | 1 |


## Resolution

**Heal method:** scheduled  
**Recovery time:** 290.0s
