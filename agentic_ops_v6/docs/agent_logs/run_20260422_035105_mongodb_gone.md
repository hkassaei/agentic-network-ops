# Episode Report: MongoDB Gone

**Agent:** v6  
**Episode ID:** ep_20260422_034503_mongodb_gone  
**Date:** 2026-04-22T03:45:04.491363+00:00  
**Duration:** 359.8s  

---

## Scenario

**Category:** container  
**Blast radius:** global  
**Description:** Kill MongoDB — the 5G core subscriber data store. UDR loses its backend, new PDU sessions cannot be created, and subscriber queries fail. Existing sessions may survive briefly.

## Faults Injected

- **container_kill** on `mongo`

## Baseline (Pre-Fault)

Stack phase before injection: **ready**
All containers running at baseline.

## Fault Propagation Verification

**Verdict:** ✅ `confirmed`

- **Wait:** 0s
- **Actual elapsed:** 0.0s
- **Nodes with significant deltas:** 3
- **Nodes with any drift:** 5

## Symptoms Observed

Symptoms detected: **Yes**  
Observation iterations: 1

## Anomaly Screening (Phase 0)

**ANOMALY DETECTED.** Overall anomaly score: 0.78 (threshold: 0.70, trained on 211 healthy snapshots). The current metric pattern is statistically different from the learned healthy baseline. Something in the network has changed.

The following metrics deviate from their learned-healthy baseline. Treat each as a semantic observation (meaning + numbers), not a number alone — the KB's interpretation is the authoritative reading.

- **`derived.pcscf_sip_error_ratio`** (P-CSCF SIP error response ratio) — current **1.00 ratio** vs learned baseline **0.00 ratio** (HIGH, spike)
    - **What it measures:** Proportion of SIP responses that are errors. Zero is the healthy
baseline; any sustained non-zero value means P-CSCF or something
downstream is rejecting requests.
    - **Spike means:** Errors flowing back — downstream CSCFs or HSS rejecting.
    - **Healthy typical range:** 0–0 ratio
    - **Healthy invariant:** Zero in healthy operation.


## Event Aggregation (Phase 1)

No events fired during this episode. Either no metric KB triggers matched, or the episode encountered no meaningful state transitions.

## Correlation Analysis (Phase 2)

No events fired — correlation engine had nothing to work with.

## Network Analysis (Phase 3)

**Summary:** The IMS stack is failing all SIP requests due to the I-CSCF timing out on Diameter requests to the HSS, indicating HSS is unresponsive or there is a network partition between them.

**Layer status:**

| Layer | Rating | Note |
|---|---|---|
| **infrastructure** | 🟡 YELLOW | The MongoDB container is down, which affects several non-IMS 5G core NFs. While not the primary cause of the IMS failure, it indicates an infrastructure-level issue. |
| **ran** | 🟢 GREEN | RAN appears healthy with connected UEs. |
| **core** | 🟢 GREEN | The 5G core appears stable for basic connectivity, though dependencies on the failed MongoDB present a risk. |
| **ims** | 🔴 RED | The IMS core is non-functional. The P-CSCF is reporting 100% SIP errors, caused by the I-CSCF failing to get responses from the HSS for user authorization. |

**INFRASTRUCTURE evidence:**
- container 'mongo' is exited

**IMS evidence:**
- derived.pcscf_sip_error_ratio = 1.0
- icscf:ims_icscf:uar_timeouts = 4.0
- icscf:ims_icscf:lir_timeouts = 1.0

**Ranked hypotheses:**

- **`h1`** (fit=0.90, nf=pyhss, specificity=specific):
    - **Statement:** The HSS (pyhss) is unresponsive to the I-CSCF, specifically failing to answer Diameter User-Authorization-Requests (UAR). This prevents the I-CSCF from routing registration requests to an S-CSCF, causing the entire SIP registration to fail and the P-CSCF to report errors.
    - **Falsification probes:**
        - Successful 'measure_rtt' from 'icscf' to the 'pyhss' IP address.
        - HSS logs show it is receiving and successfully responding to UARs from I-CSCF.
        - A diagnostic Diameter UAR query from the I-CSCF to the HSS receives a successful response.
- **`h2`** (fit=0.60, nf=icscf, specificity=moderate):
    - **Statement:** The I-CSCF has an internal fault or misconfiguration that causes it to incorrectly generate Diameter timeouts. It is failing to send requests to the HSS or is sending them to the wrong destination, leading to the appearance of an unresponsive HSS.
    - **Falsification probes:**
        - Packet capture on the I-CSCF shows valid Diameter UARs being sent to the correct HSS IP.
        - Failed 'measure_rtt' from 'icscf' to 'pyhss' IP address, which would indicate a real network issue, not an I-CSCF fault.
- **`h3`** (fit=0.10, nf=pcscf, specificity=moderate):
    - **Statement:** The P-CSCF has an internal processing fault that causes it to generate 100% SIP error responses, regardless of the actual state of its downstream dependencies (I-CSCF, S-CSCF, HSS).
    - **Falsification probes:**
        - Live metrics from I-CSCF show it is successfully processing requests and communicating with HSS.
        - A direct SIP register request sent to the I-CSCF (bypassing the P-CSCF) succeeds.


## Falsification Plans (Phase 4)

**3 falsification plan(s) — one per hypothesis:**

### Plan for `h1` (target: `pyhss`)

**Hypothesis:** The HSS (pyhss) is unresponsive to the I-CSCF, specifically failing to answer Diameter User-Authorization-Requests (UAR). This prevents the I-CSCF from routing registration requests to an S-CSCF, causing the entire SIP registration to fail and the P-CSCF to report errors.

**Probes (3):**
1. **`check_process_listeners`** — container='pyhss'
    - *Expected if hypothesis holds:* A process is listening on the Diameter port (3868/tcp), indicating the application is running but not responding to requests.
    - *Falsifying observation:* No process is listening on port 3868. This would indicate the HSS application isn't running at all, a more fundamental failure.
2. **`measure_rtt`** — from='pcrf', to_ip='pyhss_ip' (Triangulation probe)
    - *Expected if hypothesis holds:* High packet loss or timeout, suggesting a network issue affecting pyhss broadly.
    - *Falsifying observation:* Clean RTT (<5ms). If a third-party container can reach HSS without issue, it implies the problem is specific to the I-CSCF-HSS link, pointing towards h2.
3. **`query_subscriber`** — imsi='<any_valid_imsi>'
    - *Expected if hypothesis holds:* The query succeeds. The hypothesis is about network unresponsiveness to Diameter, not a total internal failure of the HSS application.
    - *Falsifying observation:* The tool times out or returns an error. This would suggest the HSS is internally broken and cannot even query its own database, a deeper failure than just network unresponsiveness.

*Notes:* This plan tests HSS liveness from three different angles: process readiness, network reachability from a neutral third party, and internal application health, providing strong cross-layer validation.

### Plan for `h2` (target: `icscf`)

**Hypothesis:** The I-CSCF has an internal fault or misconfiguration that causes it to incorrectly generate Diameter timeouts. It is failing to send requests to the HSS or is sending them to the wrong destination, leading to the appearance of an unresponsive HSS.

**Probes (3):**
1. **`read_running_config`** — container='icscf'
    - *Expected if hypothesis holds:* The configuration file shows an incorrect IP address or hostname for the HSS.
    - *Falsifying observation:* The configuration shows the correct, resolvable address for the HSS. This would rule out static misconfiguration.
2. **`get_nf_metrics`** — for container='icscf', check Diameter client metrics
    - *Expected if hypothesis holds:* Zero or very low 'outbound successful' Diameter transaction counters, and potentially high 'outbound error' counters.
    - *Falsifying observation:* A non-zero count of 'outbound successful' Diameter transactions. This would prove the I-CSCF believes it is sending requests successfully, contradicting the hypothesis that it's failing to send.
3. **`measure_rtt`** — from='icscf', to_ip='pcrf_ip' (Triangulation probe)
    - *Expected if hypothesis holds:* Clean RTT (<5ms). The hypothesis is about a specific fault related to HSS communication, not general network failure.
    - *Falsifying observation:* High packet loss. This would indicate a broader network problem with the I-CSCF container itself, suggesting its inability to reach HSS is not an application fault but a networking one.

*Notes:* This plan focuses on the I-CSCF's state: its configuration, its own metrics about its behavior, and its general network health, to isolate the fault to the I-CSCF itself.

### Plan for `h3` (target: `pcscf`)

**Hypothesis:** The P-CSCF has an internal processing fault that causes it to generate 100% SIP error responses, regardless of the actual state of its downstream dependencies (I-CSCF, S-CSCF, HSS).

**Probes (3):**
1. **`measure_rtt`** — from='pcscf', to_ip='icscf_ip'
    - *Expected if hypothesis holds:* Clean RTT (<5ms). The hypothesis claims an internal P-CSCF fault, so network connectivity to its peer should be healthy.
    - *Falsifying observation:* High packet loss or timeout. This would provide an alternative, network-based reason for P-CSCF to fail requests, falsifying the 'internal fault' claim.
2. **`get_nf_metrics`** — Compare pcscf's outbound SIP requests to icscf's inbound SIP requests
    - *Expected if hypothesis holds:* The number of SIP requests sent from P-CSCF is zero or near-zero, as it is erroring out locally.
    - *Falsifying observation:* The number of outbound requests from P-CSCF closely matches the number of inbound requests at I-CSCF. This would prove P-CSCF is successfully forwarding traffic, falsifying the hypothesis.
3. **`run_kamcmd`** — container='pcscf', command='ps'
    - *Expected if hypothesis holds:* The output shows a crashed or unhealthy worker process, or unusual memory usage.
    - *Falsifying observation:* All Kamailio processes are running normally. This indicates the P-CSCF's core processing engine is healthy, making an internal fault less likely.

*Notes:* This plan tests if the P-CSCF is truly the source of the errors by checking its network path to the next hop and comparing its outbound traffic metrics with its peer's inbound metrics.


## Parallel Investigators (Phase 5)

**3 sub-Investigator verdict(s):** **2 DISPROVEN**, **1 INCONCLUSIVE**

### `h1` — ❌ **DISPROVEN**

**Hypothesis:** The HSS (pyhss) is unresponsive to the I-CSCF, specifically failing to answer Diameter User-Authorization-Requests (UAR). This prevents the I-CSCF from routing registration requests to an S-CSCF, causing the entire SIP registration to fail and the P-CSCF to report errors.

**Reasoning:** The hypothesis that the HSS is broadly unresponsive is contradicted by network-level evidence. A triangulation probe using `measure_rtt` from a neutral third-party container (pcf) to the HSS (pyhss) showed a clean network path with 0% packet loss and sub-millisecond RTT. This indicates the HSS is reachable on the network. The problem is therefore not a general failure of the HSS, but is likely specific to the link from the I-CSCF, pointing to the I-CSCF as the more likely suspect.

**Probes executed (3):**
- **A process is listening on the Diameter port (3868/tcp), indicating the application is running but not responding to requests.** ~ AMBIGUOUS
    - *Tool:* `check_process_listeners("pyhss")`
    - *Observation:* [EVIDENCE: check_process_listeners("pyhss") -> "Neither ss nor netstat available in pyhss. Output: OCI runtime exec failed: exec failed: unable to start container process: exec: \"netstat\": executable file not found in $PATH: unknown"]
    - *Comment:* The tool failed because the necessary utilities (ss, netstat) were not present in the container. This probe could not be completed and its result is inconclusive.
- **High packet loss or timeout, suggesting a network issue affecting pyhss broadly.** ✗ CONTRADICTS
    - *Tool:* `measure_rtt("pcf", "172.22.0.18")`
    - *Observation:* [EVIDENCE: measure_rtt("pcf", "172.22.0.18") -> "3 packets transmitted, 3 received, 0% packet loss, time 2060ms\nrtt min/avg/max/mdev = 0.088/0.167/0.309/0.100 ms"]
    - *Comment:* This result directly contradicts the hypothesis. The falsifying observation in the plan was a clean RTT from a third-party, which is exactly what was observed. This shows the HSS is reachable on the network, shifting suspicion to the I-CSCF's specific connection.
- **The query succeeds. The hypothesis is about network unresponsiveness to Diameter, not a total internal failure of the HSS application.** ✓ CONSISTENT
    - *Tool:* `query_subscriber("001011234567891", "ims")`
    - *Observation:* [EVIDENCE: query_subscriber("001011234567891", "ims") -> "{\n  \"ims_subscriber\": {\n    \"subscriber_id\": 27,\n    \"nam\": 0,\n    \"last_modified\": \"2026-04-21T01:49:46Z\",\n    \"enabled\": true,\n    \"roaming_enabled\": true,\n    \"auc_id\": 27,\n    \"roaming_rule_list\": null,\n    \"default_apn\": 1,\n    \"subscribed_rau_tau_timer\": 300,\n    \"apn_list\": \"1,2\",\n    \"serving_m…
    - *Comment:* The successful subscriber query confirms that the HSS application is internally healthy and its database is accessible. This is consistent with the hypothesis, which posits a network-level unresponsiveness rather than an internal HSS fault.

**Alternative suspects:** icscf

### `h2` — ❌ **DISPROVEN**

**Hypothesis:** The I-CSCF has an internal fault or misconfiguration that causes it to incorrectly generate Diameter timeouts. It is failing to send requests to the HSS or is sending them to the wrong destination, leading to the appearance of an unresponsive HSS.

**Reasoning:** The hypothesis is disproven by multiple probes. First, the I-CSCF's configuration points to the correct HSS destination. Second, metrics show the I-CSCF is successfully sending Diameter requests and receiving replies, contradicting the claim that it's failing to send them. Finally, network probes show a healthy, low-latency path to the HSS, indicating that the timeouts are not due to a network partition or an I-CSCF-internal fault, but are likely due to the HSS itself not responding at the application layer.

**Probes executed (3):**
- **Check I-CSCF's configured Diameter peer for the HSS.** ✗ CONTRADICTS
    - *Tool:* `run_kamcmd("icscf", "cdp.list_peers")`
    - *Observation:* [EVIDENCE: run_kamcmd("icscf", "cdp.list_peers") -> "FQDN: hss.ims.mnc001.mcc001.3gppnetwork.org"]
    - *Comment:* The I-CSCF is configured with the correct FQDN for the HSS. This falsifies the 'sending to the wrong destination' part of the hypothesis.
- **Check I-CSCF's Diameter client metrics.** ✗ CONTRADICTS
    - *Tool:* `get_nf_metrics()`
    - *Observation:* [EVIDENCE: get_nf_metrics() -> "ims_icscf:uar_replies_received = 23.0"]
    - *Comment:* The I-CSCF has received 23 UAR replies from the HSS. This falsifies the claim that the I-CSCF is failing to send requests, as it is clearly engaging in successful two-way communication.
- **Measure RTT from I-CSCF to HSS and a triangulation target (PCRF).** ✗ CONTRADICTS
    - *Tool:* `measure_rtt("icscf", "172.22.0.18")`
    - *Observation:* [EVIDENCE: measure_rtt("icscf", "172.22.0.18") -> "0% packet loss, rtt min/avg/max/mdev = 0.048/0.081/0.105/0.024 ms"]
    - *Comment:* The network path from I-CSCF to HSS is perfectly healthy. This contradicts the idea that timeouts are caused by a network issue or an internal I-CSCF fault. The timeouts reported by the I-CSCF are therefore likely legitimate, pointing to a problem at the HSS application layer.

**Alternative suspects:** pyhss

### `h3` — ❓ **INCONCLUSIVE**

**Hypothesis:** The P-CSCF has an internal processing fault that causes it to generate 100% SIP error responses, regardless of the actual state of its downstream dependencies (I-CSCF, S-CSCF, HSS).

**Reasoning:** Mechanical guardrail: InvestigatorAgent_h3 made only 0 tool call(s); minimum is 2. Self-reported output was discarded.


## Evidence Validation (Phase 6)

**Overall verdict:** severe
**Overall confidence:** none

**Per sub-Investigator:**

| Agent | Tool Calls | Citations | Verdict | Confidence |
|---|---|---|---|---|
| `InvestigatorAgent_h1` | 5 | 3/3 | clean | high |
| `InvestigatorAgent_h2` | 6 | 3/3 | clean | high |
| `InvestigatorAgent_h3` | 0 | 0/0 | severe | none |
|  |  |  |  | *ZERO tool calls — all citations fabricated* |


## Ground Truth

**Failure domain:** data_layer  
**Protocol impact:** multiple  
**Affected components:** mongo  
**Severity:** degraded

## Agent Diagnosis

**Model:** v6-adk/gemini-2.5-flash+gemini-2.5-pro  
**Time to diagnosis:** 219.5s

### Prompt to RCA Agent

```
The 5G SA + IMS stack is experiencing issues. Investigate and diagnose the root cause.
```

**Diagnosis:**

> ### causes
> - **summary**: IMS registration is failing system-wide because the Home Subscriber Server (HSS) is not responding to Diameter authorization requests from the I-CSCF, causing upstream SIP requests to time out and fail.
> - **timeline**:
>     1. The I-CSCF begins logging "DIAMETER timeout" errors when attempting to send User-Authorization-Requests (UAR) to the HSS (`pyhss`).
>     2. Unable to get a response from the HSS, the I-CSCF cannot authorize subscribers or select an S-CSCF.
>     3. All SIP REGISTER requests forwarded from the P-CSCF to the I-CSCF subsequently fail.
>     4. The P-CSCF, as the entry point, reports 100% SIP error rates, reflecting the total failure of the downstream registration process.
> - **root_cause**: The HSS (`pyhss`) application is failing to process and respond to Diameter requests from the I-CSCF in a timely manner. While the HSS container is running and reachable on the network, its application layer is unresponsive to these specific queries. The primary suspect is `pyhss`.
> - **affected_components**:
>     - `pyhss` (HSS): Root Cause
>     - `icscf` (I-CSCF): Secondary (Correctly reporting timeouts caused by its dependency)
>     - `pcscf` (P-CSCF): Symptomatic (Observing the end-to-end SIP registration failure)
> - **recommendation**: The operator should investigate the HSS (`pyhss`) container. A good first step would be to examine the internal application logs within the `pyhss` container for any errors or warnings related to Diameter message processing. It would also be prudent to verify the container's resource utilization (CPU, memory) to rule out a performance bottleneck.
> - **confidence**: low
> - **explanation**: The investigation focused on three hypotheses: an unresponsive HSS (`h1`), a faulty I-CSCF (`h2`), and a faulty P-CSCF (`h3`). Probes successfully disproved that the I-CSCF was at fault, showing it was correctly configured and had a clear network path to the HSS. This indicates the timeouts it reported were legitimate. The HSS was confirmed to be reachable on the network, but this does not guarantee its application is healthy. The combination of a correctly operating I-CSCF reporting timeouts and a reachable-but-unresponsive HSS points to an application-layer fault within the HSS. The final confidence is **low** because the automated investigation of the P-CSCF (`h3`) was inconclusive; the Evidence Validator determined that the sub-agent for this hypothesis failed to run and fabricated its findings, which reduces trust in the overall automated diagnosis.

### Scoring Breakdown

**Overall score: 40%**

**Scorer assessment:** The agent correctly identified the severity and fault type of the observed symptoms and showed good layer accuracy in its intermediate reasoning, but completely missed the actual root cause and primary affected component. Its low confidence was well-calibrated given the incorrect diagnosis.

| Dimension | Result | Rationale |
|-----------|--------|-----------|
| Root cause correct | No | The simulated failure was the killing of the 'mongo' component, making it unreachable. The agent's final diagnosis incorrectly identifies the HSS ('pyhss') application as the root cause, stating it is unresponsive to Diameter requests. While the intermediate network analysis correctly notes 'container 'mongo' is exited', this is not carried through to the final root cause in the AGENT DIAGNOSIS. |
| Component overlap | 0% | The primary affected component in the simulated failure was 'mongo'. The agent's final diagnosis lists 'pyhss' (HSS) as the 'Root Cause' in its affected components, completely missing 'mongo'. |
| Severity correct | Yes | The simulated failure (MongoDB killed) would lead to a complete outage for dependent services. The agent correctly identifies a 'system-wide' failure of IMS registration, with '100% SIP error rates' and 'total failure', which matches the severity of a complete outage. |
| Fault type identified | Yes | The simulated failure involved 'mongo' being 'completely unreachable (down/not responding)'. The agent's diagnosis describes the HSS as 'not responding' and its 'application layer is unresponsive', which correctly identifies the observable fault type (unresponsiveness/unreachability), even if attributed to the wrong component. |
| Layer accuracy | Yes | The 'mongo' component belongs to the 'infrastructure' layer. The agent's intermediate network analysis correctly identifies 'container 'mongo' is exited' and attributes it to the 'infrastructure' layer with a 'yellow' rating. It also correctly attributes the observed IMS registration failure to the 'ims' layer with a 'red' rating. There is no misattribution of layers. |
| Confidence calibrated | Yes | The agent's diagnosis for the root cause and primary affected component is incorrect. However, it states 'confidence: low' and provides a rationale about inconclusive investigations and fabricated findings, indicating it is aware of the limitations and uncertainty in its diagnosis. This low confidence is appropriate given the incorrect root cause. |

**Ranking:** The agent provided a single root cause candidate ('pyhss') in its final diagnosis, which was incorrect. Therefore, the correct cause was not listed.


### Token Usage

| Metric | Count |
|--------|-------|
| Input tokens | 160,669 |
| Output tokens | 5,560 |
| Thinking tokens | 16,810 |
| **Total tokens** | **183,039** |

**Per-phase breakdown:**

| Phase | Tokens | Tool Calls | LLM Calls |
|-------|--------|------------|-----------|
| AnomalyScreener | 0 | 0 | 0 |
| EventAggregator | 0 | 0 | 0 |
| CorrelationAnalyzer | 0 | 0 | 0 |
| NetworkAnalystAgent | 34,655 | 3 | 4 |
| InstructionGeneratorAgent | 17,080 | 1 | 2 |
| InvestigatorAgent_h1 | 49,821 | 5 | 6 |
| InvestigatorAgent_h2 | 68,018 | 6 | 6 |
| InvestigatorAgent_h3 | 5,786 | 0 | 1 |
| EvidenceValidator | 0 | 0 | 0 |
| SynthesisAgent | 7,679 | 0 | 1 |


## Resolution

**Heal method:** scheduled  
**Recovery time:** 359.8s
