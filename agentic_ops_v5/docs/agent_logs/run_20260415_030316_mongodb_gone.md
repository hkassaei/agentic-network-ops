# Episode Report: MongoDB Gone

**Agent:** v5  
**Episode ID:** ep_20260415_025917_mongodb_gone  
**Date:** 2026-04-15T02:59:19.422651+00:00  
**Duration:** 236.4s  

---

## Scenario

**Category:** container  
**Blast radius:** global  
**Description:** Kill MongoDB — the 5G core subscriber data store. UDR loses its backend, new PDU sessions cannot be created, and subscriber queries fail. Existing sessions may survive briefly.

## Faults Injected

- **container_kill** on `mongo`

## Baseline (Pre-Fault)

Stack phase before injection: **ready**
All containers running at baseline.

## Fault Propagation Verification

**Verdict:** ✅ `confirmed`

- **Wait:** 0s
- **Actual elapsed:** 0.0s
- **Nodes with significant deltas:** 3
- **Nodes with any drift:** 4

## Symptoms Observed

Symptoms detected: **Yes**  
Observation iterations: 1

### Metrics Changes

| Node | Metric | Baseline | Current | Delta |
|------|--------|----------|---------|-------|
| icscf | core:rcv_requests_register | 30.0 | 48.0 | 18.0 |
| icscf | ims_icscf:uar_replies_response_time | 822.0 | 1418.0 | 596.0 |
| icscf | cdp:replies_response_time | 822.0 | 1418.0 | 596.0 |
| icscf | cdp:replies_received | 15.0 | 24.0 | 9.0 |
| icscf | ims_icscf:uar_replies_received | 15.0 | 24.0 | 9.0 |
| pcscf | core:rcv_requests_register | 30.0 | 48.0 | 18.0 |
| pcscf | script:register_success | 15.0 | 24.0 | 9.0 |
| pcscf | sl:1xx_replies | 39.0 | 59.0 | 20.0 |
| pcscf | sl:4xx_replies | 9.0 | 11.0 | 2.0 |
| pcscf | script:register_time | 4187.0 | 6881.0 | 2694.0 |
| pcscf | core:rcv_requests_invite | 9.0 | 11.0 | 2.0 |
| scscf | core:rcv_requests_register | 30.0 | 48.0 | 18.0 |
| scscf | ims_auth:mar_replies_received | 15.0 | 24.0 | 9.0 |
| scscf | cdp:replies_response_time | 3107.0 | 5072.0 | 1965.0 |
| scscf | cdp:replies_received | 30.0 | 48.0 | 18.0 |
| scscf | ims_registrar_scscf:sar_replies_received | 15.0 | 24.0 | 9.0 |
| scscf | ims_registrar_scscf:sar_replies_response_time | 1570.0 | 2528.0 | 958.0 |
| scscf | ims_auth:mar_replies_response_time | 1537.0 | 2544.0 | 1007.0 |
| scscf | ims_registrar_scscf:accepted_regs | 15.0 | 24.0 | 9.0 |

## Anomaly Screening (Phase 0)

**ANOMALY DETECTED.** Overall anomaly score: 0.96 (threshold: 0.70, trained on 99 healthy snapshots). The current metric pattern is statistically different from the learned healthy baseline. Something in the network has changed.

The following specific metrics were flagged as the top contributors to the anomaly. These MUST be reflected in your layer ratings:

| Component | Metric | Current | Learned Normal | Severity |
|-----------|--------|---------|---------------|----------|
| rtpengine | packet_loss_standard_deviation | 15.00 | 0.00 | HIGH |
| rtpengine | average_packet_loss | 8.00 | 0.00 | HIGH |
| derived | pcscf_sip_error_ratio | 1.00 | 0.00 | HIGH |
| icscf | ims_icscf:lir_avg_response_time | 0.00 | 51.03 | HIGH |
| scscf | ims_auth:mar_avg_response_time | 104.00 | 88.89 | MEDIUM |
| scscf | ims_registrar_scscf:sar_avg_response_time | 105.00 | 119.92 | MEDIUM |
| normalized | upf.gtp_outdatapktn3upf_per_ue | 0.03 | 2.50 | MEDIUM |
| normalized | upf.gtp_indatapktn3upf_per_ue | 0.13 | 2.58 | MEDIUM |
| normalized | smf.bearers_per_ue | 2.00 | 2.66 | LOW |
| normalized | pcscf.dialogs_per_ue | 0.00 | 0.65 | LOW |

## Network Analysis (Phase 1)

**Summary:** The network is experiencing a critical failure in the infrastructure layer and severe degradation in the core and IMS layers, primarily due to an exited MongoDB container and significant data plane packet loss.

**Layer status:**

| Layer | Rating | Note |
|---|---|---|
| **infrastructure** | 🔴 RED | Critical database container (mongo) is down, impacting core network functions. |
| **ran** | 🟢 GREEN | RAN appears healthy, with UEs connected to the gNB and gNB connected to the AMF. |
| **core** | 🔴 RED | Core is critically impacted by infrastructure failure (MongoDB) and shows signs of data plane degradation at the UPF. |
| **ims** | 🔴 RED | IMS is heavily degraded with significant packet loss in the media path (RTPEngine) and SIP signaling errors. |

**INFRASTRUCTURE evidence:**
- mongo container is 'exited' per get_network_status.
- UDR to MongoDB link is 'INACTIVE' per get_network_topology.

**CORE evidence:**
- Dependency on 'mongo' container which has exited, breaking the UDR's data link.
- upf.gtp_indatapktn3upf_per_ue = 0.13 (vs 2.58 baseline) - MEDIUM severity anomaly.
- upf.gtp_outdatapktn3upf_per_ue = 0.03 (vs 2.50 baseline) - MEDIUM severity anomaly.
- Discrepancy in UPF GTP counters: fivegs_ep_n3_gtp_indatapktn3upf=2266 vs fivegs_ep_n3_gtp_outdatapktn3upf=1409, suggesting packet loss.

**IMS evidence:**
- rtpengine.average_packet_loss = 8.00 (vs 0.00 baseline) - HIGH severity anomaly.
- rtpengine.packet_loss_standard_deviation = 15.00 (vs 0.00 baseline) - HIGH severity anomaly.
- derived.pcscf_sip_error_ratio = 1.00 (vs 0.00 baseline) - HIGH severity anomaly.
- icscf.ims_icscf:lir_avg_response_time = 0.00 (vs 51.03 baseline) - HIGH severity anomaly.

**Suspect components:**

- **mongo** (high): The container has exited. As the backend for UDR, its failure is critical and impacts 5G subscriber data management, likely causing control plane issues.
- **rtpengine** (high): Anomaly screener flagged with HIGH severity for significant packet loss (average_packet_loss: 8.0), directly impacting voice quality. This is a primary indicator of the service impact.
- **upf** (medium): Anomaly screener flagged anomalous GTP packet counters. Raw metrics show a large discrepancy between inbound (2266) and outbound (1409) GTP packets, suggesting significant packet loss. As the convergence point for UE traffic, this could be the root cause of both SIP and RTP issues.
- **pcscf** (low): High SIP error ratio is likely a downstream symptom of packet loss at the UPF (affecting SIP messages from the UE) or authentication failures stemming from the core/infrastructure issues.

**Investigation hint:** The investigation should focus on two primary areas. First, diagnose the critical failure of the `mongo` container, as its outage cripples the 5G core's ability to access subscriber data via UDR. Second, investigate the User Plane Function (UPF) as the likely source of data plane issues. The significant packet loss reported at `rtpengine` and SIP errors at `pcscf` are probably downstream symptoms of packet loss occurring within the UPF, which is the common path for all UE traffic.

**Tools called:** get_vonr_components, get_network_topology, get_network_status, get_nf_metrics, get_dp_quality_gauges, read_env_config, measure_rtt, compare_to_baseline, check_stack_rules


## Pattern Match (Phase 2)

**✅ Match:** MongoDB down — 5G subscriber data unavailable

- **Confidence:** very_high
- **Failure domain:** infrastructure
- **Matched signatures:** 1
  - `infrastructure_mongodb_down` (score: 1.0)
- **Baseline anomalies:** 2 metrics across 2 components


## Investigation Instruction (Phase 3)

> Instruction generation skipped: Network Analyst diagnosis is definitive.

## Investigation (Phase 4)

> Investigation skipped: Network Analyst produced a high-confidence diagnosis with definitive evidence (RED layer + high-confidence suspect). See Phase 1 analysis.

## Evidence Validation (Phase 5)

**Verdict:** clean
**Investigator confidence:** high
**Citations:** 4/4 verified

```
Investigator was intentionally skipped (Network Analyst diagnosis was definitive). Validating Network Analyst evidence only.
Network Analyst made 9 tool calls. Evidence validation: 4/4 NA citations verified.
Verdict: clean. Confidence: high.

Tool calls vs. citations:
  NetworkAnalystAgent:
    ✗ check_stack_rules — called but NOT cited in output
    ✗ compare_to_baseline — called but NOT cited in output
    ✗ get_dp_quality_gauges — called but NOT cited in output
    ✓ get_network_status — called AND cited (1x)
    ✓ get_network_topology — called AND cited (1x)
    ✓ get_nf_metrics — called AND cited (2x)
    ✗ get_vonr_components — called but NOT cited in output
    ✗ measure_rtt — called but NOT cited in output
    ✗ read_env_config — called but NOT cited in output
```

## Ground Truth

**Failure domain:** data_layer  
**Protocol impact:** multiple  
**Affected components:** mongo  
**Severity:** degraded

## Agent Diagnosis

**Model:** v5-adk/gemini-2.5-flash+gemini-2.5-pro  
**Time to diagnosis:** 105.1s

### Prompt to RCA Agent

```
The 5G SA + IMS stack is experiencing issues. Investigate and diagnose the root cause.
```

**Diagnosis:**

> ### causes
> - **summary**: The network is down due to a critical infrastructure failure where the MongoDB container has exited, making 5G subscriber data unavailable and leading to cascading failures in the core and IMS control planes.
> - **timeline**:
>     1. The `mongo` container, which provides the database backend for the UDR, exits unexpectedly.
>     2. The UDR's link to MongoDB becomes `INACTIVE`, crippling its ability to serve subscriber data.
>     3. Core network functions (UDM, AUSF) fail to access subscriber profiles, leading to control plane errors and an inability to process user registrations and sessions correctly.
>     4. Simultaneously, the UPF exhibits significant data plane packet loss, as seen in the large discrepancy between inbound and outbound GTP packet counts.
>     5. This packet loss in the data plane manifests as severe service degradation in the IMS layer, including high RTP packet loss at `rtpengine` and SIP signaling errors at the `pcscf`.
> - **root_cause**: The `mongo` container has exited. As the database for the User Data Repository (UDR), its failure makes all 5G subscriber data inaccessible to the core network. This is a fundamental infrastructure failure that breaks control plane functions dependent on subscriber information.
> - **affected_components**:
>     - `mongo`: Root Cause
>     - `udr`: Symptomatic
>     - `upf`: Secondary Failure (experiencing packet loss independent of the database issue)
>     - `rtpengine`: Symptomatic
>     - `pcscf`: Symptomatic
> - **recommendation**: The operator should first investigate the `mongo` container to determine why it exited by checking container logs and host system status. Secondly, investigate the UPF for the source of the severe data plane packet loss.
> - **confidence**: high
> - **explanation**: The primary cause of the outage is the exited `mongo` container, which has taken the User Data Repository offline and broken the 5G core's ability to manage subscriber data. This is confirmed by both network status and topology checks. A secondary, severe issue is the significant data plane packet loss occurring at the UPF, which is the direct cause of the poor voice quality and signaling failures observed in the IMS services.

### Scoring Breakdown

**Overall score: 100%**

**Scorer assessment:** The agent provided an outstanding diagnosis, accurately identifying the root cause, affected components, severity, and fault type, with correct layer attribution and appropriate confidence.

| Dimension | Result | Rationale |
|-----------|--------|-----------|
| Root cause correct | Yes | The agent correctly identified the 'mongo' container being 'exited' and its functional impact (subscriber data unavailability) as the root cause, which is semantically equivalent to the simulated failure mode of 'mongo' being unreachable/down. |
| Component overlap | 100% | The agent correctly identified 'mongo' as the primary root cause component. It also listed other affected components, which are downstream effects and do not detract from the score. |
| Severity correct | Yes | The agent described the network as 'down' due to a 'critical infrastructure failure' and 'fundamental infrastructure failure', leading to 'cascading failures' and 'heavily degraded' IMS, which accurately reflects the complete outage caused by the MongoDB failure. |
| Fault type identified | Yes | The agent identified the fault type as the 'mongo' container being 'exited' and its link becoming 'INACTIVE', which clearly indicates a component unreachable/down state. |
| Layer accuracy | Yes | The agent correctly attributed the 'mongo' failure to the 'infrastructure' layer, rating it RED with appropriate evidence, matching the ground truth. |
| Confidence calibrated | Yes | The agent stated 'high' confidence, which is appropriate given the accuracy and detail of its diagnosis, supported by tool evidence. |

**Ranking position:** #1 — The agent explicitly identified 'mongo' as the 'root_cause' and listed it first among affected and suspect components.


### Token Usage

| Metric | Count |
|--------|-------|
| Input tokens | 79,380 |
| Output tokens | 2,627 |
| Thinking tokens | 5,989 |
| **Total tokens** | **87,996** |

**Per-phase breakdown:**

| Phase | Tokens | Tool Calls | LLM Calls |
|-------|--------|------------|-----------|
| AnomalyScreener | 0 | 0 | 0 |
| NetworkAnalystAgent | 80,498 | 19 | 5 |
| PatternMatcherAgent | 0 | 0 | 0 |
| EvidenceValidatorAgent | 0 | 0 | 0 |
| SynthesisAgent | 7,498 | 0 | 1 |


## Resolution

**Heal method:** scheduled  
**Recovery time:** 236.4s
