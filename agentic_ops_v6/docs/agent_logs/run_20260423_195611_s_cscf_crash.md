# Episode Report: S-CSCF Crash

**Agent:** v6  
**Episode ID:** ep_20260423_195025_s_cscf_crash  
**Date:** 2026-04-23T19:50:27.236555+00:00  
**Duration:** 343.6s  

---

## Scenario

**Category:** container  
**Blast radius:** single_nf  
**Description:** Kill the S-CSCF (Serving-CSCF). This is the core SIP registrar and call controller. All IMS authentication stops, new registrations are impossible, and active calls will eventually drop.

## Faults Injected

- **container_kill** on `scscf`

## Baseline (Pre-Fault)

Stack phase before injection: **ready**
All containers running at baseline.

## Fault Propagation Verification

**Verdict:** ✅ `confirmed`

- **Wait:** 0s
- **Actual elapsed:** 0.0s
- **Nodes with significant deltas:** 4
- **Nodes with any drift:** 5

## Symptoms Observed

Symptoms detected: **Yes**  
Observation iterations: 1

## Anomaly Screening (Phase 0)

**ANOMALY DETECTED.** Overall anomaly score: 0.90 (threshold: 0.70, trained on 211 healthy snapshots). The current metric pattern is statistically different from the learned healthy baseline. Something in the network has changed.

The following metrics deviate from their learned-healthy baseline. Treat each as a semantic observation (meaning + numbers), not a number alone — the KB's interpretation is the authoritative reading.

- **`normalized.upf.gtp_outdatapktn3upf_per_ue`** (GTP-U downlink rate per UE (N3)) — current **0.01 packets_per_second** vs learned baseline **3.34 packets_per_second** (MEDIUM, drop)
    - **What it measures:** Health of the downlink user-plane path UPF → gNB. Typically mirrors
uplink rate during calls/data; asymmetry indicates directional
faults (e.g., UPF receiving from internet but not forwarding).
    - **Drop means:** No traffic leaving UPF toward RAN.
    - **Healthy typical range:** 0–10 packets_per_second
    - **Healthy invariant:** Per-UE — constant regardless of UE pool size. Roughly symmetric with uplink during healthy calls.

- **`normalized.upf.gtp_indatapktn3upf_per_ue`** (GTP-U uplink rate per UE (N3)) — current **0.12 packets_per_second** vs learned baseline **3.42 packets_per_second** (MEDIUM, drop)
    - **What it measures:** Health of the uplink user-plane path gNB → UPF. Drops to near-zero
during RAN or N3 outage; stays nonzero during active calls or data
sessions. Decoupled from SIP signaling (signals data plane, not
control plane).
    - **Drop means:** Data plane dead on uplink — UPF receiving no packets from gNB.
    - **Healthy typical range:** 0–10 packets_per_second
    - **Healthy invariant:** Per-UE — constant regardless of UE pool size. Rises during active VoNR calls (~100 pps for G.711 voice) and data transfer.

- **`normalized.pcscf.dialogs_per_ue`** (Active SIP dialogs per registered UE at P-CSCF) — current **0.00 count** vs learned baseline **0.57 count** (LOW, drop)
    - **What it measures:** How many calls per user are currently in progress at the P-CSCF.
Going to zero from a non-zero baseline means calls have ended
(normal) OR call setup is failing system-wide (degradation).
Together with rcv_requests_* it discriminates the two.
    - **Drop means:** No active calls. Normal at rest; suspicious if prior state had calls AND INVITE rate is also zero.
    - **Healthy typical range:** 0–1 count
    - **Healthy invariant:** Per-UE — scale-independent. 0 at rest, ~1 per active VoNR call.

- **`derived.upf_activity_during_calls`** — current **1.00** vs learned baseline **0.47** (LOW, spike). *(No KB context available — interpret from the metric name.)*


## Event Aggregation (Phase 1)

No events fired during this episode. Either no metric KB triggers matched, or the episode encountered no meaningful state transitions.

## Correlation Analysis (Phase 2)

No events fired — correlation engine had nothing to work with.

## Network Analysis (Phase 3)

**Summary:** The S-CSCF has crashed, bringing down the IMS control plane and preventing all call setups, which in turn causes the data plane to be silent.

**Layer status:**

| Layer | Rating | Note |
|---|---|---|
| **infrastructure** | 🟢 GREEN | The underlying container platform and network appear to be functioning correctly. |
| **ran** | 🟢 GREEN | The RAN appears healthy and connected to the core. |
| **core** | 🟡 YELLOW | The core data plane is inactive, but this is a symptom of the upstream IMS failure. The core control plane appears healthy. |
| **ims** | 🔴 RED | A critical component of the IMS subsystem has failed, making it the epicenter of the outage. |

**CORE evidence:**
- UPF GTP traffic is near zero, both uplink and downlink.
- All core network functions (AMF, SMF, UPF, etc.) are running.

**IMS evidence:**
- The S-CSCF container has exited.
- All SIP sessions are failing, resulting in zero active dialogs at the P-CSCF.

**Ranked hypotheses:**

- **`h1`** (fit=0.95, nf=scscf, specificity=specific):
    - **Statement:** The S-CSCF container has exited, causing a complete failure of the IMS control plane. This prevents SIP session setup, leading to zero active calls and consequently, no user plane traffic through the UPF and RTPEngine.
    - **Falsification probes:**
        - Check the container logs for 'scscf' to determine the cause of the exit.
        - Attempt to restart the 'scscf' container and observe if IMS registrations and call setups resume.
        - Check 'icscf' logs for errors related to forwarding requests to the S-CSCF.
- **`h2`** (fit=0.30, nf=mysql, specificity=specific):
    - **Statement:** The IMS database (MySQL) experienced a failure or became unreachable, which in turn caused the S-CSCF to crash after being unable to access required data.
    - **Falsification probes:**
        - Check the logs of the 'scscf' container for any database connection errors preceding its crash.
        - Verify connectivity from other IMS components (e.g., 'pyhss', 'icscf') to the MySQL database, as they appear to be functioning.
        - Check the logs of the 'mysql' container for any internal errors.
- **`h3`** (fit=0.20, nf=upf, specificity=moderate):
    - **Statement:** A data plane partition exists between the RAN and the UPF on the N3 interface, and the S-CSCF failure is a separate, coincidental fault.
    - **Falsification probes:**
        - After resolving the S-CSCF failure, attempt a VoNR call; successful media flow would disprove a separate data plane fault.
        - Measure RTT and packet loss between the UPF and other core components like the SMF to check for broader network issues.


## Falsification Plans (Phase 4)

**3 falsification plan(s) — one per hypothesis:**

### Plan for `h1` (target: `scscf`)

**Hypothesis:** The S-CSCF container has exited, causing a complete failure of the IMS control plane. This prevents SIP session setup, leading to zero active calls and consequently, no user plane traffic through the UPF and RTPEngine.

**Probes (3):**
1. **`get_network_status`** — Check status of all containers, especially 'scscf'
    - *Expected if hypothesis holds:* The 'scscf' container has status 'exited'.
    - *Falsifying observation:* The 'scscf' container is in 'running' state. This would directly contradict the core claim of the hypothesis.
2. **`run_kamcmd`** — container='icscf', command='dispatcher.list'
    - *Expected if hypothesis holds:* The S-CSCF destination in the dispatcher list will be marked as inactive (e.g., flags='i' or 'p').
    - *Falsifying observation:* The S-CSCF destination is marked as active (flags='a'). This implies the I-CSCF can still reach the S-CSCF, contradicting that it's the cause of failure.
3. **`measure_rtt`** — from='icscf', to_ip='scscf_ip'
    - *Expected if hypothesis holds:* 100% packet loss or high latency, as the container is not running.
    - *Falsifying observation:* Normal RTT (<5ms) and 0% packet loss. This would prove the container is up and reachable on the network, falsifying the 'exited' claim.

*Notes:* This plan focuses on verifying the primary claim of the hypothesis: the S-CSCF container has exited. Probes check the container's status directly, its perceived status from the upstream I-CSCF, and its basic network reachability.

### Plan for `h2` (target: `mysql`)

**Hypothesis:** The IMS database (MySQL) experienced a failure or became unreachable, which in turn caused the S-CSCF to crash after being unable to access required data.

**Probes (3):**
1. **`get_network_status`** — Check status of 'mysql' container
    - *Expected if hypothesis holds:* The 'mysql' container is in an 'exited' or 'unhealthy' state.
    - *Falsifying observation:* The 'mysql' container is 'running' and healthy.
2. **`query_subscriber`** — imsi='<a_known_imsi>'
    - *Expected if hypothesis holds:* The query fails or times out, as PyHSS (which relies on MySQL) cannot access the database.
    - *Falsifying observation:* The query returns valid subscriber data. This proves that another component (PyHSS) can successfully use the MySQL database, making a general database failure unlikely.
3. **`measure_rtt`** — from='pyhss', to_ip='mysql_ip'
    - *Expected if hypothesis holds:* High packet loss or latency.
    - *Falsifying observation:* Normal RTT (<5ms). This, combined with a successful subscriber query, would strongly falsify the hypothesis that MySQL is unreachable.

*Notes:* This plan triangulates the health of the MySQL database. It checks the container status, application-level functionality via a dependent service (PyHSS), and network-level reachability. A failure of S-CSCF due to MySQL would likely also manifest in these other areas.

### Plan for `h3` (target: `upf`)

**Hypothesis:** A data plane partition exists between the RAN and the UPF on the N3 interface, and the S-CSCF failure is a separate, coincidental fault.

**Probes (3):**
1. **`measure_rtt`** — from='nr_gnb', to_ip='upf_ip'
    - *Expected if hypothesis holds:* 100% packet loss or high latency, indicating a partition on the N3 interface.
    - *Falsifying observation:* Normal RTT (<5ms) and 0% packet loss. This directly falsifies the claim of a data plane partition on the N3 interface.
2. **`measure_rtt`** — from='upf', to_ip='nr_gnb_ip'
    - *Expected if hypothesis holds:* 100% packet loss or high latency.
    - *Falsifying observation:* Normal RTT (<5ms) and 0% packet loss. This serves as a triangulation probe and confirms bidirectional connectivity between the RAN and UPF.
3. **`get_nf_metrics`** — Get metrics for 'smf', specifically looking for PDU session counts.
    - *Expected if hypothesis holds:* A non-zero PDU session count. If sessions exist but no traffic flows (as confirmed by DP gauges), it supports the idea of a data plane-specific issue.
    - *Falsifying observation:* Zero PDU sessions. If no sessions are established, the data plane is expected to be silent. This would indicate the problem is not a data plane partition, but rather a control plane issue preventing session setup, thus falsifying the hypothesis.

*Notes:* This plan directly targets the main testable claim of the hypothesis: the N3 interface partition. The first two probes test bidirectional connectivity. The third probe investigates the alternative cause for a silent data plane – if no PDU sessions are even established, there is no reason to suspect a data plane partition.


## Parallel Investigators (Phase 5)

**3 sub-Investigator verdict(s):** **1 NOT_DISPROVEN**, **2 DISPROVEN**

### `h1` — ✅ **NOT_DISPROVEN**

**Hypothesis:** The S-CSCF container has exited, causing a complete failure of the IMS control plane. This prevents SIP session setup, leading to zero active calls and consequently, no user plane traffic through the UPF and RTPEngine.

**Reasoning:** All executed probes yielded results consistent with the hypothesis. The primary claim, that the S-CSCF container has exited, was directly confirmed. The network-level consequence of this, the unreachability of the S-CSCF from the I-CSCF, was also confirmed via a ping test showing 100% packet loss. No evidence was found to contradict the hypothesis.

**Probes executed (3):**
- **Check status of all containers, especially 'scscf'** ✓ CONSISTENT
    - *Tool:* `get_network_status()`
    - *Observation:* [EVIDENCE: get_network_status() -> "'scscf': 'exited'"]
    - *Comment:* This directly confirms the central assertion of the hypothesis that the S-CSCF container has exited.
- **container='icscf', command='dispatcher.list'** ~ AMBIGUOUS
    - *Tool:* `run_kamcmd(container='icscf', command='dispatcher.list')`
    - *Observation:* [EVIDENCE: run_kamcmd('icscf', 'dispatcher.list') -> 'error: 500 - command dispatcher.list not found']
    - *Comment:* The tool command failed, so the S-CSCF's status in the I-CSCF's dispatcher list could not be verified. This result doesn't contradict the hypothesis, but it also doesn't provide the expected confirmation.
- **from='icscf', to_ip='scscf_ip'** ✓ CONSISTENT
    - *Tool:* `measure_rtt(container='icscf', target_ip='172.22.0.20')`
    - *Observation:* [EVIDENCE: measure_rtt('icscf', '172.22.0.20') -> '3 packets transmitted, 0 received, +3 errors, 100% packet loss']
    - *Comment:* This confirms that the S-CSCF is unreachable from the I-CSCF at the network layer, which is the expected result for an exited container. This supports the hypothesis.

### `h2` — ❌ **DISPROVEN**

**Hypothesis:** The IMS database (MySQL) experienced a failure or became unreachable, which in turn caused the S-CSCF to crash after being unable to access required data.

**Reasoning:** The `mysql` container is confirmed to be in a 'running' state, and the `pyhss` container, which also depends on MySQL, was able to successfully query subscriber data. This demonstrates that the database is both running and accessible at the application layer, directly contradicting the hypothesis that a database failure caused the S-CSCF crash.

**Probes executed (3):**
- **Check status of 'mysql' container** ✗ CONTRADICTS
    - *Tool:* `default_api.get_network_status()`
    - *Observation:* [EVIDENCE: get_network_status() -> "{\"containers\": {\"mysql\": \"running\"}}"]
    - *Comment:* The container status is 'running', which contradicts the 'exited' or 'unhealthy' state expected by the hypothesis.
- **imsi='<a_known_imsi>'** ✗ CONTRADICTS
    - *Tool:* `default_api.query_subscriber(imsi='001011234567891')`
    - *Observation:* [EVIDENCE: query_subscriber(imsi='001011234567891') -> "{\"ims_subscriber\": {\"subscriber_id\": 27,...}}"]
    - *Comment:* The query returned valid subscriber data. This proves that another component (PyHSS) can successfully use the MySQL database, making a general database failure unlikely.
- **from='pyhss', to_ip='mysql_ip'** ~ AMBIGUOUS
    - *Tool:* `default_api.measure_rtt(container='pyhss', target_ip='172.22.0.17')`
    - *Observation:* [EVIDENCE: measure_rtt(container = "pyhss", target_ip = "172.22.0.17") -> "Ping failed from pyhss to 172.22.0.17: OCI runtime exec failed: exec failed: unable to start container process: exec: \"ping\": executable file not found in $PATH: unknown"]
    - *Comment:* The RTT measurement could not be performed because the 'ping' utility is not available in the 'pyhss' container. This result is inconclusive and does not provide evidence for or against the hypothesis.

**Alternative suspects:** scscf

### `h3` — ❌ **DISPROVEN**

**Hypothesis:** A data plane partition exists between the RAN and the UPF on the N3 interface, and the S-CSCF failure is a separate, coincidental fault.

**Reasoning:** The core claim of a data plane partition on the N3 interface was falsified. A round-trip time measurement from the UPF to the gNB showed 0% packet loss and sub-millisecond latency, indicating a healthy network path. This directly contradicts the expectation of packet loss or high latency that would be present during a network partition. The silent data plane is therefore not due to a network issue on N3, but is an expected consequence of the control plane failure at the S-CSCF, which prevents call setup.

**Probes executed (3):**
- **Measure RTT from UPF to RAN (nr_gnb) to test for a partition on the N3 interface.** ✗ CONTRADICTS
    - *Tool:* `measure_rtt(container='upf', target_ip='172.22.0.23')`
    - *Observation:* [EVIDENCE: measure_rtt("upf", "172.22.0.23") -> "3 packets transmitted, 3 received, 0% packet loss, time 2050ms\nrtt min/avg/max/mdev = 0.079/0.135/0.244/0.077 ms"]
    - *Comment:* The RTT is normal (<1ms) and there is 0% packet loss. This result directly falsifies the hypothesis that a data plane partition exists on the N3 interface, as connectivity between the UPF and the RAN is healthy in this direction.
- **Get metrics for 'smf', specifically looking for PDU session counts.** ~ AMBIGUOUS
    - *Tool:* `get_nf_metrics()`
    - *Observation:* [EVIDENCE: get_nf_metrics() -> "SMF [4 PDU] (via prometheus):\n  fivegs_smffunction_sm_sessionnbr = 4.0  [counter]\n  pfcp_sessions_active = 4.0  [counter]"]
    - *Comment:* The probe found non-zero cumulative counters for PDU sessions on the SMF. This is technically consistent with the hypothesis's expectation of existing sessions. However, these are lifetime counters, not gauges of currently active sessions, making the observation ambiguous. Given the strong contradictory evidence from the RTT probe, these counters likely represent historical sessions, not active ones that are failing to pass traffic.
- **Measure RTT from RAN to UPF as a triangulation probe.** ~ AMBIGUOUS
    - *Tool:* `measure_rtt(container = "nr-gnb", target_ip = "172.22.0.8")`
    - *Observation:* [EVIDENCE: measure_rtt("nr-gnb", "172.22.0.8") -> "Unknown container 'nr-gnb'."]
    - *Comment:* The probe could not be executed as the tool does not support initiating measurements from the RAN container. While this prevents a full bidirectional test, the successful probe in the reverse direction (UPF to RAN) provides strong evidence against a partition.

**Alternative suspects:** scscf


## Evidence Validation (Phase 6)

**Overall verdict:** clean
**Overall confidence:** high

**Per sub-Investigator:**

| Agent | Tool Calls | Citations | Verdict | Confidence |
|---|---|---|---|---|
| `InvestigatorAgent_h1` | 4 | 3/3 | clean | high |
| `InvestigatorAgent_h2` | 4 | 3/3 | clean | high |
| `InvestigatorAgent_h3` | 5 | 3/3 | clean | high |


## Ground Truth

**Failure domain:** ims_signaling  
**Protocol impact:** SIP  
**Affected components:** scscf  
**Severity:** degraded

## Agent Diagnosis

**Model:** v6-adk/gemini-2.5-flash+gemini-2.5-pro  
**Time to diagnosis:** 206.1s

### Prompt to RCA Agent

```
The 5G SA + IMS stack is experiencing issues. Investigate and diagnose the root cause.
```

**Diagnosis:**

> ### causes
> - **summary**: The Serving-CSCF (S-CSCF) container has crashed, which brought down the IMS control plane and prevented all new call setups.
> - **timeline**:
>     1. The `scscf` container exited for an unknown reason.
>     2. As a result, the IMS control plane became non-functional, as confirmed by the I-CSCF being unable to reach the S-CSCF.
>     3. This failure prevented any new SIP sessions from being established.
>     4. Consequently, no user plane traffic was generated, leading to an inactive data plane (UPF, RTPEngine).
> - **root_cause**: The primary suspect is the `scscf` component. The S-CSCF container has exited, causing a complete failure of the IMS control plane.
> - **affected_components**:
>     - `scscf`: Root Cause
>     - `icscf`: Secondary
>     - `upf`: Symptomatic
>     - `rtpengine`: Symptomatic
> - **recommendation**: Verify the logs for the `scscf` container to determine the reason for its exit. Further investigation of the `icscf` logs may also show errors related to its inability to forward session requests to the now-unreachable S-CSCF.
> - **confidence**: high
> - **explanation**: The root cause of the outage is the failure of the Serving-CSCF (S-CSCF). The investigation confirmed hypothesis `h1`: the `scscf` container has exited and is unreachable at the network layer from other IMS components like the I-CSCF. This single failure in the IMS control plane fully explains the observed symptoms, including the lack of call setup and the resulting silence on the data plane. Alternative hypotheses were disproven; a database failure (`h2`) was ruled out as the MySQL container is running and accessible, and a data plane partition (`h3`) was falsified by direct connectivity tests showing a healthy link between the core and RAN.

### Scoring Breakdown

**Overall score: 100%**

**Scorer assessment:** The agent provided an excellent and highly accurate diagnosis, correctly identifying the root cause, affected components, severity, and fault type, with appropriate confidence and layer attribution.

| Dimension | Result | Rationale |
|-----------|--------|-----------|
| Root cause correct | Yes | The agent correctly identified the S-CSCF as the root cause, stating that the 'scscf' container has exited and caused a complete failure of the IMS control plane, which perfectly matches the simulated failure mode of an S-CSCF crash leading to unreachability. |
| Component overlap | 100% | The agent explicitly lists `scscf` as the 'Root Cause' in the `affected_components` section, which is the primary affected component in the simulated failure. |
| Severity correct | Yes | The agent correctly assessed the severity as a 'complete failure of the IMS control plane' and that it 'prevented all new call setups', which aligns with the simulated 'component completely unreachable (down/not responding)' state. |
| Fault type identified | Yes | The agent identified the fault type as the 'scscf container has exited' and is 'unreachable at the network layer', which accurately describes the observable failure mode of a component being down/not responding. |
| Layer accuracy | Yes | The agent's network analysis correctly rated the 'ims' layer as 'red' and provided evidence that 'The S-CSCF container has exited', aligning with the ground truth that 'scscf' belongs to the 'ims' layer. |
| Confidence calibrated | Yes | The agent's diagnosis is highly accurate and well-supported by evidence, making its 'high' confidence level appropriate and well-calibrated. |

**Ranking position:** #1 — The agent provided a single, clear root cause in its `root_cause` field, which was correct.


### Token Usage

| Metric | Count |
|--------|-------|
| Input tokens | 204,963 |
| Output tokens | 5,172 |
| Thinking tokens | 16,769 |
| **Total tokens** | **226,904** |

**Per-phase breakdown:**

| Phase | Tokens | Tool Calls | LLM Calls |
|-------|--------|------------|-----------|
| AnomalyScreener | 0 | 0 | 0 |
| EventAggregator | 0 | 0 | 0 |
| CorrelationAnalyzer | 0 | 0 | 0 |
| NetworkAnalystAgent | 36,665 | 3 | 4 |
| InstructionGeneratorAgent | 41,670 | 3 | 4 |
| InvestigatorAgent_h1 | 34,185 | 4 | 4 |
| InvestigatorAgent_h2 | 48,243 | 4 | 5 |
| InvestigatorAgent_h3 | 60,458 | 5 | 6 |
| EvidenceValidator | 0 | 0 | 0 |
| SynthesisAgent | 5,683 | 0 | 1 |


## Resolution

**Heal method:** scheduled  
**Recovery time:** 343.6s
