# Episode Report: MongoDB Gone

**Agent:** v7  
**Episode ID:** ep_20260521_124213_mongodb_gone  
**Date:** 2026-05-21T12:42:15.015985+00:00  
**Duration:** 333.5s  

---

## Scenario

**Category:** container  
**Blast radius:** global  
**Description:** Kill MongoDB — the 5G core subscriber data store. UDR loses its backend, new PDU sessions cannot be created, and subscriber queries fail. Existing sessions may survive briefly.

## Faults Injected

- **container_kill** on `mongo`

## Baseline (Pre-Fault)

Stack phase before injection: **ready**
All containers running at baseline.

## Fault Propagation Verification

**Verdict:** ✅ `confirmed`

- **Propagation window:** 126s (ObservationTrafficAgent drove traffic for this window; verifier added wait=0s on top)
- **Nodes with significant deltas:** 3
- **Nodes with any drift:** 4

## Symptoms Observed

Symptoms detected: **Yes**  
Observation iterations: 1

## Anomaly Screening (Phase 0)

No anomalies detected by the statistical screener.

## Symptom Classifier (Phase 0.5)

**Label:** `application_layer`  
**Flag counts:** transport=0, application=0, ambiguous=0

**Rationale:**

```
No anomaly flags in the screener output. Nothing for the transport-layer path walk to localize; the orchestrator routes through the application-layer pipeline by default.
```

## Transport-Layer Route (Phase 0.6)

*Skipped — classifier label is `application_layer`. Routes through the app-layer pipeline (Phases 1-7) below.*

## Event Aggregation (Phase 1)

No events fired during this episode. Either no metric KB triggers matched, or the episode encountered no meaningful state transitions.

## Correlation Analysis (Phase 2)

No events fired — correlation engine had nothing to work with.

## RAG & Operational Lessons (Phase 2.5)

### RAG retrieval — prior similar episodes

**Status:** `no_flags` — no_flags (corpus_size=102)
**Index:** `/home/ehoskas/agentic-network-ops/rag_index`  **Corpus size:** 102 cases

*Phase 0 produced no anomaly flags, so the query couldn't be built — no retrieval ran. Likely a clean stack with a transient symptom that subsided.*

### Operational lessons — hand-authored rules corpus

**Status:** `injected` — lessons=16, chars=15678
**Path:** `/home/ehoskas/agentic-network-ops/agentic_ops_common/rag/lessons.yaml`  **Count:** 16  **Block size:** 15678 chars
**Injected lesson IDs:** `L01`, `L02`, `L03`, `L04`, `L05`, `L06`, `L07`, `L08`, `L09`, `L10`, `L11`, `L12`, `L13`, `L14`, `L15`, `L16`

### NA citations of the injected content

**Cited lesson IDs:**
- `L10`

*Citations are verbatim string matches — case_ids and lesson_ids appear in the NA's emitted text (summary, hypothesis statements, or layer notes). The EvidenceValidator can audit any cited case by following its `source_episode_path` from the retrieval table above.*

## Network Analysis (Phase 3)

**Summary:** The mongo container has unexpectedly exited (Lesson L10), breaking database connections for UDR, PCF, NSSF, and BSF across the core network.

**Ranked hypotheses:**

- **`h1`** (fit=0.95, nf=mongo, specificity=specific):
    - **Statement:** mongo is the source of the failure, as its container has unexpectedly exited, causing database connection failures for dependent core components (UDR, PCF, NSSF, BSF). (Per L10)
    - **Falsification probes:**
        - Check docker logs for the mongo container to identify crash reasons or exit codes.
        - Attempt to manually restart the mongo container and observe if it fails to bind or crashes again.


## Falsification Plans (Phase 4)

**1 falsification plan(s) — one per hypothesis:**

### Plan for `h1` (target: `mongo`)

**Hypothesis:** mongo is the source of the failure, as its container has unexpectedly exited, causing database connection failures for dependent core components (UDR, PCF, NSSF, BSF).

**Probes (4):**
1. **`get_network_status`** — container: mongo
    - *Expected if hypothesis holds:* The probe's reading confirms the mongo container is in an Exited or Down state.
    - *Falsifying observation:* The probe's reading confirms the mongo container is Up and healthy.
2. **`check_process_listeners`** — container: mongo
    - *Expected if hypothesis holds:* The probe returns an error or shows empty output for port 27017.
    - *Falsifying observation:* The probe returns an active listener bound to port 27017.
3. **`measure_rtt`** — from: udr, to_ip: mongo
    - *Expected if hypothesis holds:* The probe shows 100% packet loss or unreachability to mongo.
    - *Falsifying observation:* The probe shows successful pings, which is inconsistent with mongo being down.
4. **`measure_rtt`** — from: udr, to_ip: nrf
    - *Expected if hypothesis holds:* The probe shows successful pings, confirming the failure in the previous probe is specific to mongo.
    - *Falsifying observation:* The probe shows packet loss or unreachability, indicating the failure is attributable to one of the conflated UDR or network elements rather than mongo itself.

*Notes:* Using get_network_status and check_process_listeners for direct state observation. Paired measure_rtt probes confirm network unreachability specific to mongo from a dependent NF (UDR).


## Parallel Investigators (Phase 5)

**1 sub-Investigator verdict(s):** **1 NOT_DISPROVEN**

### `h1` — ✅ **NOT_DISPROVEN**

**Hypothesis:** mongo is the source of the failure, as its container has unexpectedly exited, causing database connection failures for dependent core components (UDR, PCF, NSSF, BSF). (Per L10)

**Reasoning:** [Multi-shot consensus — both shots returned NOT_DISPROVEN.]

Shot 1: The get_network_status probe directly confirms that the mongo container is in the 'exited' and 'down_or_absent' state. Network reachability tests from udr to mongo fail due to name resolution (which is expected for a down container), while udr to nrf succeeds, confirming the failure is isolated to mongo and not a general network stack issue on udr. The hypothesis holds.

Shot 2: The network status check confirms that the mongo container is in an exited state. Network routing tests show that dependent components like UDR cannot resolve or reach mongo, while successfully maintaining reachability to other components like NRF, confirming the localized failure of the mongo container.

**Probes executed (4):**
- **Check network status to see if mongo container is down or exited.** ✓ CONSISTENT
    - *Tool:* `get_network_status()`
    - *Observation:* [EVIDENCE: get_network_status() -> "\"down_or_absent\": [\"mongo\"], \"containers\": {\"mongo\": \"exited\"}"]
    - *Comment:* Directly confirms the mongo container is down/exited.
- **Check if mongo is listening on port 27017.** ~ AMBIGUOUS
    - *Tool:* `check_process_listeners("mongo")`
    - *Observation:* [EVIDENCE: check_process_listeners("mongo") -> "PROBE_TOOL_UNAVAILABLE: check_process_listeners cannot run on container `mongo`"]
    - *Comment:* The container was already confirmed down, but the tool itself is missing the required binary ss/netstat. No evidence gathered from this specific probe.
- **Check network reachability from udr to mongo.** ✓ CONSISTENT
    - *Tool:* `measure_rtt("udr", "mongo")`
    - *Observation:* [EVIDENCE: measure_rtt("udr", "mongo") -> "Ping failed from 'udr' to 'mongo': ping: mongo: Temporary failure in name resolution"]
    - *Comment:* Confirms mongo is completely unreachable from a dependent NF (udr), as expected when the container is down.
- **Check network reachability from udr to nrf to isolate the failure to mongo.** ✓ CONSISTENT
    - *Tool:* `measure_rtt("udr", "nrf")`
    - *Observation:* [EVIDENCE: measure_rtt("udr", "nrf") -> "66 packets transmitted, 66 received, 0% packet loss"]
    - *Comment:* Confirms the failure to reach mongo from udr is not due to a network stack issue on udr, narrowing the fault down to mongo itself.


## Evidence Validation (Phase 6)

**Overall verdict:** clean
**Overall confidence:** high

**Per sub-Investigator:**

| Agent | Tool Calls | Citations | Verdict | Confidence |
|---|---|---|---|---|
| `InvestigatorAgent_h1` | 8 | 4/4 | clean | high |


## Ground Truth

**Failure domain:** data_layer  
**Protocol impact:** multiple  
**Affected components:** mongo  
**Severity:** degraded

## Agent Diagnosis

**Model:** v7-adk/pro=gemini-3.1-pro-preview+flash=gemini-3.5-flash  
**Time to diagnosis:** 196.6s

### Prompt to RCA Agent

```
The 5G SA + IMS stack is experiencing issues. Investigate and diagnose the root cause.
```

**Diagnosis:**

> ### causes
> - **summary**: The mongo database container has unexpectedly exited, breaking database connections for dependent 5G core network functions.
> - **timeline**:
>     1. Network functions including UDR, PCF, NSSF, and BSF experienced database connection failures.
>     2. Network status check confirmed the mongo container is in an 'exited' and 'down_or_absent' state.
>     3. Ping from UDR to mongo failed with a temporary failure in name resolution.
>     4. Ping from UDR to NRF succeeded, isolating the connectivity issue strictly to the mongo container.
> - **root_cause**: The mongo container is in an exited state, making it unreachable and unresolvable to dependent components like UDR, PCF, NSSF, and BSF. (primary_suspect_nf: `mongo`)
> - **affected_components**:
>     - `?`: ?
>     - `?`: ?
>     - `?`: ?
>     - `?`: ?
>     - `?`: ?
> - **recommendation**: Verify the mongo container state and inspect the container logs to determine the cause of the unexpected exit.
> - **confidence**: medium
> - **verdict_kind**: confirmed
> - **explanation**: The failure originates from the mongo database container, which has unexpectedly crashed or exited. Diagnostic network status checks explicitly show the mongo container as 'exited' and 'down_or_absent'. Reachability tests from the UDR confirm that the mongo hostname cannot be resolved, resulting in a complete failure for dependent network functions (UDR, PCF, NSSF, BSF) attempting to connect to the database. Control tests verified that the UDR can successfully reach other components like the NRF, ruling out a broader network stack issue and isolating the root cause entirely to the mongo container.

### Scoring Breakdown

**Overall score: 80%**

**Scorer assessment:** The agent successfully diagnosed the MongoDB outage as the root cause, but failed to populate the structured affected components list.

| Dimension | Result | Rationale |
|-----------|--------|-----------|
| Root cause correct | Yes | The agent correctly identified that the mongo container has exited and is unreachable, which is the exact simulated failure mode. |
| Component overlap | 0% | Although the agent correctly identified 'mongo' as the primary suspect in the root_cause field, the 'affected_components' list was left unpopulated with placeholder '?' values. |
| Severity correct | Yes | The agent correctly identified the severity as a complete outage, noting that the container is 'exited' and 'unreachable' with 'complete failure' for dependent functions. |
| Fault type identified | Yes | The agent correctly identified the fault type as unreachable/down, specifically noting that the mongo container is in an exited state and cannot be resolved or reached. |
| Layer accuracy | Yes | No layer status information was provided in the agent's network analysis, so no misattribution was detected. |
| Confidence calibrated | Yes | The agent's confidence is set to medium, which is appropriate given it has solid container status and ping evidence, though the affected components list failed to populate. |

**Ranking position:** #1 — The agent identified 'mongo' as the single primary root cause.


### Token Usage

| Metric | Count |
|--------|-------|
| Input tokens | 185,808 |
| Output tokens | 3,443 |
| Thinking tokens | 9,257 |
| **Total tokens** | **198,508** |

**Per-phase breakdown:**

| Phase | Tokens | Tool Calls | LLM Calls |
|-------|--------|------------|-----------|
| AnomalyScreener | 0 | 0 | 0 |
| SymptomClassifier | 0 | 0 | 0 |
| EventAggregator | 0 | 0 | 0 |
| CorrelationAnalyzer | 0 | 0 | 0 |
| RAGRetriever | 0 | 0 | 0 |
| OperationalLessons | 0 | 0 | 0 |
| NetworkAnalystAgent | 70,639 | 5 | 3 |
| Phase 3 NetworkAnalyst__guardrail | 0 | 0 | 0 |
| InstructionGeneratorAgent | 9,093 | 0 | 1 |
| Phase 4 InstructionGenerator__guardrail | 0 | 0 | 0 |
| InstructionGeneratorAgent | 10,540 | 0 | 1 |
| Phase 4 InstructionGenerator__guardrail | 0 | 0 | 0 |
| Phase 4 InstructionGenerator__guardrail | 0 | 0 | 0 |
| InvestigatorAgent_h1 | 66,490 | 4 | 5 |
| InvestigatorAgent_h1 | 29,331 | 4 | 2 |
| InvestigatorAgent_h1__reconciliation | 0 | 0 | 0 |
| Phase5FanOutAudit | 0 | 0 | 0 |
| Phase6.5CandidatePool | 0 | 0 | 0 |
| EvidenceValidator | 0 | 0 | 0 |
| SynthesisAgent | 12,415 | 0 | 1 |
| Phase 7 Synthesis__guardrail | 0 | 0 | 0 |


## Resolution

**Heal method:** scheduled  
**Recovery time:** 333.5s
