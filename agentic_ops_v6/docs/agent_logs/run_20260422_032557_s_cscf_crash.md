# Episode Report: S-CSCF Crash

**Agent:** v6  
**Episode ID:** ep_20260422_032045_s_cscf_crash  
**Date:** 2026-04-22T03:20:47.195146+00:00  
**Duration:** 310.0s  

---

## Scenario

**Category:** container  
**Blast radius:** single_nf  
**Description:** Kill the S-CSCF (Serving-CSCF). This is the core SIP registrar and call controller. All IMS authentication stops, new registrations are impossible, and active calls will eventually drop.

## Faults Injected

- **container_kill** on `scscf`

## Baseline (Pre-Fault)

Stack phase before injection: **ready**
All containers running at baseline.

## Fault Propagation Verification

**Verdict:** ✅ `confirmed`

- **Wait:** 0s
- **Actual elapsed:** 0.0s
- **Nodes with significant deltas:** 2
- **Nodes with any drift:** 4

## Symptoms Observed

Symptoms detected: **Yes**  
Observation iterations: 1

## Anomaly Screening (Phase 0)

**ANOMALY DETECTED.** Overall anomaly score: 0.90 (threshold: 0.70, trained on 211 healthy snapshots). The current metric pattern is statistically different from the learned healthy baseline. Something in the network has changed.

The following metrics deviate from their learned-healthy baseline. Treat each as a semantic observation (meaning + numbers), not a number alone — the KB's interpretation is the authoritative reading.

- **`normalized.upf.gtp_outdatapktn3upf_per_ue`** (GTP-U downlink rate per UE (N3)) — current **0.02 packets_per_second** vs learned baseline **3.34 packets_per_second** (MEDIUM, drop)
    - **What it measures:** Health of the downlink user-plane path UPF → gNB. Typically mirrors
uplink rate during calls/data; asymmetry indicates directional
faults (e.g., UPF receiving from internet but not forwarding).
    - **Drop means:** No traffic leaving UPF toward RAN.
    - **Healthy typical range:** 0–10 packets_per_second
    - **Healthy invariant:** Per-UE — constant regardless of UE pool size. Roughly symmetric with uplink during healthy calls.

- **`normalized.upf.gtp_indatapktn3upf_per_ue`** (GTP-U uplink rate per UE (N3)) — current **0.11 packets_per_second** vs learned baseline **3.42 packets_per_second** (MEDIUM, drop)
    - **What it measures:** Health of the uplink user-plane path gNB → UPF. Drops to near-zero
during RAN or N3 outage; stays nonzero during active calls or data
sessions. Decoupled from SIP signaling (signals data plane, not
control plane).
    - **Drop means:** Data plane dead on uplink — UPF receiving no packets from gNB.
    - **Healthy typical range:** 0–10 packets_per_second
    - **Healthy invariant:** Per-UE — constant regardless of UE pool size. Rises during active VoNR calls (~100 pps for G.711 voice) and data transfer.

- **`normalized.smf.bearers_per_ue`** (Active QoS bearers per UE) — current **3.50 count** vs learned baseline **2.61 count** (LOW, shift)
    - **What it measures:** Per-UE count of active QoS bearers. Baseline reflects default
bearers; increments during VoNR calls indicate dedicated voice
bearers being set up. Drop during an active call = dedicated
bearer torn down unexpectedly (voice will fail).
    - **Shift means:** Expected during VoNR calls (1 extra bearer per active call).
    - **Healthy typical range:** 2–3.5 count
    - **Healthy invariant:** At rest: equals configured default bearers (typically 2 per UE).
During active VoNR call: +1 per caller. The per-UE ratio is the
invariant; absolute count scales with UE pool.

- **`normalized.pcscf.dialogs_per_ue`** (Active SIP dialogs per registered UE at P-CSCF) — current **0.00 count** vs learned baseline **0.57 count** (LOW, drop)
    - **What it measures:** How many calls per user are currently in progress at the P-CSCF.
Going to zero from a non-zero baseline means calls have ended
(normal) OR call setup is failing system-wide (degradation).
Together with rcv_requests_* it discriminates the two.
    - **Drop means:** No active calls. Normal at rest; suspicious if prior state had calls AND INVITE rate is also zero.
    - **Healthy typical range:** 0–1 count
    - **Healthy invariant:** Per-UE — scale-independent. 0 at rest, ~1 per active VoNR call.

- **`derived.upf_activity_during_calls`** — current **1.00** vs learned baseline **0.47** (LOW, spike). *(No KB context available — interpret from the metric name.)*


## Event Aggregation (Phase 1)

No events fired during this episode. Either no metric KB triggers matched, or the episode encountered no meaningful state transitions.

## Correlation Analysis (Phase 2)

No events fired — correlation engine had nothing to work with.

## Network Analysis (Phase 3)

**Summary:** The IMS core is down due to the S-CSCF container having exited, which prevents VoNR call setup and results in no user plane traffic.

**Layer status:**

| Layer | Rating | Note |
|---|---|---|
| **infrastructure** | 🟢 GREEN | The underlying infrastructure and non-IMS services appear to be stable. |
| **ran** | 🟢 GREEN | The RAN layer appears healthy and is connected to the core. |
| **core** | 🟡 YELLOW | The 5G core is creating bearers for calls, but the user plane is inactive because calls are not being successfully established by the IMS. |
| **ims** | 🔴 RED | The S-CSCF, a critical component for call session control, is down. This is causing a complete outage of the IMS service. |

**CORE evidence:**
- normalized.smf.bearers_per_ue is high (3.50)
- normalized.upf.gtp_outdatapktn3upf_per_ue is near zero
- normalized.upf.gtp_indatapktn3upf_per_ue is near zero

**IMS evidence:**
- S-CSCF container has exited
- All SIP and Diameter links to S-CSCF are inactive
- normalized.pcscf.dialogs_per_ue is zero, indicating no active calls

**Ranked hypotheses:**

- **`h1`** (fit=0.95, nf=scscf, specificity=specific):
    - **Statement:** The S-CSCF container has exited, causing a complete failure of the IMS session setup (VoNR calls). SIP INVITE messages from the I-CSCF cannot be processed, leading to call setup failures, which explains the absence of active SIP dialogs and zero user plane traffic (GTP), even though bearers are being created by the 5G core.
    - **Falsification probes:**
        - Check the logs of the scscf container to find the reason for its exit.
        - Restart the scscf container and observe if new VoNR calls can be established successfully.
        - Check I-CSCF logs for errors related to forwarding SIP requests to the S-CSCF.
- **`h2`** (fit=0.60, nf=ims, specificity=vague):
    - **Statement:** A critical failure in the IMS core, beyond just the S-CSCF, is preventing the processing of SIP signaling. This could be a misconfiguration or a broader service outage affecting multiple IMS components.
    - **Falsification probes:**
        - Verify the configuration of all IMS components (P-CSCF, I-CSCF, S-CSCF, HSS) for correctness.
        - Test basic SIP registration and call flows in a controlled environment with logging enabled on all IMS NFs.
- **`h3`** (fit=0.40, nf=upf, specificity=moderate):
    - **Statement:** The User Plane Function (UPF) is experiencing a severe internal fault or partition that prevents it from forwarding any GTP-U traffic. This is causing all data-plane-dependent services, including VoNR, to fail.
    - **Falsification probes:**
        - Check UPF logs for any error messages related to packet processing or PFCP session handling.
        - Generate simple PDU session traffic (not VoNR) and check if any traffic flows through the UPF.
        - Use `measure_rtt` to test connectivity from UPF to both the gNB (RAN) and core network components.


## Falsification Plans (Phase 4)

**3 falsification plan(s) — one per hypothesis:**

### Plan for `h1` (target: `scscf`)

**Hypothesis:** The S-CSCF container has exited, causing a complete failure of the IMS session setup (VoNR calls). SIP INVITE messages from the I-CSCF cannot be processed, leading to call setup failures, which explains the absence of active SIP dialogs and zero user plane traffic (GTP), even though bearers are being created by the 5G core.

**Probes (3):**
1. **`get_network_status`** — component='scscf'
    - *Expected if hypothesis holds:* The container status for 'scscf' is 'exited' or another non-running state.
    - *Falsifying observation:* The container status is 'running'. This directly contradicts the hypothesis that the container has exited.
2. **`check_process_listeners`** — container='scscf'
    - *Expected if hypothesis holds:* No process is listening on the SIP port (5060/TCP), as the Kamailio process would be down.
    - *Falsifying observation:* A process is actively listening on port 5060/TCP. This proves the S-CSCF process is up and has bound to its service port.
3. **`run_kamcmd`** — container='icscf', command='dispatcher.list'
    - *Expected if hypothesis holds:* The S-CSCF destination in the I-CSCF's dispatcher list is marked as 'disabled' or 'inactive' due to probing failures.
    - *Falsifying observation:* The S-CSCF destination is present and marked as 'active'. This shows the upstream component (I-CSCF) considers S-CSCF to be healthy.

*Notes:* This plan directly tests the central claim of hypothesis h1: that the S-CSCF container is down. It uses a direct status check, a process-level check, and a check from the perspective of the adjacent upstream NF (I-CSCF).

### Plan for `h2` (target: `ims`)

**Hypothesis:** A critical failure in the IMS core, beyond just the S-CSCF, is preventing the processing of SIP signaling. This could be a misconfiguration or a broader service outage affecting multiple IMS components.

**Probes (3):**
1. **`get_nf_metrics`** — component='icscf'
    - *Expected if hypothesis holds:* Metrics on I-CSCF related to forwarding requests to HSS (e.g., diameter errors) would be elevated, or metrics for received SIP requests would be zero, suggesting an upstream failure.
    - *Falsifying observation:* Metrics show I-CSCF is receiving requests from P-CSCF and attempting to forward them to S-CSCF, but failing (e.g., high `kamailio.dispatcher.retries` for the scscf destination). This isolates the fault to S-CSCF, not a broader issue.
2. **`measure_rtt`** — from='icscf', to_ip='pyhss_ip'
    - *Expected if hypothesis holds:* High RTT or packet loss between I-CSCF and HSS, indicating a failure on the critical Cx interface.
    - *Falsifying observation:* Clean RTT (< 5ms), indicating the network path for the Cx interface is healthy and the problem is not a network partition between these components.
3. **`query_subscriber`** — imsi='some_known_imsi'
    - *Expected if hypothesis holds:* The command fails or returns an error, indicating the HSS is unresponsive or misconfigured.
    - *Falsifying observation:* The command successfully returns subscriber data, proving the HSS is operational and correctly configured for lookups.

*Notes:* This plan aims to falsify the 'broader IMS failure' hypothesis by confirming the health of other critical IMS components (I-CSCF, HSS) and their interconnections. If they are working correctly, the problem is likely isolated to S-CSCF.

### Plan for `h3` (target: `upf`)

**Hypothesis:** The User Plane Function (UPF) is experiencing a severe internal fault or partition that prevents it from forwarding any GTP-U traffic. This is causing all data-plane-dependent services, including VoNR, to fail.

**Probes (3):**
1. **`get_dp_quality_gauges`** — window_seconds=60
    - *Expected if hypothesis holds:* Packet rates for flows transiting the UPF (e.g., `gNB->UPF` and `UPF->Core`) are zero or near-zero.
    - *Falsifying observation:* Non-zero packet rates are observed. This directly contradicts the claim that the UPF is not forwarding any GTP-U traffic.
2. **`measure_rtt`** — from='nr_gnb', to_ip='upf_n3_ip'
    - *Expected if hypothesis holds:* High RTT or 100% packet loss, suggesting the UPF's network stack is unresponsive.
    - *Falsifying observation:* Clean RTT (< 5ms). This proves the N3 interface is healthy from a network perspective, making an 'internal fault' that manifests as a network issue less likely.
3. **`measure_rtt`** — from='upf', to_ip='nr_gnb_ip'
    - *Expected if hypothesis holds:* High RTT or 100% packet loss, consistent with the forward-path measurement.
    - *Falsifying observation:* Clean RTT (< 5ms). This, combined with the forward probe, confirms bidirectional connectivity between the gNB and UPF, falsifying the 'partition' aspect of the hypothesis.

*Notes:* This plan focuses on the data plane. It starts with a direct measurement of traffic forwarding and then uses triangulated RTT probes to confirm or deny a network-level partition at the UPF's N3 interface.


## Parallel Investigators (Phase 5)

**3 sub-Investigator verdict(s):** **1 NOT_DISPROVEN**, **2 DISPROVEN**

### `h1` — ✅ **NOT_DISPROVEN**

**Hypothesis:** The S-CSCF container has exited, causing a complete failure of the IMS session setup (VoNR calls). SIP INVITE messages from the I-CSCF cannot be processed, leading to call setup failures, which explains the absence of active SIP dialogs and zero user plane traffic (GTP), even though bearers are being created by the 5G core.

**Reasoning:** The primary claim of the hypothesis, that the S-CSCF container has exited, was directly confirmed by `get_network_status`. The necessary consequence of this, that no process would be listening for SIP traffic, was confirmed by `check_process_listeners`. The third probe, intended to check the I-CSCF's dispatcher state, failed because the command was not found, but this ambiguity does not outweigh the direct evidence from the first two probes. No evidence contradicts the hypothesis.

**Probes executed (3):**
- **Check the status of the 'scscf' container.** ✓ CONSISTENT
    - *Tool:* `print(default_api.get_network_status())`
    - *Observation:* [EVIDENCE: get_network_status() -> "'scscf': 'exited'"]
    - *Comment:* The tool output confirms that the scscf container has exited, which is the central claim of the hypothesis.
- **Check for processes listening on the SIP port within the 'scscf' container.** ✓ CONSISTENT
    - *Tool:* `print(default_api.check_process_listeners(container='scscf'))`
    - *Observation:* [EVIDENCE: check_process_listeners("scscf") -> "Error response from daemon: container cfb808a9bc67ca46a2355b05bea28407d83285ff1bd77561080e32db686f7b62 is not running"]
    - *Comment:* The tool confirms the container is not running, which is consistent with the hypothesis that the S-CSCF process is down and therefore not listening on any ports.
- **Check the I-CSCF's dispatcher list to see if the S-CSCF is marked as inactive.** ~ AMBIGUOUS
    - *Tool:* `print(default_api.run_kamcmd(container='icscf', command='dispatcher.list'))`
    - *Observation:* [EVIDENCE: run_kamcmd("icscf", "dispatcher.list") -> "error: 500 - command dispatcher.list not found"]
    - *Comment:* The probe could not be completed as the 'dispatcher.list' command does not exist in the kamcmd implementation. This result is ambiguous and does not provide evidence for or against the hypothesis.

### `h2` — ❌ **DISPROVEN**

**Hypothesis:** A critical failure in the IMS core, beyond just the S-CSCF, is preventing the processing of SIP signaling. This could be a misconfiguration or a broader service outage affecting multiple IMS components.

**Reasoning:** The hypothesis of a broad IMS core failure beyond just the S-CSCF is disproven. Probes confirmed that other critical IMS components, specifically the I-CSCF and HSS, are healthy and communicating correctly. The I-CSCF is receiving requests and successfully querying the HSS, the network path between them is healthy, and the HSS is responsive and serving correct subscriber data. This evidence isolates the failure to the S-CSCF.

**Probes executed (3):**
- **Metrics on I-CSCF related to forwarding requests to HSS (e.g., diameter errors) would be elevated, or metrics for received SIP requests would be zero, suggesting an upstream failure.** ✗ CONTRADICTS
    - *Tool:* `print(default_api.get_nf_metrics())`
    - *Observation:* [EVIDENCE: get_nf_metrics() -> "ICSCF [14 req] (via kamcmd):... core:rcv_requests_register = 14.0... ims_icscf:uar_replies_received = 5.0... ims_icscf:uar_timeouts = 0.0"]
    - *Comment:* The I-CSCF is receiving REGISTER requests (14) and successfully communicating with the HSS (5 UAR replies, 0 timeouts). This indicates the I-CSCF and its HSS interface are working, contradicting the idea of a broader failure.
- **High RTT or packet loss between I-CSCF and HSS, indicating a failure on the critical Cx interface.** ✗ CONTRADICTS
    - *Tool:* `print(default_api.measure_rtt(container = "icscf", target_ip = "172.22.0.18"))`
    - *Observation:* [EVIDENCE: measure_rtt("icscf", "172.22.0.18") -> "3 packets transmitted, 3 received, 0% packet loss, time 2026ms\nrtt min/avg/max/mdev = 0.081/0.132/0.199/0.049 ms"]
    - *Comment:* The RTT between the I-CSCF and HSS (pyhss) is excellent (<1ms) with no packet loss. This proves the network path for the Cx interface is healthy, falsifying the possibility of a network partition between these components.
- **The command fails or returns an error, indicating the HSS is unresponsive or misconfigured.** ✗ CONTRADICTS
    - *Tool:* `print(default_api.query_subscriber(domain = "ims", imsi = "001011234567891"))`
    - *Observation:* [EVIDENCE: query_subscriber("ims", "001011234567891") -> "{\n  \"ims_subscriber\": {\n    \"imsi\": \"001011234567891\" ... \"scscf\": \"sip:scscf.ims.mnc001.mcc001.3gppnetwork.org:6060\"...}"]
    - *Comment:* The HSS successfully returned subscriber data, including the assigned S-CSCF. This proves the HSS is operational and correctly configured, which contradicts the hypothesis of a widespread IMS failure.

**Alternative suspects:** scscf

### `h3` — ❌ **DISPROVEN**

**Hypothesis:** The User Plane Function (UPF) is experiencing a severe internal fault or partition that prevents it from forwarding any GTP-U traffic. This is causing all data-plane-dependent services, including VoNR, to fail.

**Reasoning:** The hypothesis of a UPF fault or partition is disproven. Probes reveal that while the UPF is not forwarding data plane traffic, it has perfect network connectivity to the RAN. A successful round-trip-time measurement between the UPF and the gNB falsifies the 'partition' theory. The lack of egress traffic, combined with healthy connectivity, indicates the UPF is not receiving any user plane traffic to forward, pointing to an upstream failure in call setup, not a fault within the UPF itself.

**Probes executed (3):**
- **Packet rates for flows transiting the UPF (e.g., `gNB->UPF` and `UPF->Core`) are zero or near-zero.** ✓ CONSISTENT
    - *Tool:* `default_api.get_dp_quality_gauges(window_seconds=60)`
    - *Observation:* [EVIDENCE: get_dp_quality_gauges(60) -> "UPF:\n    in  packets/sec: 0.2\n    out packets/sec: 0.0\n    in  throughput : 0.01 KB/s\n    out throughput : 0.0 KB/s\n    active sessions: 8"]
    - *Comment:* The observation of zero outbound traffic is consistent with the hypothesis's claimed effect. However, it doesn't prove the UPF is the cause.
- **High RTT or 100% packet loss, consistent with the forward-path measurement.** ✗ CONTRADICTS
    - *Tool:* `default_api.measure_rtt(container='upf', target_ip='172.22.0.23')`
    - *Observation:* [EVIDENCE: measure_rtt('upf', '172.22.0.23') -> "3 packets transmitted, 3 received, 0% packet loss, time 2046ms\nrtt min/avg/max/mdev = 0.093/0.189/0.354/0.116 ms"]
    - *Comment:* This probe result directly contradicts the 'partition' aspect of the hypothesis. A clean RTT proves bidirectional network health between the UPF and the gNB. The UPF is not isolated from the RAN.
- **High RTT or 100% packet loss, suggesting the UPF's network stack is unresponsive.** ~ AMBIGUOUS
    - *Tool:* `default_api.measure_rtt(container='nr_gnb', target_ip='172.22.0.8')`
    - *Observation:* [EVIDENCE: measure_rtt('nr_gnb', '172.22.0.8') -> "Unknown container 'nr_gnb'."]
    - *Comment:* This probe could not be executed as planned because there is no 'nr_gnb' container from which to run tools. However, the reverse-path probe was successful and is sufficient to disprove a network partition, as ICMP is a round-trip protocol.

**Alternative suspects:** scscf, smf


## Evidence Validation (Phase 6)

**Overall verdict:** clean
**Overall confidence:** high

**Per sub-Investigator:**

| Agent | Tool Calls | Citations | Verdict | Confidence |
|---|---|---|---|---|
| `InvestigatorAgent_h1` | 3 | 3/3 | clean | high |
| `InvestigatorAgent_h2` | 4 | 3/3 | clean | high |
| `InvestigatorAgent_h3` | 4 | 3/3 | clean | high |


## Ground Truth

**Failure domain:** ims_signaling  
**Protocol impact:** SIP  
**Affected components:** scscf  
**Severity:** degraded

## Agent Diagnosis

**Model:** v6-adk/gemini-2.5-flash+gemini-2.5-pro  
**Time to diagnosis:** 164.7s

### Prompt to RCA Agent

```
The 5G SA + IMS stack is experiencing issues. Investigate and diagnose the root cause.
```

**Diagnosis:**

> ### causes
> - **summary**: The primary IMS call processing component (S-CSCF) has failed, leading to a complete outage of the Voice-over-NR (VoNR) service.
> - **timeline**:
>     1. The S-CSCF container terminated unexpectedly.
>     2. The I-CSCF, which routes call session requests, began failing to forward SIP INVITE messages to the S-CSCF.
>     3. As a result, no VoNR calls could be established, leading to an absence of user plane traffic at the UPF, even though the 5G Core continued to establish data bearers for the call attempts.
> - **root_cause**: The S-CSCF container has exited. The reason for the exit has not yet been determined. `primary_suspect_nf`: `scscf`.
> - **affected_components**:
>     - `scscf`: Root Cause
>     - `ims`: Secondary
>     - `upf`: Symptomatic
> - **recommendation**: Investigate the logs of the `scscf` container to determine the reason it exited. Corroborate the impact by checking the `icscf` container logs for errors related to forwarding SIP requests.
> - **confidence**: high
> - **explanation**: The investigation confirmed with high confidence that the root cause is the failure of the S-CSCF container. The `get_network_status` tool directly observed that the container's state was 'exited', which is the central claim of the surviving hypothesis (`h1`). This single failure explains all observed symptoms: the IMS core is down because its primary session manager is gone, and consequently, no user plane traffic is generated for the UPF to forward. Alternative hypotheses, such as a broader IMS failure (`h2`) or a UPF partition (`h3`), were actively disproven. Probes showed other IMS components (I-CSCF, HSS) were healthy and that the UPF had excellent network connectivity, isolating the fault to the S-CSCF.

### Scoring Breakdown

**Overall score: 100%**

**Scorer assessment:** The agent provided an excellent and accurate diagnosis, correctly identifying the root cause, affected component, severity, and fault type, with appropriate confidence and layer attribution.

| Dimension | Result | Rationale |
|-----------|--------|-----------|
| Root cause correct | Yes | The simulated failure was an S-CSCF crash, leading to the S-CSCF being unreachable. The agent correctly identified the root cause as 'The S-CSCF container has exited.', which is semantically equivalent to the simulated failure mode. |
| Component overlap | 100% | The primary affected component is 'scscf', and the agent correctly listed 'scscf' as 'Root Cause' in its 'affected_components'. |
| Severity correct | Yes | The simulated failure described a complete outage ('All IMS authentication stops, new registrations are impossible, and active calls will eventually drop'). The agent's summary states 'complete outage of the Voice-over-NR (VoNR) service', which matches the severity. |
| Fault type identified | Yes | The simulated failure mode was 'Component completely unreachable (down/not responding)'. The agent identified that 'The S-CSCF container has exited' and its 'state was 'exited'', which clearly indicates an unreachable/down component. |
| Layer accuracy | Yes | The ground truth states 'scscf' belongs to the 'ims' layer. The agent's network analysis correctly rated the 'ims' layer as 'red' with the evidence 'S-CSCF container has exited'. |
| Confidence calibrated | Yes | The agent stated 'high' confidence, and its diagnosis is accurate and well-supported by direct observation ('get_network_status tool directly observed that the container's state was 'exited''). |

**Ranking position:** #1 — The agent's final diagnosis clearly identifies a single root cause, 'The S-CSCF container has exited', which is the correct one. The intermediate hypotheses also show this as the top-ranked hypothesis.


### Token Usage

| Metric | Count |
|--------|-------|
| Input tokens | 160,475 |
| Output tokens | 5,726 |
| Thinking tokens | 14,142 |
| **Total tokens** | **180,343** |

**Per-phase breakdown:**

| Phase | Tokens | Tool Calls | LLM Calls |
|-------|--------|------------|-----------|
| AnomalyScreener | 0 | 0 | 0 |
| EventAggregator | 0 | 0 | 0 |
| CorrelationAnalyzer | 0 | 0 | 0 |
| NetworkAnalystAgent | 33,774 | 3 | 4 |
| InstructionGeneratorAgent | 15,830 | 3 | 2 |
| InvestigatorAgent_h1 | 28,503 | 3 | 4 |
| InvestigatorAgent_h2 | 53,484 | 4 | 5 |
| InvestigatorAgent_h3 | 42,666 | 4 | 5 |
| EvidenceValidator | 0 | 0 | 0 |
| SynthesisAgent | 6,086 | 0 | 1 |


## Resolution

**Heal method:** scheduled  
**Recovery time:** 310.0s
