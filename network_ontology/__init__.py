"""Network Ontology — domain knowledge graph for 5G SA + IMS troubleshooting.

A standalone package that models the network's component topology, causal
failure chains, log semantics, symptom signatures, and protocol stack rules
in a Neo4j graph database.

Consumed by RCA agents (agentic_ops_v4+), the chaos framework, and the GUI.
"""

__version__ = "0.1.0"
