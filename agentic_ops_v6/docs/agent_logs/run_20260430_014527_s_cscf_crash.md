# Episode Report: S-CSCF Crash

**Agent:** v6  
**Episode ID:** ep_20260430_014002_s_cscf_crash  
**Date:** 2026-04-30T01:40:04.209163+00:00  
**Duration:** 322.4s  

---

## Scenario

**Category:** container  
**Blast radius:** single_nf  
**Description:** Kill the S-CSCF (Serving-CSCF). This is the core SIP registrar and call controller. All IMS authentication stops, new registrations are impossible, and active calls will eventually drop.

## Faults Injected

- **container_kill** on `scscf`

## Baseline (Pre-Fault)

Stack phase before injection: **ready**
All containers running at baseline.

## Fault Propagation Verification

**Verdict:** ✅ `confirmed`

- **Wait:** 0s
- **Actual elapsed:** 0.0s
- **Nodes with significant deltas:** 3
- **Nodes with any drift:** 4

## Symptoms Observed

Symptoms detected: **Yes**  
Observation iterations: 1

## Anomaly Screening (Phase 0)

**ANOMALY DETECTED.** Overall anomaly score: 14.80 (per-bucket threshold: 11.07, context bucket (0, 0), trained on 323 healthy snapshots). The current metric pattern is statistically different from the learned healthy baseline. Something in the network has changed.

The following metrics deviate from their learned-healthy baseline. Treat each as a semantic observation (meaning + numbers), not a number alone — the KB's interpretation is the authoritative reading.

- **`normalized.pcscf.core:rcv_requests_invite_per_ue`** (SIP INVITE rate per UE at P-CSCF) — current **0.01 requests_per_second** vs learned baseline **0.00 requests_per_second** (MEDIUM, spike)
    - **What it measures:** Call attempt rate from registered UEs. Unlike REGISTER (periodic),
INVITEs only fire when UEs place calls. Zero is normal during
quiet periods; nonzero INVITE with zero dialogs is the signature
of call setup failure.
    - **Spike means:** Fewer call attempts.
    - **Healthy typical range:** 0–0.2 requests_per_second
    - **Healthy invariant:** Per-UE rate.

- **`normalized.smf.bearers_per_ue`** (Active QoS bearers per UE) — current **3.50 count** vs learned baseline **2.48 count** (MEDIUM, shift)
    - **What it measures:** Per-UE count of active QoS bearers. Baseline reflects default
bearers; increments during VoNR calls indicate dedicated voice
bearers being set up. Drop during an active call = dedicated
bearer torn down unexpectedly (voice will fail).
    - **Shift means:** Expected during VoNR calls (1 extra bearer per active call).
    - **Healthy typical range:** 2–3.5 count
    - **Healthy invariant:** At rest: equals configured default bearers (typically 2 per UE).
During active VoNR call: +1 per caller. The per-UE ratio is the
invariant; absolute count scales with UE pool.

- **`normalized.upf.gtp_indatapktn3upf_per_ue`** (GTP-U uplink rate per UE (N3)) — current **0.11 packets_per_second** vs learned baseline **1.45 packets_per_second** (LOW, drop)
    - **What it measures:** Health of the uplink user-plane path gNB → UPF. Drops to near-zero
during RAN or N3 outage; stays nonzero during active calls or data
sessions. Decoupled from SIP signaling (signals data plane, not
control plane).
    - **Drop means:** Data plane dead on uplink — UPF receiving no packets from gNB.
    - **Healthy typical range:** 0–10 packets_per_second
    - **Healthy invariant:** Per-UE — constant regardless of UE pool size. Rises during active VoNR calls (~100 pps for G.711 voice) and data transfer.

- **`normalized.upf.gtp_outdatapktn3upf_per_ue`** (GTP-U downlink rate per UE (N3)) — current **0.03 packets_per_second** vs learned baseline **1.45 packets_per_second** (LOW, drop)
    - **What it measures:** Health of the downlink user-plane path UPF → gNB. Typically mirrors
uplink rate during calls/data; asymmetry indicates directional
faults (e.g., UPF receiving from internet but not forwarding).
    - **Drop means:** No traffic leaving UPF toward RAN.
    - **Healthy typical range:** 0–10 packets_per_second
    - **Healthy invariant:** Per-UE — constant regardless of UE pool size. Roughly symmetric with uplink during healthy calls.


## Event Aggregation (Phase 1)

No events fired during this episode. Either no metric KB triggers matched, or the episode encountered no meaningful state transitions.

## Correlation Analysis (Phase 2)

No events fired — correlation engine had nothing to work with.

## Network Analysis (Phase 3)

**Summary:** The S-CSCF has exited, causing a failure in the IMS signaling plane that prevents VoNR call setup and results in no user plane traffic.

**Layer status:**

| Layer | Rating | Note |
|---|---|---|
| **infrastructure** | 🟢 GREEN | All infrastructure components are running, but the exited state of the S-CSCF application-level container is the trigger for the anomaly. |
| **ran** | 🟢 GREEN | No metrics or events indicate a failure originating in the RAN. UEs are able to attempt calls, which implies the RAN is functional. |
| **core** | 🟡 YELLOW | The data plane at the UPF is inactive and the SMF has allocated extra bearers. These are strong downstream symptoms of the IMS signaling failure, rather than a root cause within the core. |
| **ims** | 🔴 RED | The S-CSCF is a critical component for IMS call session control. Its failure is the most likely root cause. |

**CORE evidence:**
- normalized.upf.gtp_indatapktn3upf_per_ue is near zero.
- normalized.upf.gtp_outdatapktn3upf_per_ue is near zero.
- normalized.smf.bearers_per_ue is elevated.

**IMS evidence:**
- scscf container has exited.
- Multiple inactive links to S-CSCF from P-CSCF, I-CSCF, and HSS.
- normalized.pcscf.core:rcv_requests_invite_per_ue spiked and then dropped, indicating call attempts are failing.

**Ranked hypotheses:**

- **`h1`** (fit=0.90, nf=scscf, specificity=specific):
    - **Statement:** The S-CSCF container has exited, which is breaking the VoNR call setup signaling flow and preventing any calls from being established.
    - **Falsification probes:**
        - Check the logs of the scscf container to determine the reason for its exit.
        - Attempt to restart the scscf container and observe if VoNR calls can be successfully established.
        - Examine the I-CSCF logs for SIP errors related to forwarding requests to the S-CSCF.
- **`h2`** (fit=0.40, nf=upf, specificity=moderate):
    - **Statement:** The User Plane Function (UPF) is the source of the data plane outage, preventing any GTP-U packets from being forwarded.
    - **Falsification probes:**
        - After resolving any IMS-layer faults, verify if GTP traffic flows through the UPF during a successful call.
        - Check the UPF logs for any errors related to GTP-U handling or N4 session programming from the SMF.
- **`h3`** (fit=0.30, nf=icscf, specificity=specific):
    - **Statement:** The I-CSCF is unable to select an S-CSCF, due to a failure in its Diameter communication with the HSS.
    - **Falsification probes:**
        - Check I-CSCF logs for any Diameter User-Data-Request (UDR) timeouts or errors on the Cx interface to the HSS.
        - Check HSS logs to confirm if it is receiving and successfully responding to Cx interface queries from the I-CSCF.


## Falsification Plans (Phase 4)

**3 falsification plan(s) — one per hypothesis:**

### Plan for `h1` (target: `scscf`)

**Hypothesis:** The S-CSCF container has exited, which is breaking the VoNR call setup signaling flow and preventing any calls from being established.

**Probes (3):**
1. **`get_network_status`** — Check the status of the 'scscf' container.
    - *Expected if hypothesis holds:* The container status for 'scscf' is 'exited' or another non-running state.
    - *Falsifying observation:* The container status for 'scscf' is 'running'.
2. **`run_kamcmd`** — scscf, command='core.uptime'
    - *Expected if hypothesis holds:* The command fails or returns an error, as the Kamailio process is not running.
    - *Falsifying observation:* The command returns a valid uptime, indicating the S-CSCF's Kamailio process is active.
3. **`get_network_status`** — Check the status of the 'icscf' container.
    - *Expected if hypothesis holds:* The 'icscf' container is running, but is failing to forward traffic to the exited 'scscf'.
    - *Falsifying observation:* The 'icscf' container is also in an exited state, suggesting a broader failure.

*Notes:* This hypothesis is the most specific and has the highest fit. The probes are designed to directly verify the container's state and check the immediate upstream component (I-CSCF) for expected signs of failure. The relevant flow is 'vonr_call_setup', steps 4-7.

### Plan for `h2` (target: `upf`)

**Hypothesis:** The User Plane Function (UPF) is the source of the data plane outage, preventing any GTP-U packets from being forwarded.

**Probes (3):**
1. **`get_dp_quality_gauges`** — window_seconds=60
    - *Expected if hypothesis holds:* UPF gauges show high packet loss or error rates for GTP-U traffic.
    - *Falsifying observation:* UPF gauges show zero traffic (pps/kbps) and zero packet loss, indicating that no user plane traffic is reaching it in the first place.
2. **`get_diagnostic_metrics`** — Get metrics for 'nr_gnb', specifically GTP traffic counters for the N3 interface.
    - *Expected if hypothesis holds:* The gNB's metrics show a non-zero count of GTP-U packets transmitted towards the UPF.
    - *Falsifying observation:* The gNB's metrics show zero GTP-U packets transmitted, which proves that the UPF is not receiving traffic to drop, falsifying the hypothesis that it is the source of the outage.
3. **`get_diagnostic_metrics`** — Get metrics for 'smf', specifically N4 interface-related metrics.
    - *Expected if hypothesis holds:* SMF metrics show a high number of N4 session establishment or modification failures.
    - *Falsifying observation:* SMF metrics show that N4 sessions are being established and modified successfully, indicating the control plane link to the UPF is healthy.

*Notes:* This plan uses the 'Activity-vs-drops' pattern. Probe 1 looks for the effect (packet loss at UPF), while Probe 2 (its disambiguation partner) checks if the upstream component (gNB) is even sending traffic. If no traffic is sent, the UPF cannot be the source of the drop. This is crucial for falsifying h2, which the NA already suspects is a downstream symptom.

### Plan for `h3` (target: `icscf`)

**Hypothesis:** The I-CSCF is unable to select an S-CSCF, due to a failure in its Diameter communication with the HSS.

**Probes (3):**
1. **`get_diagnostic_metrics`** — Get metrics for 'icscf', specifically Kamailio CDP (Diameter) metrics.
    - *Expected if hypothesis holds:* A high count of 'kamailio.cdp.timeout' or other Diameter error metrics.
    - *Falsifying observation:* Zero or few Diameter errors/timeouts on the I-CSCF, indicating it is able to communicate with the HSS.
2. **`get_diagnostic_metrics`** — Get metrics for 'pyhss', specifically Diameter Cx interface metrics.
    - *Expected if hypothesis holds:* Low or zero 'pyhss.diameter.cx.requests_received' from the I-CSCF, or a high number of errors in processing.
    - *Falsifying observation:* The HSS metrics show it is successfully receiving and responding to a steady stream of Diameter requests from the I-CSCF.
3. **`get_diagnostic_metrics`** — Get metrics for 'icscf', looking for SIP forwarding failures (e.g. 'kamailio.tm.uac_replies' with 5xx codes).
    - *Expected if hypothesis holds:* These SIP forwarding failure metrics should be low. The primary failure signature should be in Diameter (probe 1).
    - *Falsifying observation:* A high number of SIP forwarding failures. This suggests the I-CSCF successfully used Diameter to find an S-CSCF, but then failed to forward the SIP request, contradicting this hypothesis and strongly supporting h1.

*Notes:* This hypothesis competes with h1 as the explanation for the call setup failure. Probe 3 is a key discriminator: if the I-CSCF is failing on SIP forwarding, it must have already succeeded in its Diameter query to the HSS, which falsifies this hypothesis. Relevant flow steps: 'ims_registration' step 4 (UAR) and 'vonr_call_setup' step 6 (LIR).


## Parallel Investigators (Phase 5)

**3 sub-Investigator verdict(s):** **1 NOT_DISPROVEN**, **2 DISPROVEN**

### `h1` — ✅ **NOT_DISPROVEN**

**Hypothesis:** The S-CSCF container has exited, which is breaking the VoNR call setup signaling flow and preventing any calls from being established.

**Reasoning:** All probes executed were consistent with the hypothesis. The get_network_status tool confirmed that the 'scscf' container was in an 'exited' state, and a subsequent attempt to probe the Kamailio process within that container failed as expected because the container was not running. The upstream I-CSCF component was confirmed to be in a 'running' state, which is also consistent with the scenario described by the hypothesis.

**Probes executed (3):**
- **Check the status of the 'scscf' container.** ✓ CONSISTENT
    - *Tool:* `print(default_api.get_network_status())`
    - *Observation:* [EVIDENCE: get_network_status() -> "'scscf': 'exited'"]
    - *Comment:* The tool output directly confirms the central claim of the hypothesis: the S-CSCF container has exited.
- **scscf, command='core.uptime'** ✓ CONSISTENT
    - *Tool:* `print(default_api.run_kamcmd(container='scscf', command='core.uptime'))`
    - *Observation:* [EVIDENCE: run_kamcmd(container='scscf', command='core.uptime') -> 'Error response from daemon: container 296541323c5727623fad7bf686d0b5b75996b40c220a646eb893b3bc074efb64 is not running']
    - *Comment:* The command failed because the container is not running, which is the expected outcome if the container has exited as the hypothesis states.
- **Check the status of the 'icscf' container.** ✓ CONSISTENT
    - *Tool:* `print(default_api.get_network_status())`
    - *Observation:* [EVIDENCE: get_network_status() -> "'icscf': 'running'"]
    - *Comment:* The observation that the upstream I-CSCF is running is consistent with the hypothesis that the failure is localized to the S-CSCF.

### `h2` — ❌ **DISPROVEN**

**Hypothesis:** The User Plane Function (UPF) is the source of the data plane outage, preventing any GTP-U packets from being forwarded.

**Reasoning:** Probe 1 showed that the UPF is receiving almost no traffic (0.2 pps in), which contradicts the hypothesis that it's the source of the outage by dropping packets. If it were the source, we would expect to see incoming traffic that is not being forwarded. Probe 3 showed that the SMF has successfully established PDU sessions and reports no N4 interface failures, indicating the control plane to the UPF is healthy. The lack of traffic at the UPF is therefore a symptom of an upstream failure, not a fault within the UPF itself.

**Probes executed (3):**
- **Check UPF data plane gauges to see if it is dropping GTP-U traffic.** ✗ CONTRADICTS
    - *Tool:* `default_api.get_dp_quality_gauges(at_time_ts=1777513299.6751771, window_seconds=60)`
    - *Observation:* [EVIDENCE: get_dp_quality_gauges(at_time_ts=1777513299.6751771, window_seconds=60) -> "UPF: in packets/sec: 0.2, out packets/sec: 0.1, in throughput : 0.08 KB/s, out throughput : 0.03 KB/s"]
    - *Comment:* The observation of near-zero traffic contradicts the expectation of high packet loss. The UPF isn't dropping traffic; it's not receiving any. This is consistent with the falsifying observation in the plan.
- **Check gNB's metrics to see if it is transmitting GTP-U packets towards the UPF.** ~ AMBIGUOUS
    - *Tool:* `default_api.get_diagnostic_metrics(at_time_ts=1777513299.6751771, nfs=["nr_gnb"])`
    - *Observation:* [EVIDENCE: get_diagnostic_metrics(at_time_ts=1777513299.6751771, nfs=["nr_gnb"]) -> ""]
    - *Comment:* The tool returned no metrics for nr_gnb. This likely means gNB transmitted zero packets (which would support falsification), but absence of evidence isn't definitive proof. The result is inconclusive on its own but points away from the hypothesis.
- **Check SMF's N4 interface-related metrics for failures.** ✗ CONTRADICTS
    - *Tool:* `default_api.get_diagnostic_metrics(at_time_ts=1777513299.6751771, nfs=["smf"])`
    - *Observation:* [EVIDENCE: get_diagnostic_metrics(at_time_ts=1777513299.6751771, nfs=["smf"]) -> "fivegs_smffunction_sm_sessionnbr = 4 [counter]"]
    - *Comment:* The SMF reports active PDU sessions and no N4 interface errors were observed, contradicting the expectation of failures. This indicates the control plane link between the SMF and the UPF is healthy.

**Alternative suspects:** nr_gnb, scscf

### `h3` — ❌ **DISPROVEN**

**Hypothesis:** The I-CSCF is unable to select an S-CSCF, due to a failure in its Diameter communication with the HSS.

**Reasoning:** The hypothesis is disproven because metrics show the I-CSCF is not experiencing Diameter communication failures with the HSS. Instead, the evidence points to the I-CSCF not receiving any SIP traffic from upstream, which means it isn't even initiating the Diameter queries that the hypothesis claims are failing. The fault lies upstream of the I-CSCF.

**Probes executed (3):**
- **Get metrics for 'icscf', specifically Kamailio CDP (Diameter) metrics.** ✗ CONTRADICTS
    - *Tool:* `print(default_api.get_diagnostic_metrics(at_time_ts = 1777513299.6751771, nfs = ["icscf"]))`
    - *Observation:* [EVIDENCE: get_diagnostic_metrics(at_time_ts=1777513299.6751771, nfs=['icscf']) -> "cdp:timeout = 0 [counter]", "normalized.icscf.core:rcv_requests_register_per_ue = 0"]
    - *Comment:* The hypothesis expected Diameter timeouts or errors. The observation of zero timeouts contradicts this. Furthermore, the observation that the I-CSCF is receiving zero SIP requests indicates the premise of the hypothesis is wrong; the I-CSCF isn't failing at the Diameter step, it's not even reaching that step.
- **Get metrics for 'pyhss', specifically Diameter Cx interface metrics.** ~ AMBIGUOUS
    - *Tool:* `print(default_api.get_diagnostic_metrics(at_time_ts = 1777513299.6751771, nfs = ["pyhss"]))`
    - *Observation:* [EVIDENCE: get_diagnostic_metrics(at_time_ts=1777513299.6751771, nfs=['pyhss']) -> ""]
    - *Comment:* The tool returned no metrics for the HSS. This is inconclusive; it neither supports nor contradicts the hypothesis.
- **Get metrics for 'icscf', looking for SIP forwarding failures (e.g. 'kamailio.tm.uac_replies' with 5xx codes).** ✗ CONTRADICTS
    - *Tool:* `print(default_api.get_diagnostic_metrics(at_time_ts = 1777513299.6751771, nfs = ["icscf"]))`
    - *Observation:* [EVIDENCE: get_diagnostic_metrics(at_time_ts=1777513299.6751771, nfs=['icscf']) -> "derived.icscf_sip_error_ratio = 0"]
    - *Comment:* While low SIP forwarding failures were considered 'consistent' in the plan, this is misleading. Combined with the evidence from the first probe that the I-CSCF is receiving no SIP requests, the zero SIP error ratio confirms that the I-CSCF is idle. The failure is not within the I-CSCF's logic, but in the lack of inbound traffic.

**Alternative suspects:** pcscf


## Evidence Validation (Phase 6)

**Overall verdict:** clean
**Overall confidence:** high

**Per sub-Investigator:**

| Agent | Tool Calls | Citations | Verdict | Confidence |
|---|---|---|---|---|
| `InvestigatorAgent_h1` | 2 | 3/3 | clean | high |
| `InvestigatorAgent_h2` | 3 | 3/3 | clean | high |
| `InvestigatorAgent_h3` | 3 | 3/3 | clean | high |


## Ground Truth

**Failure domain:** ims_signaling  
**Protocol impact:** SIP  
**Affected components:** scscf  
**Severity:** degraded

## Agent Diagnosis

**Model:** v6-adk/gemini-2.5-flash+gemini-2.5-pro  
**Time to diagnosis:** 187.5s

### Prompt to RCA Agent

```
The 5G SA + IMS stack is experiencing issues. Investigate and diagnose the root cause.
```

**Diagnosis:**

> ### causes
> - **summary**: The Serving-CSCF (S-CSCF) container has exited, which has caused a complete failure of the IMS signaling plane, preventing any Voice over New Radio (VoNR) calls from being established.
> - **timeline**:
>     1. No anomalous events were fired by the monitoring system; the outage was detected via end-to-end test failures.
> - **root_cause**: The S-CSCF container has exited, breaking the VoNR call setup signaling flow. The primary suspect network function is `scscf`.
> - **affected_components**:
>     - `scscf`: Root Cause
>     - `upf`: Symptomatic
>     - `smf`: Symptomatic
>     - `icscf`: Symptomatic
> - **recommendation**: Investigate the logs of the `scscf` container to determine the reason for its unexpected exit. Concurrently, examine the I-CSCF logs for SIP errors related to its failure to forward requests to the now-unavailable S-CSCF.
> - **confidence**: high
> - **explanation**: The investigation confirmed with high confidence that the S-CSCF is the source of the outage. Hypothesis `h1` was validated when investigation tools confirmed the `scscf` container was in an 'exited' state. This single failure in a critical IMS component explains the entire outage. Alternative hypotheses were disproven: the UPF (`h2`) was ruled out because it is receiving almost no traffic, proving the fault is in the upstream signaling plane, not the data plane. The I-CSCF (`h3`) was also ruled out, as metrics showed it was not receiving any SIP requests to process, indicating the failure point was downstream from it.

### Scoring Breakdown

**Overall score: 100%**

**Scorer assessment:** The agent provided an excellent and highly accurate diagnosis, correctly identifying the S-CSCF crash as the root cause, its impact, and its location within the network stack with high confidence.

| Dimension | Result | Rationale |
|-----------|--------|-----------|
| Root cause correct | Yes | The agent correctly identified the S-CSCF container exiting as the root cause, which directly matches the simulated failure mode of the S-CSCF being unreachable/down. |
| Component overlap | 100% | The agent explicitly listed 'scscf' as the 'Root Cause' in the 'affected_components' section, which is the primary affected component in the simulated failure. |
| Severity correct | Yes | The agent described a 'complete failure of the IMS signaling plane' and that VoNR calls are 'prevented from being established', which accurately reflects the complete outage caused by the S-CSCF crash. |
| Fault type identified | Yes | The agent identified the fault as the 'S-CSCF container has exited' and being in an 'exited' state, which clearly indicates a component being down/unresponsive, matching the 'component completely unreachable' simulated failure type. |
| Layer accuracy | Yes | The agent's network analysis correctly rated the 'ims' layer as 'red' and provided evidence directly related to the S-CSCF, which belongs to the IMS layer. |
| Confidence calibrated | Yes | The agent stated 'high' confidence, which is appropriate given the accuracy and completeness of its diagnosis across all dimensions. |

**Ranking position:** #1 — The agent provided a single, clear root cause ('scscf container has exited') and explicitly labeled 'scscf' as the 'Root Cause' in its affected components, indicating it was the top and only root cause identified.


### Token Usage

| Metric | Count |
|--------|-------|
| Input tokens | 146,313 |
| Output tokens | 5,496 |
| Thinking tokens | 17,854 |
| **Total tokens** | **169,663** |

**Per-phase breakdown:**

| Phase | Tokens | Tool Calls | LLM Calls |
|-------|--------|------------|-----------|
| AnomalyScreener | 0 | 0 | 0 |
| EventAggregator | 0 | 0 | 0 |
| CorrelationAnalyzer | 0 | 0 | 0 |
| NetworkAnalystAgent | 39,644 | 3 | 4 |
| InstructionGeneratorAgent | 22,896 | 3 | 2 |
| InvestigatorAgent_h1 | 28,665 | 2 | 3 |
| InvestigatorAgent_h2 | 23,241 | 3 | 2 |
| InvestigatorAgent_h3 | 49,612 | 3 | 4 |
| EvidenceValidator | 0 | 0 | 0 |
| SynthesisAgent | 5,605 | 0 | 1 |


## Resolution

**Heal method:** scheduled  
**Recovery time:** 322.4s
