# Episode Report: DNS Failure

**Agent:** v6  
**Episode ID:** ep_20260429_164313_dns_failure  
**Date:** 2026-04-29T16:43:15.770309+00:00  
**Duration:** 295.7s  

---

## Scenario

**Category:** container  
**Blast radius:** global  
**Description:** Kill the DNS server. IMS domain resolution breaks — SIP routing depends on DNS NAPTR/SRV records for the IMS domain. All new SIP transactions that require DNS resolution will fail.

## Faults Injected

- **container_kill** on `dns`

## Baseline (Pre-Fault)

Stack phase before injection: **ready**
All containers running at baseline.

## Fault Propagation Verification

**Verdict:** ✅ `confirmed`

- **Wait:** 0s
- **Actual elapsed:** 0.0s
- **Nodes with significant deltas:** 3
- **Nodes with any drift:** 4

## Symptoms Observed

Symptoms detected: **Yes**  
Observation iterations: 1

## Anomaly Screening (Phase 0)

**ANOMALY DETECTED.** Overall anomaly score: 44.56 (per-bucket threshold: 26.31, context bucket (0, 1), trained on 323 healthy snapshots). The current metric pattern is statistically different from the learned healthy baseline. Something in the network has changed.

The following metrics deviate from their learned-healthy baseline. Treat each as a semantic observation (meaning + numbers), not a number alone — the KB's interpretation is the authoritative reading.

- **`context.cx_active`** — current **0.00** vs learned baseline **0.59** (MEDIUM, drop). *(No KB context available — interpret from the metric name.)*

- **`normalized.icscf.cdp_replies_per_ue`** (I-CSCF Diameter reply rate per UE) — current **0.00 replies_per_second_per_ue** vs learned baseline **0.03 replies_per_second_per_ue** (MEDIUM, drop)
    - **What it measures:** Liveness of the I-CSCF↔HSS Cx path. Drops to 0 when HSS is unreachable OR when no signaling is occurring at the I-CSCF (idle or upstream P-CSCF partitioned).
    - **Drop means:** No Cx replies in the window. Could be healthy idle OR a Cx-path fault.
    - **Healthy typical range:** 0–1 replies_per_second_per_ue

- **`normalized.icscf.core:rcv_requests_register_per_ue`** (SIP REGISTER rate per UE at I-CSCF) — current **0.00 requests_per_second** vs learned baseline **0.06 requests_per_second** (MEDIUM, drop)
    - **What it measures:** Health of the P-CSCF → I-CSCF forwarding path (Mw interface). When
this drops to zero while P-CSCF REGISTER rate is still non-zero,
it's the SIGNATURE of an IMS partition between P-CSCF and I-CSCF.
    - **Drop means:** Either UEs not registering at all, or P-CSCF isolated from I-CSCF.
    - **Healthy typical range:** 0–0.5 requests_per_second
    - **Healthy invariant:** Per-UE rate. Should closely track ims.pcscf.rcv_requests_register_per_ue.

- **`normalized.pcscf.core:rcv_requests_invite_per_ue`** (SIP INVITE rate per UE at P-CSCF) — current **0.01 requests_per_second** vs learned baseline **0.00 requests_per_second** (MEDIUM, spike)
    - **What it measures:** Call attempt rate from registered UEs. Unlike REGISTER (periodic),
INVITEs only fire when UEs place calls. Zero is normal during
quiet periods; nonzero INVITE with zero dialogs is the signature
of call setup failure.
    - **Spike means:** Fewer call attempts.
    - **Healthy typical range:** 0–0.2 requests_per_second
    - **Healthy invariant:** Per-UE rate.

- **`normalized.pcscf.core:rcv_requests_register_per_ue`** (SIP REGISTER rate per UE at P-CSCF) — current **0.01 requests_per_second** vs learned baseline **0.06 requests_per_second** (MEDIUM, drop)
    - **What it measures:** How actively UEs are refreshing their IMS registrations with the
P-CSCF. REGISTERs arrive periodically (re-registration timer) plus
at attach. Sustained zero means UEs cannot reach P-CSCF OR the
UE-to-network SIP path is broken.
    - **Drop means:** No REGISTERs flowing. Unusual unless UEs are all deregistered.
    - **Healthy typical range:** 0–0.5 requests_per_second
    - **Healthy invariant:** Per-UE rate — same value at any deployment scale.

- **`normalized.scscf.cdp_replies_per_ue`** (S-CSCF CDP Diameter replies per UE) — current **0.00 replies_per_second_per_ue** vs learned baseline **0.06 replies_per_second_per_ue** (MEDIUM, drop)
    - **What it measures:** Active S-CSCF Diameter traffic with HSS. Near-zero when registrations idle OR HSS partition.
    - **Drop means:** No active S-CSCF Diameter exchanges (idle or partitioned).
    - **Healthy typical range:** 0–1 replies_per_second_per_ue
    - **Healthy invariant:** Per-UE rate; varies with registration/auth load.

- **`normalized.scscf.core:rcv_requests_register_per_ue`** (SIP REGISTER rate per UE at S-CSCF) — current **0.00 requests_per_second** vs learned baseline **0.06 requests_per_second** (MEDIUM, drop)
    - **What it measures:** Health of the I-CSCF → S-CSCF forwarding path. Drop to zero while
I-CSCF is receiving REGISTERs = S-CSCF-side issue (crashed, or
I-CSCF → S-CSCF path broken).
    - **Drop means:** S-CSCF isolated or not running.
    - **Healthy typical range:** 0–0.5 requests_per_second
    - **Healthy invariant:** Per-UE rate. Tracks icscf.register rate.

- **`normalized.smf.bearers_per_ue`** (Active QoS bearers per UE) — current **3.50 count** vs learned baseline **2.48 count** (MEDIUM, shift)
    - **What it measures:** Per-UE count of active QoS bearers. Baseline reflects default
bearers; increments during VoNR calls indicate dedicated voice
bearers being set up. Drop during an active call = dedicated
bearer torn down unexpectedly (voice will fail).
    - **Shift means:** Expected during VoNR calls (1 extra bearer per active call).
    - **Healthy typical range:** 2–3.5 count
    - **Healthy invariant:** At rest: equals configured default bearers (typically 2 per UE).
During active VoNR call: +1 per caller. The per-UE ratio is the
invariant; absolute count scales with UE pool.

- **`normalized.upf.gtp_indatapktn3upf_per_ue`** (GTP-U uplink rate per UE (N3)) — current **0.09 packets_per_second** vs learned baseline **1.45 packets_per_second** (LOW, drop)
    - **What it measures:** Health of the uplink user-plane path gNB → UPF. Drops to near-zero
during RAN or N3 outage; stays nonzero during active calls or data
sessions. Decoupled from SIP signaling (signals data plane, not
control plane).
    - **Drop means:** Data plane dead on uplink — UPF receiving no packets from gNB.
    - **Healthy typical range:** 0–10 packets_per_second
    - **Healthy invariant:** Per-UE — constant regardless of UE pool size. Rises during active VoNR calls (~100 pps for G.711 voice) and data transfer.

- **`normalized.upf.gtp_outdatapktn3upf_per_ue`** (GTP-U downlink rate per UE (N3)) — current **0.06 packets_per_second** vs learned baseline **1.45 packets_per_second** (LOW, drop)
    - **What it measures:** Health of the downlink user-plane path UPF → gNB. Typically mirrors
uplink rate during calls/data; asymmetry indicates directional
faults (e.g., UPF receiving from internet but not forwarding).
    - **Drop means:** No traffic leaving UPF toward RAN.
    - **Healthy typical range:** 0–10 packets_per_second
    - **Healthy invariant:** Per-UE — constant regardless of UE pool size. Roughly symmetric with uplink during healthy calls.


## Event Aggregation (Phase 1)

No events fired during this episode. Either no metric KB triggers matched, or the episode encountered no meaningful state transitions.

## Correlation Analysis (Phase 2)

No events fired — correlation engine had nothing to work with.

## Network Analysis (Phase 3)

**Summary:** The IMS stack is down due to a failure of the DNS service, which prevents service discovery between all IMS components.

**Layer status:**

| Layer | Rating | Note |
|---|---|---|
| **infrastructure** | 🔴 RED | The DNS service is a critical piece of infrastructure for the IMS stack. Its failure is the root cause of the outage. |
| **ran** | 🟢 GREEN | The RAN appears healthy and connected to the AMF. The fault lies upstream in the IMS domain. |
| **core** | 🟢 GREEN | 5G Core components (AMF, SMF, UPF) appear to be running and configured correctly. The issue is isolated to the IMS subsystem. |
| **ims** | 🔴 RED | The entire IMS signaling plane has collapsed. Components cannot route SIP or Diameter messages because they cannot resolve hostnames via the failed DNS service. |

**INFRASTRUCTURE evidence:**
- The 'dns' container has an 'exited' status.
- Network topology shows all links to DNS are INACTIVE.

**IMS evidence:**
- normalized.icscf.cdp_replies_per_ue: 0
- normalized.icscf.core:rcv_requests_register_per_ue: 0
- normalized.scscf.cdp_replies_per_ue: 0
- normalized.pcscf.core:rcv_requests_register_per_ue: near 0

**Ranked hypotheses:**

- **`h1`** (fit=1.00, nf=dns, specificity=specific):
    - **Statement:** The 'dns' container has exited. This prevents all IMS components (P-CSCF, I-CSCF, S-CSCF, HSS) from performing the DNS lookups required to discover and communicate with each other, causing a total outage of all IMS procedures, including registration.
    - **Falsification probes:**
        - Restart the 'dns' container; if IMS registration and call setup succeed, the hypothesis is confirmed.
        - From the 'pcscf' container, attempt to resolve the hostname 'icscf'; if this fails, the hypothesis is supported.


## Falsification Plans (Phase 4)

**1 falsification plan(s) — one per hypothesis:**

### Plan for `h1` (target: `dns`)

**Hypothesis:** The 'dns' container has exited. This prevents all IMS components (P-CSCF, I-CSCF, S-CSCF, HSS) from performing the DNS lookups required to discover and communicate with each other, causing a total outage of all IMS procedures, including registration.

**Probes (3):**
1. **`get_network_status`** — Check the status of all containers, with a focus on 'dns'.
    - *Expected if hypothesis holds:* The container 'dns' will be in an 'exited' or 'down' state.
    - *Falsifying observation:* The container 'dns' is in a 'running' state. This would directly contradict the hypothesis.
2. **`check_process_listeners`** — container='dns'
    - *Expected if hypothesis holds:* The tool will either fail (if the container is down) or return an empty list of listeners, confirming no process is listening on the DNS port.
    - *Falsifying observation:* The output shows a process listening on UDP port 53. This would indicate the DNS service is running and listening for requests, falsifying the hypothesis.
3. **`get_nf_metrics`** — Filter metrics for the 'pcscf' container. Look for metrics related to outbound connection timeouts or DNS resolution failures.
    - *Expected if hypothesis holds:* There will be a high or increasing count of client-side SIP transaction timeouts (e.g., kamailio_tm_uac_timeout) or specific DNS failure metrics on the P-CSCF, as it fails to resolve the I-CSCF hostname.
    - *Falsifying observation:* Zero values for DNS failure and client-side timeout metrics on the P-CSCF. This suggests the P-CSCF is successfully resolving and connecting to its upstream hop.

*Notes:* This plan directly tests the central claim of the hypothesis: the status of the 'dns' container. It then verifies the expected service impact by checking for listening ports and observing client-side metrics on an affected IMS component (P-CSCF).


## Parallel Investigators (Phase 5)

**1 sub-Investigator verdict(s):** **1 DISPROVEN**

### `h1` — ❌ **DISPROVEN**

**Hypothesis:** The 'dns' container has exited. This prevents all IMS components (P-CSCF, I-CSCF, S-CSCF, HSS) from performing the DNS lookups required to discover and communicate with each other, causing a total outage of all IMS procedures, including registration.

**Reasoning:** Probes 1 and 2 confirmed the central fact that the 'dns' container has exited. However, Probe 3 produced strong evidence contradicting the hypothesis's claimed impact. Metrics show that SIP requests are flowing from P-CSCF to I-CSCF, and the S-CSCF has zero Diameter timeouts in its communication with the HSS. This directly falsifies the assertion that *all* IMS components are prevented from performing DNS lookups and communicating.

**Probes executed (3):**
- **Check the status of all containers, with a focus on 'dns'.** ✓ CONSISTENT
    - *Tool:* `print(default_api.get_network_status())`
    - *Observation:* [EVIDENCE: get_network_status() -> "containers": {"dns": "exited"}]
    - *Comment:* The tool output confirms that the 'dns' container is in an 'exited' state, which is consistent with the hypothesis's prerequisite.
- **Check what ports and protocols a container's processes are listening on.** ✓ CONSISTENT
    - *Tool:* `print(default_api.check_process_listeners(container = "dns"))`
    - *Observation:* [EVIDENCE: check_process_listeners("dns") -> "Error response from daemon: container a3236bbd200d0c9869398d241e9584e0f1e681c398164fe88aedae8a8d368477 is not running"]
    - *Comment:* The tool failed because the container is not running, which is the expected outcome if the container has exited. This confirms no DNS service can be running in that container.
- **Filter metrics for the 'pcscf' container. Look for metrics related to outbound connection timeouts or DNS resolution failures.** ✗ CONTRADICTS
    - *Tool:* `print(default_api.get_nf_metrics())`
    - *Observation:* [EVIDENCE: get_nf_metrics() -> "ICSCF: core:rcv_requests_register = 48.0, SCSCF: cdp:timeout = 0.0"]
    - *Comment:* The metrics show that the I-CSCF has received 48 REGISTER requests, proving P-CSCF can resolve and connect to it. Furthermore, the S-CSCF shows 0 Diameter timeouts, proving it can resolve and connect to the HSS. This contradicts the hypothesis that all inter-component communication is failing due to DNS.

**Alternative suspects:** icscf


## Evidence Validation (Phase 6)

**Overall verdict:** clean
**Overall confidence:** high

**Per sub-Investigator:**

| Agent | Tool Calls | Citations | Verdict | Confidence |
|---|---|---|---|---|
| `InvestigatorAgent_h1` | 3 | 3/3 | clean | high |


## Ground Truth

**Failure domain:** data_layer  
**Protocol impact:** multiple  
**Affected components:** dns  
**Severity:** degraded

## Agent Diagnosis

**Model:** v6-adk/gemini-2.5-flash+gemini-2.5-pro  
**Time to diagnosis:** 141.5s

### Prompt to RCA Agent

```
The 5G SA + IMS stack is experiencing issues. Investigate and diagnose the root cause.
```

**Diagnosis:**

> ### causes
> - **summary**: The initial hypothesis of a complete IMS outage due to DNS failure has been disproven; evidence now points to a fault within the I-CSCF, which is receiving registration requests but failing to forward them.
> - **timeline**:
>     1. The investigation began with the hypothesis that a downed 'dns' container was preventing all IMS components from communicating.
>     2. Probes confirmed that the 'dns' container had indeed exited.
>     3. However, metrics analysis contradicted the hypothesis's impact, showing that the P-CSCF was successfully sending SIP REGISTER requests to the I-CSCF, and the S-CSCF was communicating with the HSS without errors.
>     4. This proved that component-to-component communication was not completely broken, disproving the DNS hypothesis and isolating the fault downstream from the P-CSCF.
>     5. The I-CSCF is now the primary suspect, as it is the last component known to receive the signaling traffic.
> - **root_cause**: The root cause is not confirmed. The initial hypothesis (DNS failure) was disproven. The investigation surfaced a new lead, with evidence suggesting a fault in the `icscf`.
> - **affected_components**:
>     - `icscf`: Root Cause (Candidate)
>     - `pcscf`: Symptomatic
>     - `scscf`: Symptomatic
>     - `hss`: Symptomatic
>     - `dns`: Secondary
> - **recommendation**: The operator should investigate the `icscf`. Verify its logs and configuration to determine why it is accepting SIP REGISTER requests from the `pcscf` but not forwarding user authorization requests to the HSS or routing registration requests to the `scscf`.
> - **confidence**: low
> - **explanation**: The initial and only hypothesis, which claimed a `dns` container failure was preventing all IMS service discovery, was definitively disproven. The Investigator confirmed the `dns` container was down but found contradictory evidence showing that some components were still able to communicate successfully (e.g., P-CSCF to I-CSCF). This falsified the core assertion that DNS was the cause of the *entire* outage. The evidence suggests that traffic is reaching the I-CSCF but is not being processed or forwarded correctly, making it the most likely suspect. Confidence is low because the original hypothesis set was exhausted and this is a new lead requiring manual investigation.

### Scoring Breakdown

**Overall score: 31%**

**Scorer assessment:** The agent correctly identified the DNS component as down and attributed it to the correct layer, and its low confidence was calibrated given its incorrect conclusion. However, it incorrectly dismissed DNS as the root cause, misidentified the primary affected component, and failed to correctly assess the severity of the outage.

| Dimension | Result | Rationale |
|-----------|--------|-----------|
| Root cause correct | No | The simulated failure was a DNS failure, with the 'dns' component being unreachable. The agent's final diagnosis explicitly states that the 'initial hypothesis (DNS failure) was disproven' and instead suggests a fault in the 'icscf' as a new lead. This directly contradicts the ground truth. |
| Component overlap | 30% | The primary affected component is 'dns'. The agent lists 'dns' in its 'affected_components' but labels it as 'Secondary', while labeling 'icscf' as 'Root Cause (Candidate)'. This indicates the agent identified the component but mis-ranked its causal role. |
| Severity correct | No | The simulated failure resulted in 'All new SIP transactions that require DNS resolution will fail', indicating a complete outage for affected services. The agent's summary states that the 'initial hypothesis of a complete IMS outage due to DNS failure has been disproven', implying a less severe or different type of outage than what occurred. |
| Fault type identified | Yes | The agent's explanation explicitly states that 'Probes confirmed that the 'dns' container had indeed exited' and 'The Investigator confirmed the 'dns' container was down'. This correctly identifies the observable fault type for the DNS component (down/unreachable), even though it later dismissed it as the root cause. |
| Layer accuracy | Yes | The ground truth states that 'dns' belongs to the 'infrastructure' layer. The agent's NETWORK ANALYSIS correctly rates the 'infrastructure' layer as 'red' and provides evidence that 'The 'dns' container has an 'exited' status', noting 'The DNS service is a critical piece of infrastructure'. |
| Confidence calibrated | Yes | The agent's diagnosis was incorrect regarding the root cause and severity. Its stated 'confidence: low' is appropriate given the inaccuracies in its final conclusion. |

**Ranking:** The agent explicitly disproved the correct root cause (DNS failure) and instead proposed an incorrect component ('icscf') as the primary suspect. In the 'affected_components' list, 'dns' is listed as 'Secondary' while 'icscf' is 'Root Cause (Candidate)', meaning the correct cause is not ranked first.


### Token Usage

| Metric | Count |
|--------|-------|
| Input tokens | 85,742 |
| Output tokens | 2,501 |
| Thinking tokens | 10,140 |
| **Total tokens** | **98,383** |

**Per-phase breakdown:**

| Phase | Tokens | Tool Calls | LLM Calls |
|-------|--------|------------|-----------|
| AnomalyScreener | 0 | 0 | 0 |
| EventAggregator | 0 | 0 | 0 |
| CorrelationAnalyzer | 0 | 0 | 0 |
| NetworkAnalystAgent | 41,388 | 5 | 3 |
| InstructionGeneratorAgent | 15,342 | 1 | 2 |
| InvestigatorAgent_h1 | 38,050 | 3 | 4 |
| EvidenceValidator | 0 | 0 | 0 |
| SynthesisAgent | 3,603 | 0 | 1 |


## Resolution

**Heal method:** scheduled  
**Recovery time:** 295.7s
