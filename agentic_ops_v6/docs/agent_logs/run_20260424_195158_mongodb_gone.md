# Episode Report: MongoDB Gone

**Agent:** v6  
**Episode ID:** ep_20260424_194728_mongodb_gone  
**Date:** 2026-04-24T19:47:30.467587+00:00  
**Duration:** 266.8s  

---

## Scenario

**Category:** container  
**Blast radius:** global  
**Description:** Kill MongoDB — the 5G core subscriber data store. UDR loses its backend, new PDU sessions cannot be created, and subscriber queries fail. Existing sessions may survive briefly.

## Faults Injected

- **container_kill** on `mongo`

## Baseline (Pre-Fault)

Stack phase before injection: **ready**
All containers running at baseline.

## Fault Propagation Verification

**Verdict:** ✅ `confirmed`

- **Wait:** 0s
- **Actual elapsed:** 0.0s
- **Nodes with significant deltas:** 3
- **Nodes with any drift:** 4

## Symptoms Observed

Symptoms detected: **Yes**  
Observation iterations: 1

## Anomaly Screening (Phase 0)

**ANOMALY DETECTED.** Overall anomaly score: 0.99 (threshold: 0.70, trained on 209 healthy snapshots). The current metric pattern is statistically different from the learned healthy baseline. Something in the network has changed.

The following metrics deviate from their learned-healthy baseline. Treat each as a semantic observation (meaning + numbers), not a number alone — the KB's interpretation is the authoritative reading.

- **`derived.pcscf_sip_error_ratio`** (P-CSCF SIP error response ratio) — current **1.00 ratio** vs learned baseline **0.00 ratio** (HIGH, spike)
    - **What it measures:** Proportion of SIP responses that are errors. Zero is the healthy
baseline; any sustained non-zero value means P-CSCF or something
downstream is rejecting requests.
    - **Spike means:** Errors flowing back — downstream CSCFs or HSS rejecting.
    - **Healthy typical range:** 0–0 ratio
    - **Healthy invariant:** Zero in healthy operation.

- **`derived.upf_activity_during_calls`** — current **1.00** vs learned baseline **0.09** (HIGH, spike). *(No KB context available — interpret from the metric name.)*

- **`normalized.upf.gtp_outdatapktn3upf_per_ue`** (GTP-U downlink rate per UE (N3)) — current **0.03 packets_per_second** vs learned baseline **6.08 packets_per_second** (HIGH, drop)
    - **What it measures:** Health of the downlink user-plane path UPF → gNB. Typically mirrors
uplink rate during calls/data; asymmetry indicates directional
faults (e.g., UPF receiving from internet but not forwarding).
    - **Drop means:** No traffic leaving UPF toward RAN.
    - **Healthy typical range:** 0–10 packets_per_second
    - **Healthy invariant:** Per-UE — constant regardless of UE pool size. Roughly symmetric with uplink during healthy calls.

- **`normalized.upf.gtp_indatapktn3upf_per_ue`** (GTP-U uplink rate per UE (N3)) — current **0.13 packets_per_second** vs learned baseline **6.15 packets_per_second** (HIGH, drop)
    - **What it measures:** Health of the uplink user-plane path gNB → UPF. Drops to near-zero
during RAN or N3 outage; stays nonzero during active calls or data
sessions. Decoupled from SIP signaling (signals data plane, not
control plane).
    - **Drop means:** Data plane dead on uplink — UPF receiving no packets from gNB.
    - **Healthy typical range:** 0–10 packets_per_second
    - **Healthy invariant:** Per-UE — constant regardless of UE pool size. Rises during active VoNR calls (~100 pps for G.711 voice) and data transfer.

- **`normalized.smf.bearers_per_ue`** (Active QoS bearers per UE) — current **2.50 count** vs learned baseline **4.15 count** (MEDIUM, shift)
    - **What it measures:** Per-UE count of active QoS bearers. Baseline reflects default
bearers; increments during VoNR calls indicate dedicated voice
bearers being set up. Drop during an active call = dedicated
bearer torn down unexpectedly (voice will fail).
    - **Shift means:** Expected during VoNR calls (1 extra bearer per active call).
    - **Healthy typical range:** 2–3.5 count
    - **Healthy invariant:** At rest: equals configured default bearers (typically 2 per UE).
During active VoNR call: +1 per caller. The per-UE ratio is the
invariant; absolute count scales with UE pool.

- **`normalized.pcscf.dialogs_per_ue`** (Active SIP dialogs per registered UE at P-CSCF) — current **0.00 count** vs learned baseline **1.58 count** (MEDIUM, drop)
    - **What it measures:** How many calls per user are currently in progress at the P-CSCF.
Going to zero from a non-zero baseline means calls have ended
(normal) OR call setup is failing system-wide (degradation).
Together with rcv_requests_* it discriminates the two.
    - **Drop means:** No active calls. Normal at rest; suspicious if prior state had calls AND INVITE rate is also zero.
    - **Healthy typical range:** 0–1 count
    - **Healthy invariant:** Per-UE — scale-independent. 0 at rest, ~1 per active VoNR call.


## Event Aggregation (Phase 1)

No events fired during this episode. Either no metric KB triggers matched, or the episode encountered no meaningful state transitions.

## Correlation Analysis (Phase 2)

No events fired — correlation engine had nothing to work with.

## Network Analysis (Phase 3)

**Summary:** The backend MongoDB container has exited, causing a cascading failure in the core and IMS layers, resulting in a complete outage of VoNR call services.

**Layer status:**

| Layer | Rating | Note |
|---|---|---|
| **infrastructure** | 🔴 RED | The primary database for UDR and PCF is down. This is the root cause of the entire outage. |
| **ran** | 🟢 GREEN | The RAN appears healthy and UEs are connected to the AMF. |
| **core** | 🔴 RED | Core NFs that depend on MongoDB (UDR, PCF) are failing. The UPF is idle because the control plane cannot establish calls, but is not the source of the fault. |
| **ims** | 🔴 RED | The IMS control plane is non-functional. P-CSCF is rejecting all requests due to downstream policy failures, and I-CSCF is timing out. |

**INFRASTRUCTURE evidence:**
- get_network_status: mongo container is 'exited'
- get_network_topology: Multiple NFs show INACTIVE links to MongoDB

**CORE evidence:**
- find_chains_by_observable_metric: 'subscriber_data_store_unavailable' chain implicates UDR and PCF
- normalized.upf.gtp_indatapktn3upf_per_ue near zero
- normalized.upf.gtp_outdatapktn3upf_per_ue near zero

**IMS evidence:**
- derived.pcscf_sip_error_ratio: 1.00
- normalized.pcscf.dialogs_per_ue: 0.00
- get_nf_metrics: I-CSCF is experiencing Diameter timeouts

**Ranked hypotheses:**

- **`h1`** (fit=0.95, nf=pcf, specificity=specific):
    - **Statement:** The primary cause of the outage is a failure at the PCF. Because its MongoDB backend is down, the PCF cannot retrieve subscriber policy data. This causes it to reject N5 policy authorization requests from the P-CSCF, which in turn leads the P-CSCF to reject all incoming SIP REGISTER and INVITE requests with a '412 Precondition Failed' error, causing a 100% SIP error rate and a total loss of service.
    - **Supporting events:** `derived.pcscf_sip_error_ratio: 1.00`, `get_network_status: mongo container is 'exited'`, `get_nf_metrics: pcscf.httpclient:connfail is high`, `find_chains_by_observable_metric: subscriber_data_store_unavailable->pcscf_n5_call_setup`
    - **Falsification probes:**
        - Restarting the 'mongo' container should cause PCF to recover and SIP errors to cease.
        - Directly probing the PCF's Npcf_AppPolicyControl API endpoint for a subscriber would result in an error.
- **`h2`** (fit=0.70, nf=hss, specificity=moderate):
    - **Statement:** As a secondary effect of the MongoDB outage, the HSS is unable to respond to the I-CSCF in a timely manner. The I-CSCF is experiencing Diameter timeouts on the Cx interface (UAR/LIR requests). This is likely because the HSS has a dependency on the UDR/UDM for certain data, and the UDR is unable to function without its database, causing HSS lookups to fail or time out.
    - **Supporting events:** `get_nf_metrics: icscf.cdp:timeout > 0`, `get_nf_metrics: ims_icscf:uar_timeouts > 0`, `get_nf_metrics: ims_icscf:lir_timeouts > 0`, `get_network_status: mongo container is 'exited'`
    - **Falsification probes:**
        - HSS logs should show errors or timeouts related to UDM or UDR communication.
        - A direct Diameter query to the HSS for subscriber information would time out or return an error.


## Falsification Plans (Phase 4)

*No output produced.*

## Parallel Investigators (Phase 5)

**2 sub-Investigator verdict(s):** **2 INCONCLUSIVE**

### `h1` — ❓ **INCONCLUSIVE**

**Hypothesis:** The primary cause of the outage is a failure at the PCF. Because its MongoDB backend is down, the PCF cannot retrieve subscriber policy data. This causes it to reject N5 policy authorization requests from the P-CSCF, which in turn leads the P-CSCF to reject all incoming SIP REGISTER and INVITE requests with a '412 Precondition Failed' error, causing a 100% SIP error rate and a total loss of service.

**Reasoning:** No falsification plan was generated for this hypothesis.

### `h2` — ❓ **INCONCLUSIVE**

**Hypothesis:** As a secondary effect of the MongoDB outage, the HSS is unable to respond to the I-CSCF in a timely manner. The I-CSCF is experiencing Diameter timeouts on the Cx interface (UAR/LIR requests). This is likely because the HSS has a dependency on the UDR/UDM for certain data, and the UDR is unable to function without its database, causing HSS lookups to fail or time out.

**Reasoning:** No falsification plan was generated for this hypothesis.


## Evidence Validation (Phase 6)

**Overall verdict:** severe
**Overall confidence:** none

**Per sub-Investigator:**

| Agent | Tool Calls | Citations | Verdict | Confidence |
|---|---|---|---|---|
| `InvestigatorAgent_h1` | 0 | 0/0 | severe | none |
|  |  |  |  | *ZERO tool calls — all citations fabricated* |
| `InvestigatorAgent_h2` | 0 | 0/0 | severe | none |
|  |  |  |  | *ZERO tool calls — all citations fabricated* |


## Ground Truth

**Failure domain:** data_layer  
**Protocol impact:** multiple  
**Affected components:** mongo  
**Severity:** degraded

## Agent Diagnosis

**Model:** v6-adk/gemini-2.5-flash+gemini-2.5-pro  
**Time to diagnosis:** 128.7s

### Prompt to RCA Agent

```
The 5G SA + IMS stack is experiencing issues. Investigate and diagnose the root cause.
```

**Diagnosis:**

> ### causes
> - **summary**: The backend MongoDB container has exited, causing a cascading failure of the PCF and UDR, which has in turn brought down the IMS control plane and resulted in a complete VoNR service outage.
> - **timeline**:
>     1. The `mongo` container, the primary database for UDR and PCF, exited.
>     2. The PCF lost its database connection and began failing to process policy requests.
>     3. The UDR lost its database connection, impacting dependent NFs like the HSS.
>     4. The P-CSCF, receiving policy authorization failures from the PCF, began rejecting all SIP REGISTER and INVITE requests with a '412 Precondition Failed' error.
>     5. The I-CSCF began experiencing timeouts when querying the HSS for subscriber location, as the HSS could no longer retrieve data from the UDR.
>     6. The combination of these failures led to a 100% SIP error rate and a total loss of service.
> - **root_cause**: The investigation strongly points to the failure of the `mongo` container in the infrastructure layer, which caused an immediate failure in its primary dependents, `pcf` and `udr`. The `primary_suspect_nf` is `pcf`, as its failure is the most direct cause of the observed SIP errors at the P-CSCF.
> - **affected_components**:
>     - `mongo`: Root Cause (Infrastructure)
>     - `pcf`: Root Cause (Core)
>     - `udr`: Root Cause (Core)
>     - `hss`: Secondary
>     - `pcscf`: Symptomatic
>     - `icscf`: Symptomatic
> - **recommendation**: The automated investigation failed to execute any verification steps. The operator should manually verify the initial hypothesis by checking the status of the backend database and the logs of the affected network functions. Specifically, an operator should:
>     1.  Verify the operational state of the `mongo` container.
>     2.  Inspect PCF logs for errors related to database connectivity or policy retrieval failures.
>     3.  Inspect HSS logs for errors or timeouts related to communication with the UDM or UDR.
> - **confidence**: low
> - **explanation**: The Network Analyst report provided two plausible hypotheses (`h1` and `h2`) that correctly identified the `mongo` container outage as the trigger for cascading failures in the PCF and HSS. However, both hypotheses were ruled **INCONCLUSIVE** because the subsequent investigation agents failed to generate or execute any verification probes. Furthermore, the Evidence Validator reported a `severe` finding, noting that the investigation agents fabricated all their supporting evidence without making any tool calls. Due to this complete failure of automated verification, confidence in the diagnosis is **low**, even though the initial analysis appears sound. Manual verification is required.

### Scoring Breakdown

**Overall score: 100%**

**Scorer assessment:** The agent provided an excellent diagnosis, correctly identifying the MongoDB failure as the root cause, its impact on service, and the affected components and layers, while also appropriately calibrating its confidence due to verification issues.

| Dimension | Result | Rationale |
|-----------|--------|-----------|
| Root cause correct | Yes | The agent correctly identified the 'mongo' container failure as the root cause. The diagnosis explicitly states: 'The investigation strongly points to the failure of the `mongo` container in the infrastructure layer'. While it also names 'pcf' as the 'primary_suspect_nf' for the observed SIP errors, the overall diagnosis, including the summary, timeline, and affected components, clearly establishes 'mongo' as the ultimate root cause. |
| Component overlap | 100% | The agent correctly identified 'mongo' as a 'Root Cause' in the `affected_components` list. It also correctly listed 'pcf' and 'udr' as 'Root Cause' due to their immediate dependency, which is acceptable as cascading failures are not penalized. |
| Severity correct | Yes | The simulated failure involved a complete unreachability of MongoDB, leading to a complete service outage. The agent's diagnosis correctly states a 'complete VoNR service outage' and 'total loss of service'. |
| Fault type identified | Yes | The simulated failure was 'MongoDB Gone' (killed container), resulting in the component being 'completely unreachable (down/not responding)'. The agent identified this as 'The backend MongoDB container has exited' and 'failure of the `mongo` container', which accurately describes the observable fault type. |
| Layer accuracy | Yes | The ground truth states 'mongo' belongs to the 'infrastructure' layer. The agent's network analysis correctly rated the 'infrastructure' layer as 'red' with evidence 'mongo container is 'exited'', and the final diagnosis also places 'mongo' in the 'Infrastructure' layer. |
| Confidence calibrated | Yes | The agent's diagnosis is correct, but it explicitly states 'low' confidence due to the failure of automated verification and fabrication of evidence by investigation agents. This demonstrates appropriate calibration, as the agent acknowledges the lack of verifiable evidence despite its initial analysis being sound. |

**Ranking:** The agent provided a single, coherent diagnosis with a clear causal chain, rather than multiple ranked candidates for the root cause. The 'root_cause' field explicitly names 'mongo' as the failure point.


### Token Usage

| Metric | Count |
|--------|-------|
| Input tokens | 43,024 |
| Output tokens | 1,791 |
| Thinking tokens | 9,362 |
| **Total tokens** | **54,177** |

**Per-phase breakdown:**

| Phase | Tokens | Tool Calls | LLM Calls |
|-------|--------|------------|-----------|
| AnomalyScreener | 0 | 0 | 0 |
| EventAggregator | 0 | 0 | 0 |
| CorrelationAnalyzer | 0 | 0 | 0 |
| NetworkAnalystAgent | 42,709 | 7 | 3 |
| InstructionGeneratorAgent | 7,542 | 0 | 1 |
| EvidenceValidator | 0 | 0 | 0 |
| SynthesisAgent | 3,926 | 0 | 1 |


## Resolution

**Heal method:** scheduled  
**Recovery time:** 266.8s
