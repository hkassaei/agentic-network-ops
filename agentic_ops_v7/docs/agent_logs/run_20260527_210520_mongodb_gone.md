# Episode Report: MongoDB Gone

**Agent:** v7  
**Episode ID:** ep_20260527_205700_mongodb_gone  
**Date:** 2026-05-27T20:57:01.965615+00:00  
**Duration:** 498.0s  

---

## Scenario

**Category:** container  
**Blast radius:** global  
**Description:** Kill MongoDB — the 5G core subscriber data store. UDR loses its backend, new PDU sessions cannot be created, and subscriber queries fail. Existing sessions may survive briefly.

## Faults Injected

- **container_kill** on `mongo`

## Baseline (Pre-Fault)

Stack phase before injection: **ready**
All containers running at baseline.

## Fault Propagation Verification

**Verdict:** ✅ `confirmed`

- **Propagation window:** 135s (ObservationTrafficAgent drove traffic for this window; verifier added wait=0s on top)
- **Nodes with significant deltas:** 2
- **Nodes with any drift:** 4

## Symptoms Observed

Symptoms detected: **Yes**  
Observation iterations: 1

## Anomaly Screening (Phase 0)

**ANOMALY DETECTED.** Overall anomaly score: 28.26 (per-bucket threshold: 26.31, context bucket (0, 1), trained on 323 healthy snapshots). The current metric pattern is statistically different from the learned healthy baseline. Something in the network has changed.

The following metrics deviate from their learned-healthy baseline. Treat each as a semantic observation (meaning + numbers), not a number alone — the KB's interpretation is the authoritative reading.

- **`derived.pcscf_sip_error_ratio`** (P-CSCF SIP error response ratio) — current **0.17 ratio** vs learned baseline **0.00 ratio** (MEDIUM, spike)
    - **What it measures:** Proportion of SIP responses that are errors. Zero is the healthy
baseline; any sustained non-zero value means P-CSCF or something
downstream is rejecting requests.
    - **Spike means:** Errors flowing back — downstream CSCFs or HSS rejecting.
    - **Healthy typical range:** 0–0 ratio
    - **Healthy invariant:** Zero in healthy operation.

- **`normalized.pcscf.core:rcv_requests_invite_per_ue`** (SIP INVITE rate per UE at P-CSCF) — current **0.02 requests_per_second** vs learned baseline **0.00 requests_per_second** (MEDIUM, spike)
    - **What it measures:** Call attempt rate from registered UEs. Unlike REGISTER (periodic),
INVITEs only fire when UEs place calls. Zero is normal during
quiet periods; nonzero INVITE with zero dialogs is the signature
of call setup failure.
    - **Spike means:** Fewer call attempts.
    - **Healthy typical range:** 0–0.2 requests_per_second
    - **Healthy invariant:** Per-UE rate.

- **`normalized.upf.gtp_indatapktn3upf_per_ue`** (GTP-U uplink rate per UE (N3)) — current **0.11 packets_per_second** vs learned baseline **1.45 packets_per_second** (LOW, drop)
    - **What it measures:** Health of the uplink user-plane path gNB → UPF. Drops to near-zero
during RAN or N3 outage; stays nonzero during active calls or data
sessions. Decoupled from SIP signaling (signals data plane, not
control plane).
    - **Drop means:** Data plane dead on uplink — UPF receiving no packets from gNB.
    - **Healthy typical range:** 0–10 packets_per_second
    - **Healthy invariant:** Per-UE rate. Constant regardless of UE pool size. Rises during
active VoNR calls (~100 pps for G.711 voice) and data transfer.
Uplink and downlink (gtp_outdatapktn3upf_per_ue) are independent
directions whose ratio is determined entirely by the current
traffic profile — voice with NULL_AUDIO (this testbed),
signaling-only chatter, idle UEs, and asymmetric data sessions
all produce persistent in/out imbalance under healthy operation.
Asymmetry between uplink and downlink rates is NEVER, by itself,
evidence of packet loss — not at any magnitude, not under any
traffic mix. To detect actual loss, use the methods listed in
stack rule `upf_counters_are_directional` (same-direction rate
comparison, RTCP loss_ratio at RTPEngine, or tc qdisc drop
counters). Rate-based metrics like this one are usually MORE
informative than the underlying lifetime cumulative counter for
current-state failure detection.

- **`normalized.upf.gtp_outdatapktn3upf_per_ue`** (GTP-U downlink rate per UE (N3)) — current **0.13 packets_per_second** vs learned baseline **1.45 packets_per_second** (LOW, drop)
    - **What it measures:** Health of the downlink user-plane path UPF → gNB. Drops to
near-zero during a downlink-affecting RAN, N3, or UPF
outage; stays nonzero during active calls or data sessions.
Decoupled from SIP signaling. The cross-direction
relationship to uplink (gtp_indatapktn3upf_per_ue) reflects
current traffic profile, NOT data-plane health — voice
with NULL_AUDIO, signaling-only chatter, idle UEs, and
asymmetric data sessions all produce persistent in/out
imbalance under healthy operation. Use this metric for
same-direction collapse detection (a known-active downlink
collapsing toward zero); do NOT infer loss from
uplink-vs-downlink asymmetry — see stack rule
upf_counters_are_directional.
    - **Drop means:** No traffic leaving UPF toward RAN.
    - **Healthy typical range:** 0–10 packets_per_second
    - **Healthy invariant:** Per-UE rate. Constant regardless of UE pool size. Uplink
(gtp_indatapktn3upf_per_ue) and downlink are independent
directions whose ratio is determined entirely by the current
traffic profile — voice with NULL_AUDIO (this testbed),
signaling-only chatter, idle UEs, and asymmetric data
sessions all produce persistent in/out imbalance under
healthy operation. Asymmetry between uplink and downlink
rates is NEVER, by itself, evidence of packet loss — not
at any magnitude, not under any traffic mix. To detect
actual loss, use the methods listed in stack rule
`upf_counters_are_directional` (same-direction rate
comparison, RTCP loss_ratio at RTPEngine, or tc qdisc drop
counters). Rate-based metrics like this one are usually
MORE informative than the underlying lifetime cumulative
counter for current-state failure detection.

- **`normalized.icscf.cdp_replies_per_ue`** (I-CSCF Diameter reply rate per UE) — current **0.03 replies_per_second_per_ue** vs learned baseline **0.03 replies_per_second_per_ue** (LOW, shift)
    - **What it measures:** Liveness of the I-CSCF↔HSS Cx path. Drops to 0 when HSS is unreachable OR when no signaling is occurring at the I-CSCF (idle or upstream P-CSCF partitioned).
    - **Shift means:** I-CSCF is actively conversing with HSS — healthy.
    - **Healthy typical range:** 0–1 replies_per_second_per_ue

- **`normalized.icscf.core:rcv_requests_register_per_ue`** (SIP REGISTER rate per UE at I-CSCF) — current **0.06 requests_per_second** vs learned baseline **0.06 requests_per_second** (LOW, shift)
    - **What it measures:** Health of the P-CSCF → I-CSCF forwarding path (Mw interface). When
this drops to zero while P-CSCF REGISTER rate is still non-zero,
it's the SIGNATURE of an IMS partition between P-CSCF and I-CSCF.
    - **Shift means:** Forwarding issue on the Mw interface, or P-CSCF stopped forwarding.
    - **Healthy typical range:** 0–0.5 requests_per_second
    - **Healthy invariant:** Per-UE rate. Should closely track ims.pcscf.rcv_requests_register_per_ue.

- **`normalized.pcscf.core:rcv_requests_register_per_ue`** (SIP REGISTER rate per UE at P-CSCF) — current **0.06 requests_per_second** vs learned baseline **0.06 requests_per_second** (LOW, shift)
    - **What it measures:** How actively UEs are refreshing their IMS registrations with the
P-CSCF. REGISTERs arrive periodically (re-registration timer) plus
at attach. Sustained zero means UEs cannot reach P-CSCF OR the
UE-to-network SIP path is broken.
    - **Shift means:** Fewer REGISTERs than expected — UE connectivity or P-CSCF reachability issue.
    - **Healthy typical range:** 0–0.5 requests_per_second
    - **Healthy invariant:** Per-UE rate — same value at any deployment scale.

- **`normalized.scscf.cdp_replies_per_ue`** (S-CSCF CDP Diameter replies per UE) — current **0.06 replies_per_second_per_ue** vs learned baseline **0.06 replies_per_second_per_ue** (LOW, shift)
    - **What it measures:** Active S-CSCF Diameter traffic with HSS. Near-zero when registrations idle OR HSS partition.
    - **Shift means:** Diameter peering loss with HSS.
    - **Healthy typical range:** 0–1 replies_per_second_per_ue
    - **Healthy invariant:** Per-UE rate; varies with registration/auth load.

- **`normalized.scscf.core:rcv_requests_register_per_ue`** (SIP REGISTER rate per UE at S-CSCF) — current **0.06 requests_per_second** vs learned baseline **0.06 requests_per_second** (LOW, shift)
    - **What it measures:** Health of the I-CSCF → S-CSCF forwarding path. Drop to zero while
I-CSCF is receiving REGISTERs = S-CSCF-side issue (crashed, or
I-CSCF → S-CSCF path broken).
    - **Shift means:** I-CSCF not forwarding or S-CSCF not receiving.
    - **Healthy typical range:** 0–0.5 requests_per_second
    - **Healthy invariant:** Per-UE rate. Tracks icscf.register rate.

- **`normalized.smf.bearers_per_ue`** (Active QoS bearers per UE) — current **2.00 count** vs learned baseline **2.48 count** (LOW, shift)
    - **What it measures:** Per-UE count of active QoS bearers. Baseline reflects default
bearers; increments during VoNR calls indicate dedicated voice
bearers being set up. Drop during an active call = dedicated
bearer torn down unexpectedly (voice will fail).
    - **Shift means:** Expected during VoNR calls (1 extra bearer per active call).
    - **Healthy typical range:** 2–3.5 count
    - **Healthy invariant:** At rest: equals configured default bearers (typically 2 per UE).
During active VoNR call: +1 per caller. The per-UE ratio is the
invariant; absolute count scales with UE pool.


## Symptom Classifier (Phase 0.5)

**Label:** `mixed`  
**Flag counts:** transport=2, application=2, ambiguous=6

### Transport-bucket flags (2)

| Metric | Direction | Score | KB-driven reason |
|---|---|---:|---|
| `normalized.upf.gtp_indatapktn3upf_per_ue` | drop | 3.50 | KB-labeled transport: core.upf.gtp_indatapktn3upf_per_ue (drop, score=3.50) |
| `normalized.upf.gtp_outdatapktn3upf_per_ue` | drop | 2.99 | KB-labeled transport: core.upf.gtp_outdatapktn3upf_per_ue (drop, score=2.99) |

### Application-bucket flags (2)

| Metric | Direction | Score | KB-driven reason |
|---|---|---:|---|
| `derived.pcscf_sip_error_ratio` | spike | 4.59 | KB-labeled application: ims.pcscf.sip_error_ratio (spike, score=4.59) |
| `normalized.smf.bearers_per_ue` | shift | 0.01 | KB-labeled application: core.smf.bearers_per_ue (shift, score=0.01) |

### Ambiguous-bucket flags (6)

| Metric | Direction | Score | KB-driven reason |
|---|---|---:|---|
| `normalized.pcscf.core:rcv_requests_invite_per_ue` | spike | 4.59 | KB-labeled mixed: ims.pcscf.rcv_requests_invite_per_ue (spike, score=4.59) |
| `normalized.icscf.cdp_replies_per_ue` | shift | 2.52 | KB-labeled mixed: ims.icscf.cdp_replies_per_ue (shift, score=2.52) |
| `normalized.icscf.core:rcv_requests_register_per_ue` | shift | 2.52 | KB-labeled mixed: ims.icscf.rcv_requests_register_per_ue (shift, score=2.52) |
| `normalized.pcscf.core:rcv_requests_register_per_ue` | shift | 2.52 | KB-labeled mixed: ims.pcscf.rcv_requests_register_per_ue (shift, score=2.52) |
| `normalized.scscf.cdp_replies_per_ue` | shift | 2.52 | KB-labeled mixed: ims.scscf.cdp_replies_per_ue (shift, score=2.52) |
| `normalized.scscf.core:rcv_requests_register_per_ue` | shift | 2.52 | KB-labeled mixed: ims.scscf.rcv_requests_register_per_ue (shift, score=2.52) |

**Rationale:**

```
label=mixed. Both transport-layer (2) and application-layer (2) signals are load-bearing. Path walk runs first; falls through to the application-layer pipeline if the walk produces null localization.

Transport signals: normalized.upf.gtp_indatapktn3upf_per_ue (drop, score=3.50) — KB-labeled transport: core.upf.gtp_indatapktn3upf_per_ue (drop, score=3.50); normalized.upf.gtp_outdatapktn3upf_per_ue (drop, score=2.99) — KB-labeled transport: core.upf.gtp_outdatapktn3upf_per_ue (drop, score=2.99)

Application signals: derived.pcscf_sip_error_ratio (spike, score=4.59) — KB-labeled application: ims.pcscf.sip_error_ratio (spike, score=4.59); normalized.smf.bearers_per_ue (shift, score=0.01) — KB-labeled application: core.smf.bearers_per_ue (shift, score=0.01)

Ambiguous signals: normalized.pcscf.core:rcv_requests_invite_per_ue (spike, score=4.59) — KB-labeled mixed: ims.pcscf.rcv_requests_invite_per_ue (spike, score=4.59); normalized.icscf.cdp_replies_per_ue (shift, score=2.52) — KB-labeled mixed: ims.icscf.cdp_replies_per_ue (shift, score=2.52); normalized.icscf.core:rcv_requests_register_per_ue (shift, score=2.52) — KB-labeled mixed: ims.icscf.rcv_requests_register_per_ue (shift, score=2.52); normalized.pcscf.core:rcv_requests_register_per_ue (shift, score=2.52) — KB-labeled mixed: ims.pcscf.rcv_requests_register_per_ue (shift, score=2.52); normalized.scscf.cdp_replies_per_ue (shift, score=2.52) — KB-labeled mixed: ims.scscf.cdp_replies_per_ue (shift, score=2.52) [+1 more]
```

## Transport-Layer Route (Phase 0.6)

### Prioritizer

**Flow:** `data_pdu_session_user_traffic` (Data PDU Session — User Traffic)  
**Direction:** both  
**Hop count:** 11

**Candidates considered:**

| Flow | Score |
|---|---:|
| `data_pdu_session_user_traffic` ← chosen | 12 |
| `vonr_media` | 12 |
| `ims_registration` | 8 |
| `vonr_call_teardown` | 8 |
| `vonr_call_setup` | 8 |

**Rationale:**

```
Primary flow `data_pdu_session_user_traffic` (score=12, 11 hops); walker probes 5 candidate flows in parallel. Load-bearing components: ['icscf', 'pcscf', 'scscf', 'smf', 'upf']. Other candidate flows considered: vonr_media=12, ims_registration=8, vonr_call_teardown=8, vonr_call_setup=8. Hard cap (5 flows) truncated 3 additional candidates below the cut. Soft cap exceeded (5 > 3): noisy load-bearing set — inspect screener flag bucketing if this recurs.
```

### Walker

**Status:** ⚠️ **null localization**
**Window:** 5s  
**Walked flow:** `data_pdu_session_user_traffic`

**Per-hop results:**

| # | Node | Kind | Iface | Attribution | Detail |
|---:|---|---|---|---|---|
| 0 | `e2e_ue1` | container | `eth0` | `clean` | _clean_ |
| 1 | `docker_open5gs_default` | docker_bridge | `br-docker_open5gs` | `clean` | _clean_ |
| 2 | `nr_gnb` | container | `eth0` | `clean` | _clean_ |
| 3 | `docker_open5gs_default` | docker_bridge | `br-docker_open5gs` | `clean` | _clean_ |
| 4 | `upf` | container | `eth0` | `clean` | _clean_ |
| 5 | `internet` | external_network | `eth0` | `inconclusive` | _no_prober_registered_: "no HopProber registered for kind='external_network'; registered kinds: ['contai |
| 6 | `upf` | container | `eth0` | `clean` | _clean_ |
| 7 | `docker_open5gs_default` | docker_bridge | `br-docker_open5gs` | `clean` | _clean_ |
| 8 | `nr_gnb` | container | `eth0` | `clean` | _clean_ |
| 9 | `docker_open5gs_default` | docker_bridge | `br-docker_open5gs` | `clean` | _clean_ |
| 10 | `e2e_ue1` | container | `eth0` | `clean` | _clean_ |

### Prioritized Candidates Walked In Parallel

The walker probed 5 candidate flows in parallel. All walked flows are listed below (primary marked); the primary's full per-hop walk is in the Walker section above. Listing every walked flow makes the deterministic disambiguation fully auditable.

| Flow | Walker outcome | First attributed hop |
|---|---|---|
| `data_pdu_session_user_traffic` ← primary | ⚠️ null | — |
| `vonr_media` | ⚠️ null | — |
| `ims_registration` | ⚠️ null | — |
| `vonr_call_teardown` | ⚠️ null | — |
| `vonr_call_setup` | ⚠️ null | — |

> ⚠️ **Soft cap exceeded** — walker probed 5 flows (soft cap = 3). Signals a noisy load-bearing set; inspect screener flag bucketing if this recurs.

> 🔪 **Hard cap (5 flows) truncated 3 additional candidate(s)** below the cut: `diameter_cx_authentication` (4), `pdu_session_establishment` (4), `ue_deregistration` (4). These scored above zero but ranked beyond the top 5 by the prioritizer; not walked.

### Localized Synthesis

*Walker found no hop with attribution. Phase 0.6 returned None and the orchestrator fell through to the application-layer pipeline (Phases 1-7) below — the diagnosis you see in `Agent Diagnosis` came from that fallback path, not from Phase 0.6.*

## Event Aggregation (Phase 1)

No events fired during this episode. Either no metric KB triggers matched, or the episode encountered no meaningful state transitions.

## Correlation Analysis (Phase 2)

No events fired — correlation engine had nothing to work with.

## RAG & Operational Lessons (Phase 2.5)

### RAG retrieval — prior similar episodes

**Status:** `hits` — hits=5, top_sim=0.94, top_case=v6/ep_20260501_212815_mongodb_gone
**Index:** `/home/ehoskas/agentic-network-ops/rag_index`  **Corpus size:** 102 cases
**Classifier label used in retrieval:** `mixed`
**Retrieval params:** k=5, min_similarity=0.4
**Hits (5):**

| Rank | Sim | Case ID | Scenario | Ground truth | Primary suspect | Score |
|---:|---:|---|---|---|---|---:|
| 0 | 94% | `v6/ep_20260501_212815_mongodb_gone` | MongoDB Gone | `mongo` | `mongo` | 100% |
| 1 | 93% | `v6/ep_20260430_013408_p_cscf_latency` | P-CSCF Latency | `pcscf` | `pcscf` | 100% |
| 2 | 89% | `v6/ep_20260429_160912_s_cscf_crash` | S-CSCF Crash | `scscf` | `scscf` | 100% |
| 3 | 85% | `v7/ep_20260510_184035_p_cscf_packet_loss` | P-CSCF Packet Loss | `pcscf` | `pcscf` | 100% |
| 4 | 85% | `v6/ep_20260430_020340_mongodb_gone` | MongoDB Gone | `mongo` | `mongo` | 95% |

*Rendered block injected into the NA prompt's `{prior_similar_episodes}` placeholder: 4563 chars. Source paths are inlined in the block so the EvidenceValidator can audit any case the NA cites.*

### Operational lessons — hand-authored rules corpus

**Status:** `injected` — lessons=16, chars=15678
**Path:** `/home/ehoskas/agentic-network-ops/agentic_ops_common/rag/lessons.yaml`  **Count:** 16  **Block size:** 15678 chars
**Injected lesson IDs:** `L01`, `L02`, `L03`, `L04`, `L05`, `L06`, `L07`, `L08`, `L09`, `L10`, `L11`, `L12`, `L13`, `L14`, `L15`, `L16`

### NA citations of the injected content

**Cited lesson IDs:**
- `L10`

*Citations are verbatim string matches — case_ids and lesson_ids appear in the NA's emitted text (summary, hypothesis statements, or layer notes). The EvidenceValidator can audit any cited case by following its `source_episode_path` from the retrieval table above.*

## Network Analysis (Phase 3)

**Summary:** The mongo container has exited (L10), entirely blinding the UDR/PCF subscriber datastore and causing the P-CSCF to reject all calls due to N5 authorization failures; pcscf, upf, icscf, scscf, and smf are demoted as secondary downstream observers of this signaling cascade.

**Ranked hypotheses:**

- **`h1`** (fit=0.99, nf=mongo, specificity=specific):
    - **Statement:** mongo is the source of the P-CSCF SIP error ratio spike, the call setup failures, and the subsequent drops in UPF GTP-U traffic rates (L10).
    - **Falsification probes:**
        - Call get_network_status() to verify if the mongo container is in an exited state.
        - Check PCF container logs to confirm it is returning non-201 errors to P-CSCF on N5 due to datastore unavailability.
- **`h2`** (fit=0.30, nf=pcf, specificity=moderate):
    - **Statement:** pcf is the source of the P-CSCF SIP error ratio spike, the call setup failures, and the subsequent drops in UPF GTP-U traffic rates.
    - **Falsification probes:**
        - Check PCF metrics via get_nf_metrics() for policy association request counts to see if it is dropping requests internally.
        - Check get_network_status() to verify if the pcf container is healthy and running.
- **`h3`** (fit=0.10, nf=pcscf, specificity=moderate):
    - **Statement:** pcscf is the source of the P-CSCF SIP error ratio spike, the call setup failures, and the subsequent drops in UPF GTP-U traffic rates.
    - **Falsification probes:**
        - Check P-CSCF logs for internal processing errors when handling incoming SIP requests.
        - Measure RTT between P-CSCF and PCF (via SCP) to rule out a network partition on the N5 link.


## Falsification Plans (Phase 4)

**3 falsification plan(s) — one per hypothesis:**

### Plan for `h1` (target: `mongo`)

**Hypothesis:** mongo is the source of the P-CSCF SIP error ratio spike, the call setup failures, and the subsequent drops in UPF GTP-U traffic rates (L10).

**Probes (3):**
1. **`get_network_status`** — Check the running status of the mongo container.
    - *Expected if hypothesis holds:* The mongo container status is reported as exited, down, or unhealthy.
    - *Falsifying observation:* The mongo container is reported as running and healthy.
2. **`check_process_listeners`** — Check listening ports on the mongo container.
    - *Expected if hypothesis holds:* The tool returns no active listeners for the standard mongo database port (e.g., 27017) or fails to execute because the container is down.
    - *Falsifying observation:* The tool returns an active TCP listener on the expected database port.
3. **`get_diagnostic_metrics`** — Fetch PCF or UDR metrics to look for datastore connection error counters.
    - *Expected if hypothesis holds:* PCF or UDR metrics show elevated datastore connection error counters or timeouts.
    - *Falsifying observation:* PCF and UDR datastore connection error metrics are at normal baseline levels or zero.

*Notes:* Directly testing the L10 exited state claim for mongo. PCF/UDR metrics probe is included to verify the upstream impact of a blinded datastore.

### Plan for `h2` (target: `pcf`)

**Hypothesis:** pcf is the source of the P-CSCF SIP error ratio spike, the call setup failures, and the subsequent drops in UPF GTP-U traffic rates.

**Probes (3):**
1. **`get_network_status`** — Check the running status of the pcf container.
    - *Expected if hypothesis holds:* The pcf container status is reported as down, exited, or unhealthy.
    - *Falsifying observation:* The pcf container is reported as running and healthy.
2. **`get_diagnostic_metrics`** — Check PCF metrics for policy association request counts and error rates.
    - *Expected if hypothesis holds:* PCF metrics show incoming N5 requests from P-CSCF alongside a high rate of 5xx error responses or zero outbound responses.
    - *Falsifying observation:* PCF metrics show no 5xx errors and a 1:1 ratio of successful responses to incoming requests.
3. **`get_diagnostic_metrics`** — Check pcscf metrics for outbound N5/Rx policy requests to PCF (activity-vs-drops discriminator).
    - *Expected if hypothesis holds:* pcscf metrics show N5/Rx requests are actively being sent to pcf, but are timing out or receiving error codes.
    - *Falsifying observation:* pcscf metrics show no outbound N5 requests are being sent, or all sent requests receive immediate successful responses.

*Notes:* Includes the required activity-vs-drops discriminator checking the upstream (pcscf) N5 outbound rates to verify requests are actually reaching the PCF.

### Plan for `h3` (target: `pcscf`)

**Hypothesis:** pcscf is the source of the P-CSCF SIP error ratio spike, the call setup failures, and the subsequent drops in UPF GTP-U traffic rates.

**Probes (3):**
1. **`run_kamcmd`** — run_kamcmd('pcscf', 'dlg.list') to show active dialogs.
    - *Expected if hypothesis holds:* The tool returns a drop in active dialogs or zero active calls, particularly when prior state had calls and INVITE rates are non-zero.
    - *Falsifying observation:* The tool returns a typical value of active dialogs (e.g., ~1 per active VoUE), meaning calls are successfully maintaining state here.
2. **`measure_rtt`** — measure_rtt('pcscf', icscf_ip) to test the transport layer between P-CSCF and I-CSCF.
    - *Expected if hypothesis holds:* The probe shows 0% loss and low latency.
    - *Falsifying observation:* The probe shows high packet loss or unreachability.
3. **`get_diagnostic_metrics`** — Check ims.icscf.rcv_requests_register_per_ue to compare with pcscf forwarding rate.
    - *Expected if hypothesis holds:* The metric reads zero or dropping, consistent with requests not arriving at the I-CSCF despite the healthy network path.
    - *Falsifying observation:* The metric shows normal receive rates matching the pcscf inbound rates, confirming requests are successfully crossing the interface.

*Notes:* Addresses the compositional RTT probe by explicitly listing conflates_with elements (the path and the destination I-CSCF node) and provides a partner probe (I-CSCF metrics) to disambiguate.


## Parallel Investigators (Phase 5)

**3 sub-Investigator verdict(s):** **1 NOT_DISPROVEN**, **2 DISPROVEN**

### `h1` — ✅ **NOT_DISPROVEN**

**Hypothesis:** mongo is the source of the P-CSCF SIP error ratio spike, the call setup failures, and the subsequent drops in UPF GTP-U traffic rates (L10).

**Reasoning:** [Multi-shot consensus — both shots returned NOT_DISPROVEN.]

Shot 1: The primary mechanism of the hypothesis (L10: mongo is exited) is directly confirmed by get_network_status, which reports mongo in the 'down_or_absent' list with an 'exited' status. The check_process_listeners probe could not run due to missing binaries, and the diagnostic metrics tool did not surface datastore connection error metrics for PCF/UDR (a weak negative). However, the definitive exited state of the mongo container provides strong positive evidence that the datastore is down, supporting the hypothesis.

Shot 2: The get_network_status probe directly confirmed that the mongo container is in an 'exited' state, completely validating the L10 mechanism of the hypothesis. The process listener check was unavailable due to missing container binaries, and PCF/UDR datastore error metrics were not exported in the snapshot. However, the confirmed exited status of the mongo container is a strong positive that validates the hypothesis.

**Probes executed (3):**
- **Check the running status of the mongo container.** ✓ CONSISTENT
    - *Tool:* `get_network_status()`
    - *Observation:* [EVIDENCE: get_network_status() -> "down_or_absent": [ "mongo" ] ... "mongo": "exited"]
    - *Comment:* The mongo container is explicitly confirmed to be in an exited state, directly validating the core mechanism of the hypothesis.
- **Check listening ports on the mongo container.** ~ AMBIGUOUS
    - *Tool:* `check_process_listeners("mongo")`
    - *Observation:* [EVIDENCE: check_process_listeners("mongo") -> "PROBE_TOOL_UNAVAILABLE: check_process_listeners cannot run on container `mongo`"]
    - *Comment:* The container lacks the required tools and is already exited, so listener inspection could not produce a signal.
- **Fetch PCF or UDR metrics to look for datastore connection error counters.** ~ AMBIGUOUS
    - *Tool:* `get_diagnostic_metrics(at_time_ts=1779915520.80044, nfs=["pcf", "udr"])`
    - *Observation:* [EVIDENCE: get_diagnostic_metrics(1779915520.80044, ["pcf", "udr"]) -> "PCF (core layer): -- Model features -- (no model features for this NF)"]
    - *Comment:* No datastore connection error metrics are exposed in the diagnostic snapshot for PCF or UDR. This is a weak negative that does not override the strong positive of mongo being exited.

### `h2` — ❌ **DISPROVEN**

**Hypothesis:** pcf is the source of the P-CSCF SIP error ratio spike, the call setup failures, and the subsequent drops in UPF GTP-U traffic rates.

**Reasoning:** [Multi-shot consensus — both shots returned DISPROVEN.]

Shot 1: The pcf container is running normally, contradicting the hypothesis that it crashed or is the root source of the failure. Crucially, the get_network_status probe revealed that the mongo container (the backend datastore for UDR/PCF) has exited. This means the fault originates at mongo, not pcf, causing downstream PCF/N5 authorization failures.

Shot 2: The hypothesis claims that the PCF is the source of the failure. However, evidence shows the PCF container is running normally while the mongo container (which provides the UDR datastore for the PCF) has exited. The P-CSCF is experiencing N5 authorization failures (httpclient:connok = 0) because the PCF cannot fetch subscriber policies without its datastore, making mongo the true source of the cascade rather than an internal fault at the PCF.

**Probes executed (3):**
- **Check the running status of the pcf container.** ✗ CONTRADICTS
    - *Tool:* `get_network_status()`
    - *Observation:* [EVIDENCE: get_network_status() -> "\"pcf\": \"running\""] and [EVIDENCE: get_network_status() -> "\"mongo\": \"exited\""]
    - *Comment:* The pcf container is reported as running, refuting the expectation that it would be down if it were the source. Furthermore, mongo is exited, pinpointing the actual root cause.
- **Check PCF metrics for policy association request counts and error rates.** ✗ CONTRADICTS
    - *Tool:* `get_diagnostic_metrics(at_time_ts=1779915520.8004446, nfs=["pcf"])`
    - *Observation:* [EVIDENCE: get_diagnostic_metrics("pcf") -> "fivegs_pcffunction_pa_policyamassoreq = 8 [counter]"] and [EVIDENCE: get_diagnostic_metrics("pcf") -> "fivegs_pcffunction_pa_policyamassosucc = 8 [counter]"]
    - *Comment:* PCF metrics show an equal number of policy association requests and successes (8 each), with no internal 5xx error metrics reported, contradicting the expectation of PCF dropping requests internally.
- **Check pcscf metrics for outbound N5/Rx policy requests to PCF (activity-vs-drops discriminator).** ~ AMBIGUOUS
    - *Tool:* `get_diagnostic_metrics(at_time_ts=1779915520.8004446, nfs=["pcscf"])`
    - *Observation:* [EVIDENCE: get_diagnostic_metrics("pcscf") -> "httpclient:connfail = 50 [counter]"] and [EVIDENCE: get_diagnostic_metrics("pcscf") -> "httpclient:connok = 0 [counter]"]
    - *Comment:* P-CSCF metrics do not show successful HTTP client connections, and the connection failures (50) are well below the pre-existing baseline threshold (1300-1500), offering no clear signal of an active spike in N5 request drops at PCF.

**Alternative suspects:** mongo

### `h3` — ❌ **DISPROVEN**

**Hypothesis:** pcscf is the source of the P-CSCF SIP error ratio spike, the call setup failures, and the subsequent drops in UPF GTP-U traffic rates.

**Reasoning:** [Multi-shot consensus — both shots returned DISPROVEN.]

Shot 1: P-CSCF is not the root source of the failure. Metrics confirm P-CSCF is perfectly capable of forwarding SIP traffic when authorized (REGISTER receive rates at P-CSCF and I-CSCF match exactly at 0.0646) over a healthy network path (0% loss to I-CSCF). The fact that P-CSCF is receiving INVITEs but I-CSCF sees none indicates P-CSCF is deliberately rejecting call setups locally (SIP error ratio spikes to 0.1667) due to a downstream N5 authorization failure, as get_network_status confirms the mongo container has completely exited.

Shot 2: The hypothesis is disproven because P-CSCF is successfully forwarding REGISTER requests to I-CSCF at precisely matching rates, which contradicts the hypothesis that P-CSCF is internally failing. Instead, a network status check reveals that the mongo container has exited. This datastore outage blinds the PCF, resulting in N5 authorization failures that P-CSCF correctly rejects INVITEs for; thus, the fault originates at mongo rather than P-CSCF.

**Probes executed (4):**
- **run_kamcmd('pcscf', 'dlg.list') to show active dialogs.** ~ AMBIGUOUS
    - *Tool:* `run_kamcmd("pcscf", "dlg.list")`
    - *Observation:* [EVIDENCE: run_kamcmd("pcscf", "dlg.list") -> "error: 500 - command dlg.list not found"]
    - *Comment:* The command was not found in this container, yielding no evidence.
- **measure_rtt('pcscf', icscf_ip) to test the transport layer between P-CSCF and I-CSCF.** ✓ CONSISTENT
    - *Tool:* `measure_rtt("pcscf", "icscf")`
    - *Observation:* [EVIDENCE: measure_rtt("pcscf", "icscf") -> "66 packets transmitted, 66 received, 0% packet loss"]
    - *Comment:* The transport layer between P-CSCF and I-CSCF is perfectly healthy. While consistent with the plan's expected lack of network issues, it sets up the contradiction found in the next probe regarding P-CSCF's internal health.
- **Check ims.icscf.rcv_requests_register_per_ue to compare with pcscf forwarding rate.** ✗ CONTRADICTS
    - *Tool:* `get_diagnostic_metrics(at_time_ts=1779915520.80044, nfs=["icscf", "pcscf"])`
    - *Observation:* [EVIDENCE: get_diagnostic_metrics(at_time_ts=1779915520.80044, nfs=["icscf", "pcscf"]) -> "normalized.icscf.core:rcv_requests_register_per_ue = 0.0646" and "normalized.pcscf.core:rcv_requests_register_per_ue = 0.0646"]
    - *Comment:* REGISTER requests are successfully arriving at I-CSCF at the exact same rate they reach P-CSCF. This refutes the idea that P-CSCF is internally broken, partitioned, or broadly failing to process SIP requests.
- **Verify container status to identify the true root cause upstream of P-CSCF.** ✗ CONTRADICTS
    - *Tool:* `get_network_status()`
    - *Observation:* [EVIDENCE: get_network_status() -> "mongo": "exited"]
    - *Comment:* The mongo container has exited. This identifies the real source of the failure: the datastore outage causes PCF to fail N5 policy authorization, forcing P-CSCF to act as a secondary observer and reject the calls locally.

**Alternative suspects:** mongo, pcf


## Evidence Validation (Phase 6)

**Overall verdict:** clean
**Overall confidence:** high

**Per sub-Investigator:**

| Agent | Tool Calls | Citations | Verdict | Confidence |
|---|---|---|---|---|
| `InvestigatorAgent_h1` | 10 | 3/3 | clean | high |
| `InvestigatorAgent_h2` | 10 | 6/6 | clean | high |
| `InvestigatorAgent_h3` | 8 | 4/4 | clean | high |


## Ground Truth

**Failure domain:** data_layer  
**Protocol impact:** multiple  
**Affected components:** mongo  
**Severity:** degraded

## Agent Diagnosis

**Model:** v7-adk/pro=gemini-3.1-pro-preview+flash=gemini-3.5-flash  
**Time to diagnosis:** 353.3s

### Prompt to RCA Agent

```
The 5G SA + IMS stack is experiencing issues. Investigate and diagnose the root cause.
```

**Diagnosis:**

> ### causes
> - **summary**: The mongo datastore container has exited, blinding the PCF and UDR, which causes the P-CSCF to reject all incoming SIP calls due to N5 authorization failures.
> - **timeline**:
>     1. P-CSCF SIP error ratio spikes and UPF GTP-U traffic rates drop significantly.
>     2. Initial triage points to a cascade where P-CSCF fails to setup calls due to N5 authorization errors.
>     3. Probes against the network confirm P-CSCF is healthy and successfully forwarding REGISTER traffic to I-CSCF.
>     4. Further investigation reveals that the mongo container, which serves as the datastore for UDR and PCF, has entered an 'exited' state.
> - **root_cause**: The mongo database container has unexpectedly exited, causing a total datastore outage for the 5G policy and subscriber functions. (primary_suspect_nf: `mongo`)
> - **affected_components**:
>     - `mongo`: Root Cause
>     - `pcf`: Secondary
>     - `udr`: Secondary
>     - `pcscf`: Symptomatic
> - **recommendation**: Verify the running status of the mongo container using `docker ps -a | grep mongo` and inspect its crash logs with `docker logs mongo` to identify why the process terminated.
> - **confidence**: low
> - **verdict_kind**: confirmed
> - **explanation**: The mongo datastore, which backs the UDR and PCF for subscriber profiles and policy data, has completely exited. Without an available database, the PCF is unable to process and authorize N5 policy requests from the IMS core. Consequently, the P-CSCF experiences authorization failures on the N5 interface and is forced to reject incoming SIP INVITEs locally, resulting in the observed SIP error ratio spike. Investigative probes confirmed both P-CSCF and PCF containers are running natively, proving the outage is exclusively driven by the mongo container's failure.
> 
> [Confidence cap applied: emitted 'high' downgraded to 'low' because evidence-strength is WEAK (verdict h1: 1/2 CONSISTENT, 0 CONTRADICTS, 1 AMBIGUOUS (+1 excluded as tool_unavailable/error)). The confidence claim was deterministically corrected to match the underlying probe evidence; the diagnosed NF stands.]

### Scoring Breakdown

**Overall score: 90%**

**Scorer assessment:** The agent accurately diagnosed the MongoDB outage as the root cause of the network failure, though its confidence was unnecessarily capped at low.

| Dimension | Weight | Result | Rationale |
|-----------|-------:|--------|-----------|
| Root cause correct | 0.40 | Yes | The agent correctly identified that the mongo database container had exited, causing a total datastore outage for the UDR and PCF, which matches the simulated failure of MongoDB being killed. |
| Component overlap | 0.25 | 100% | Mechanical comparison: ground truth ['mongo'] vs diagnosis root cause(s) ['mongo']. mongo=Root Cause (1.0). |
| Severity correct | 0.15 | Yes | The agent correctly identified the severity as a total outage, noting that the mongo container had exited and was completely unavailable. |
| Fault type identified | 0.10 | Yes | The agent correctly identified the fault type as a component being down/unreachable, specifically noting that the mongo container had entered an 'exited' state. |
| Confidence calibrated | 0.10 | No | The agent's diagnosis was highly accurate and detailed, but its confidence was downgraded to 'low' due to programmatic evidence-strength constraints, making it under-confident. |

**Ranking position:** #1 — The agent listed 'mongo' as the primary root cause in its diagnosis.


### Token Usage

| Metric | Count |
|--------|-------|
| Input tokens | 618,611 |
| Output tokens | 10,219 |
| Thinking tokens | 38,798 |
| **Total tokens** | **667,628** |

**Per-phase breakdown:**

| Phase | Tokens | Tool Calls | LLM Calls |
|-------|--------|------------|-----------|
| AnomalyScreener | 0 | 0 | 0 |
| SymptomClassifier | 0 | 0 | 0 |
| PathPrioritizer | 0 | 0 | 0 |
| PathWalkInvestigator | 0 | 0 | 0 |
| EventAggregator | 0 | 0 | 0 |
| CorrelationAnalyzer | 0 | 0 | 0 |
| RAGRetriever | 0 | 0 | 0 |
| OperationalLessons | 0 | 0 | 0 |
| NetworkAnalystAgent | 59,443 | 6 | 2 |
| Phase 3 NetworkAnalyst__guardrail | 0 | 0 | 0 |
| InstructionGeneratorAgent | 71,977 | 4 | 5 |
| Phase 4 InstructionGenerator__guardrail | 0 | 0 | 0 |
| InstructionGeneratorAgent | 12,994 | 0 | 1 |
| Phase 4 InstructionGenerator__guardrail | 0 | 0 | 0 |
| Phase 4 InstructionGenerator__guardrail | 0 | 0 | 0 |
| InvestigatorAgent_h1 | 59,264 | 4 | 3 |
| InvestigatorAgent_h1 | 130,427 | 6 | 6 |
| InvestigatorAgent_h1__reconciliation | 0 | 0 | 0 |
| InvestigatorAgent_h2 | 34,327 | 3 | 2 |
| InvestigatorAgent_h2 | 156,130 | 7 | 8 |
| InvestigatorAgent_h2__reconciliation | 0 | 0 | 0 |
| InvestigatorAgent_h3 | 65,484 | 4 | 3 |
| InvestigatorAgent_h3 | 63,975 | 4 | 3 |
| InvestigatorAgent_h3__reconciliation | 0 | 0 | 0 |
| Phase5FanOutAudit | 0 | 0 | 0 |
| Phase6.5CandidatePool | 0 | 0 | 0 |
| EvidenceValidator | 0 | 0 | 0 |
| SynthesisAgent | 13,607 | 0 | 1 |
| Phase 7 Synthesis__guardrail | 0 | 0 | 0 |
| ImpactAssessment | 0 | 0 | 0 |


## Blast Radius & Downstream Impact

An outage originating from the mongo network function has impacted the icscf, pcscf, scscf, smf, and upf network functions. As a result, the IMS Registration, VoNR Call Setup, and VoNR Call Teardown procedures are failing, while the VoNR Media Path procedure is degraded.

The VoNR voice calls service is currently failing.

### Affected Services

- **VoNR voice calls** — 🔴 failing

### Affected Procedures

| Procedure | Service | Status | Evidence |
|---|---|---|---|
| `ims_registration` (IMS Registration) | vonr | 🔴 failing | observed degradation on pcscf along this procedure's path this episode |
| `vonr_call_setup` (VoNR Call Setup) | vonr | 🔴 failing | observed degradation on pcscf along this procedure's path this episode |
| `vonr_call_teardown` (VoNR Call Teardown) | vonr | 🔴 failing | observed degradation on pcscf along this procedure's path this episode |
| `vonr_media` (VoNR Media Path) | vonr | 🟡 degraded | low-severity signal on upf along this procedure's path this episode |

**Downstream-affected NFs (symptomatic):** `icscf`, `pcscf`, `scscf`, `smf`, `upf`

## Resolution

**Heal method:** scheduled  
**Recovery time:** 498.0s
