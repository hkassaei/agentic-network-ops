# Episode Report: MongoDB Gone

**Agent:** v6  
**Episode ID:** ep_20260422_024021_mongodb_gone  
**Date:** 2026-04-22T02:40:22.970414+00:00  
**Duration:** 315.0s  

---

## Scenario

**Category:** container  
**Blast radius:** global  
**Description:** Kill MongoDB — the 5G core subscriber data store. UDR loses its backend, new PDU sessions cannot be created, and subscriber queries fail. Existing sessions may survive briefly.

## Faults Injected

- **container_kill** on `mongo`

## Baseline (Pre-Fault)

Stack phase before injection: **ready**
All containers running at baseline.

## Fault Propagation Verification

**Verdict:** ⚠️ `inconclusive`

- **Wait:** 0s
- **Actual elapsed:** 0.0s
- **Nodes with significant deltas:** 0
- **Nodes with any drift:** 4

## Symptoms Observed

Symptoms detected: **No**  
Observation iterations: 1

## Anomaly Screening (Phase 0)

**ANOMALY DETECTED.** Overall anomaly score: 0.96 (threshold: 0.70, trained on 211 healthy snapshots). The current metric pattern is statistically different from the learned healthy baseline. Something in the network has changed.

The following metrics deviate from their learned-healthy baseline. Treat each as a semantic observation (meaning + numbers), not a number alone — the KB's interpretation is the authoritative reading.

- **`derived.pcscf_sip_error_ratio`** (P-CSCF SIP error response ratio) — current **1.00 ratio** vs learned baseline **0.00 ratio** (HIGH, spike)
    - **What it measures:** Proportion of SIP responses that are errors. Zero is the healthy
baseline; any sustained non-zero value means P-CSCF or something
downstream is rejecting requests.
    - **Spike means:** Errors flowing back — downstream CSCFs or HSS rejecting.
    - **Healthy typical range:** 0–0 ratio
    - **Healthy invariant:** Zero in healthy operation.

- **`normalized.upf.gtp_outdatapktn3upf_per_ue`** (GTP-U downlink rate per UE (N3)) — current **0.03 packets_per_second** vs learned baseline **3.34 packets_per_second** (MEDIUM, drop)
    - **What it measures:** Health of the downlink user-plane path UPF → gNB. Typically mirrors
uplink rate during calls/data; asymmetry indicates directional
faults (e.g., UPF receiving from internet but not forwarding).
    - **Drop means:** No traffic leaving UPF toward RAN.
    - **Healthy typical range:** 0–10 packets_per_second
    - **Healthy invariant:** Per-UE — constant regardless of UE pool size. Roughly symmetric with uplink during healthy calls.

- **`normalized.upf.gtp_indatapktn3upf_per_ue`** (GTP-U uplink rate per UE (N3)) — current **0.15 packets_per_second** vs learned baseline **3.42 packets_per_second** (MEDIUM, drop)
    - **What it measures:** Health of the uplink user-plane path gNB → UPF. Drops to near-zero
during RAN or N3 outage; stays nonzero during active calls or data
sessions. Decoupled from SIP signaling (signals data plane, not
control plane).
    - **Drop means:** Data plane dead on uplink — UPF receiving no packets from gNB.
    - **Healthy typical range:** 0–10 packets_per_second
    - **Healthy invariant:** Per-UE — constant regardless of UE pool size. Rises during active VoNR calls (~100 pps for G.711 voice) and data transfer.

- **`normalized.smf.bearers_per_ue`** (Active QoS bearers per UE) — current **2.00 count** vs learned baseline **2.61 count** (LOW, shift)
    - **What it measures:** Per-UE count of active QoS bearers. Baseline reflects default
bearers; increments during VoNR calls indicate dedicated voice
bearers being set up. Drop during an active call = dedicated
bearer torn down unexpectedly (voice will fail).
    - **Shift means:** Expected during VoNR calls (1 extra bearer per active call).
    - **Healthy typical range:** 2–3.5 count
    - **Healthy invariant:** At rest: equals configured default bearers (typically 2 per UE).
During active VoNR call: +1 per caller. The per-UE ratio is the
invariant; absolute count scales with UE pool.

- **`normalized.pcscf.dialogs_per_ue`** (Active SIP dialogs per registered UE at P-CSCF) — current **0.00 count** vs learned baseline **0.57 count** (LOW, drop)
    - **What it measures:** How many calls per user are currently in progress at the P-CSCF.
Going to zero from a non-zero baseline means calls have ended
(normal) OR call setup is failing system-wide (degradation).
Together with rcv_requests_* it discriminates the two.
    - **Drop means:** No active calls. Normal at rest; suspicious if prior state had calls AND INVITE rate is also zero.
    - **Healthy typical range:** 0–1 count
    - **Healthy invariant:** Per-UE — scale-independent. 0 at rest, ~1 per active VoNR call.

- **`derived.upf_activity_during_calls`** — current **1.00** vs learned baseline **0.47** (LOW, spike). *(No KB context available — interpret from the metric name.)*


## Event Aggregation (Phase 1)

No events fired during this episode. Either no metric KB triggers matched, or the episode encountered no meaningful state transitions.

## Correlation Analysis (Phase 2)

No events fired — correlation engine had nothing to work with.

## Network Analysis (Phase 3)

**Summary:** The IMS stack is down due to SIP call setup failures, caused by the PCF being unable to process QoS authorization requests because its backing MongoDB database has crashed.

**Layer status:**

| Layer | Rating | Note |
|---|---|---|
| **infrastructure** | 🔴 RED | A critical database has failed, impacting multiple network functions. |
| **ran** | 🟢 GREEN | The RAN appears to be functional. |
| **core** | 🟡 YELLOW | Core network functions are running, but a key policy component (PCF) is non-functional due to an infrastructure issue, halting user services. |
| **ims** | 🔴 RED | The IMS is completely non-functional for call setup. |

**INFRASTRUCTURE evidence:**
- get_network_status shows the 'mongo' container has 'exited'.
- get_network_topology shows multiple INACTIVE links to MongoDB from UDR, PCF, BSF, and NSSF.

**CORE evidence:**
- The PCF is dependent on the failed MongoDB, leading to downstream failures.
- Data plane is inactive, as shown by near-zero GTP traffic rates on the UPF.

**IMS evidence:**
- P-CSCF SIP error ratio is 1.0, meaning all call attempts are failing.
- P-CSCF metrics show a large number of 'httpclient:connfail' errors, indicating a failure to communicate with a required service (likely PCF).
- No active SIP dialogs or dedicated voice bearers are being established.

**Ranked hypotheses:**

- **`h1`** (fit=0.90, nf=pcf, specificity=specific):
    - **Statement:** The PCF is failing to respond to P-CSCF requests for QoS authorization because its backing database, MongoDB, has exited. This causes the P-CSCF's attempts to communicate with the PCF to fail, leading to SIP call setup failures as QoS rules for dedicated voice bearers cannot be established.
    - **Falsification probes:**
        - Check the logs of the 'pcf' container for errors related to MongoDB connectivity.
        - Restart the 'mongo' container and observe if the 'pcscf_sip_error_ratio' metric returns to its healthy baseline of 0.
        - Query the PCF's status endpoint directly to confirm it is in a failed state.
- **`h2`** (fit=0.60, nf=pcf, specificity=moderate):
    - **Statement:** A network partition or a failure of the Rx interface is preventing the P-CSCF from communicating with the PCF. This communication breakdown is causing QoS authorization to fail, resulting in the rejection of SIP INVITEs.
    - **Falsification probes:**
        - Execute 'measure_rtt(container=\'pcscf\', target_ip=\'pcf\')' to test network connectivity. A high RTT or 100% packet loss would support this hypothesis.
        - Check the configuration of both P-CSCF and PCF to ensure the Rx interface is correctly defined.
- **`h3`** (fit=0.30, nf=pcscf, specificity=specific):
    - **Statement:** The P-CSCF has an internal fault in its HTTP client module, causing all outbound requests for QoS policy to fail. This is happening independently of the status of the PCF or the network.
    - **Falsification probes:**
        - Check P-CSCF logs for internal error messages not related to connection timeouts.
        - Attempt a connection from the P-CSCF container to another known-good HTTP endpoint; success would indicate the client module itself is working.


## Falsification Plans (Phase 4)

**3 falsification plan(s) — one per hypothesis:**

### Plan for `h1` (target: `pcf`)

**Hypothesis:** The PCF is failing to respond to P-CSCF requests for QoS authorization because its backing database, MongoDB, has exited. This causes the P-CSCF's attempts to communicate with the PCF to fail, leading to SIP call setup failures as QoS rules for dedicated voice bearers cannot be established.

**Probes (3):**
1. **`get_network_status`** — Check status of all containers
    - *Expected if hypothesis holds:* The 'mongo' container is in an 'exited' state, while the 'pcf' container is 'running'.
    - *Falsifying observation:* The 'mongo' container is 'running'. This would indicate the database is not the root cause of the PCF's failure to respond.
2. **`check_process_listeners`** — container='pcf'
    - *Expected if hypothesis holds:* The PCF process is not listening on its HTTP port for the N5 interface, indicating it's internally unhealthy or has failed to initialize due to the database issue.
    - *Falsifying observation:* The PCF process is actively listening on its N5 HTTP port. This would suggest the PCF is running and waiting for requests, shifting the problem to network connectivity or a fault within the P-CSCF.
3. **`get_nf_metrics`** — Look for P-CSCF's outbound connection metrics to PCF.
    - *Expected if hypothesis holds:* The 'httpclient:connfail' metric for the 'pcscf' container is high or increasing, showing repeated failed connection attempts to the PCF.
    - *Falsifying observation:* The 'httpclient:connfail' metric for 'pcscf' is zero. This would imply the P-CSCF is not even attempting to connect, pointing to an internal issue within the P-CSCF itself (closer to h3).

*Notes:* This plan focuses on verifying the direct consequences of the MongoDB failure on the PCF process and how that failure is observed by its direct upstream client, the P-CSCF.

### Plan for `h2` (target: `pcf`)

**Hypothesis:** A network partition or a failure of the Rx interface is preventing the P-CSCF from communicating with the PCF. This communication breakdown is causing QoS authorization to fail, resulting in the rejection of SIP INVITEs.

**Probes (3):**
1. **`measure_rtt`** — from='pcscf', to_ip='pcf'
    - *Expected if hypothesis holds:* 100% packet loss or very high RTT (>500ms), indicating a network partition.
    - *Falsifying observation:* A clean, low RTT (<10ms), which proves basic network connectivity is healthy between the two components.
2. **`measure_rtt`** — from='pcf', to_ip='pcscf'
    - *Expected if hypothesis holds:* 100% packet loss, similar to the forward direction.
    - *Falsifying observation:* A clean, low RTT (<10ms). If this probe succeeds while the forward probe fails, it indicates an asymmetric network issue (e.g., firewall rule) rather than a simple partition, thus weakening the original hypothesis.
3. **`measure_rtt`** — from='amf', to_ip='pcf'
    - *Expected if hypothesis holds:* This is for triangulation. If the partition is specific to the P-CSCF's network segment, this probe should show a low RTT. If the PCF's ingress is the problem, this will also fail.
    - *Falsifying observation:* If the primary probe (pcscf->pcf) shows low RTT, the hypothesis is already false. If the primary probe shows packet loss but this one succeeds, it isolates the problem to the P-CSCF's egress path.

*Notes:* This plan is designed to rigorously test the network connectivity claim. It includes mandatory triangulation probes (reverse path and third-source) to avoid ambiguity in interpreting packet loss between two points.

### Plan for `h3` (target: `pcscf`)

**Hypothesis:** The P-CSCF has an internal fault in its HTTP client module, causing all outbound requests for QoS policy to fail. This is happening independently of the status of the PCF or the network.

**Probes (3):**
1. **`get_nf_metrics`** — Compare SIP invite receipts vs. HTTP client requests at P-CSCF.
    - *Expected if hypothesis holds:* The 'core:rcv_requests_invite' metric is increasing, but the 'httpclient:requests' metric is flat or zero. This shows it is receiving calls but not attempting to make the required outbound HTTP call to the PCF.
    - *Falsifying observation:* The 'httpclient:requests' metric is increasing alongside 'core:rcv_requests_invite'. This proves the HTTP client is attempting to send requests, falsifying the hypothesis that the client module is completely faulted.
2. **`measure_rtt`** — from='pcscf', to_ip='pyhss'
    - *Expected if hypothesis holds:* A clean, low RTT (<10ms). The hypothesis claims a fault in the application's HTTP client, not the container's underlying network stack.
    - *Falsifying observation:* 100% packet loss or high RTT. This would indicate a broader networking problem with the P-CSCF container, making the hypothesis too specific.
3. **`check_process_listeners`** — container='pcscf'
    - *Expected if hypothesis holds:* The P-CSCF process is actively listening on its SIP ports (e.g., 5050) to receive INVITEs.
    - *Falsifying observation:* The P-CSCF process is not listening on its SIP ports. This would contradict the fact that it's processing SIP calls at all, indicating a more fundamental process failure.

*Notes:* This plan attempts to isolate the fault to the P-CSCF's HTTP client module by checking if it even tries to send requests and if the container's basic network connectivity to other components is functional.


## Parallel Investigators (Phase 5)

**3 sub-Investigator verdict(s):** **1 NOT_DISPROVEN**, **2 DISPROVEN**

### `h1` — ✅ **NOT_DISPROVEN**

**Hypothesis:** The PCF is failing to respond to P-CSCF requests for QoS authorization because its backing database, MongoDB, has exited. This causes the P-CSCF's attempts to communicate with the PCF to fail, leading to SIP call setup failures as QoS rules for dedicated voice bearers cannot be established.

**Reasoning:** The evidence strongly supports the hypothesis. The prerequisite condition, the MongoDB container being down, was confirmed. The primary effect, the P-CSCF failing to connect to the PCF, was also confirmed by a very high connection failure count. While the PCF process was found to be listening on its network port, this doesn't contradict the core of the hypothesis, as a process can listen for connections but still be unable to service them due to a backend failure.

**Probes executed (3):**
- **Check status of all containers** ✓ CONSISTENT
    - *Tool:* `default_api.get_network_status()`
    - *Observation:* [EVIDENCE: get_network_status() -> "'containers': {'mongo': 'exited', 'pcf': 'running'}"]
    - *Comment:* This result confirms the prerequisite of the hypothesis: the 'mongo' container has exited while the 'pcf' container is still running.
- **Check what ports and protocols a container's processes are listening on.** ✗ CONTRADICTS
    - *Tool:* `default_api.check_process_listeners(container='pcf')`
    - *Observation:* [EVIDENCE: check_process_listeners("pcf") -> "tcp   LISTEN 0      5        172.22.0.27:7777       0.0.0.0:*    users:(('open5gs-pcfd',pid=8,fd=12))"]
    - *Comment:* The PCF process is listening on its service port. This contradicts the expectation that it would be completely unresponsive, but it doesn't falsify the overall hypothesis. The process can be listening but unable to handle requests due to the database issue.
- **Look for P-CSCF's outbound connection metrics to PCF.** ✓ CONSISTENT
    - *Tool:* `default_api.get_nf_metrics()`
    - *Observation:* [EVIDENCE: get_nf_metrics() -> "PCSCF [...]: httpclient:connfail = 16885.0, httpclient:connok = 0.0"]
    - *Comment:* The metrics show a massive number of connection failures from the P-CSCF and zero successful connections. This directly supports the hypothesis that P-CSCF's attempts to communicate with the PCF are failing.

### `h2` — ❌ **DISPROVEN**

**Hypothesis:** A network partition or a failure of the Rx interface is preventing the P-CSCF from communicating with the PCF. This communication breakdown is causing QoS authorization to fail, resulting in the rejection of SIP INVITEs.

**Reasoning:** All three network connectivity probes (`measure_rtt` from pcscf->pcf, pcf->pcscf, and amf->pcf) showed excellent results with zero packet loss and very low round-trip times. This directly contradicts the hypothesis that a network partition or failure is preventing communication. The underlying network layer is healthy.

**Probes executed (3):**
- **from='pcscf', to_ip='pcf'** ✗ CONTRADICTS
    - *Tool:* `measure_rtt(container='pcscf', target_ip='172.22.0.27')`
    - *Observation:* [EVIDENCE: measure_rtt("pcscf", "172.22.0.27") -> "3 packets transmitted, 3 received, 0% packet loss, time 2040ms\nrtt min/avg/max/mdev = 0.113/0.154/0.226/0.051 ms"]
    - *Comment:* The RTT is extremely low and there is no packet loss, which proves basic network connectivity is healthy. This contradicts the network partition hypothesis.
- **from='pcf', to_ip='pcscf'** ✗ CONTRADICTS
    - *Tool:* `measure_rtt(container='pcf', target_ip='172.22.0.21')`
    - *Observation:* [EVIDENCE: measure_rtt("pcf", "172.22.0.21") -> "3 packets transmitted, 3 received, 0% packet loss, time 2056ms\nrtt min/avg/max/mdev = 0.064/0.078/0.101/0.016 ms"]
    - *Comment:* The reverse path also shows a clean, low RTT, confirming that there is no asymmetric network issue.
- **from='amf', to_ip='pcf'** ✗ CONTRADICTS
    - *Tool:* `measure_rtt(container='amf', target_ip='172.22.0.27')`
    - *Observation:* [EVIDENCE: measure_rtt("amf", "172.22.0.27") -> "3 packets transmitted, 3 received, 0% packet loss, time 2036ms\nrtt min/avg/max/mdev = 0.118/0.203/0.369/0.116 ms"]
    - *Comment:* This triangulation probe confirms that the PCF is reachable from other parts of the network, reinforcing the conclusion that there is no network partition affecting the PCF.

**Alternative suspects:** pcf

### `h3` — ❌ **DISPROVEN**

**Hypothesis:** The P-CSCF has an internal fault in its HTTP client module, causing all outbound requests for QoS policy to fail. This is happening independently of the status of the PCF or the network.

**Reasoning:** The hypothesis is that the P-CSCF's HTTP client module is internally faulted and not making outbound requests. However, metrics show the P-CSCF is attempting a large number of HTTP connections that are failing ('httpclient:connfail' is high, 'httpclient:connok' is zero). This proves the client module is active and trying to send requests, falsifying the core claim of the hypothesis and suggesting the problem lies with the remote endpoint (PCF) or the path to it.

**Probes executed (3):**
- **Compare SIP invite receipts vs. HTTP client requests at P-CSCF.** ✗ CONTRADICTS
    - *Tool:* `default_api.get_nf_metrics()`
    - *Observation:* [EVIDENCE: get_nf_metrics() -> "PCSCF ... core:rcv_requests_invite = 24.0 ... httpclient:connfail = 16883.0 ... httpclient:connok = 0.0"]
    - *Comment:* The high 'connfail' count proves the HTTP client is attempting to make outbound requests, which contradicts the hypothesis that the module has an internal fault that prevents it from doing so. The failures must be happening at the connection stage, not because the client is broken.
- **from='pcscf', to_ip='pyhss'** ✓ CONSISTENT
    - *Tool:* `default_api.measure_rtt(container='pcscf', target_ip='172.22.0.18')`
    - *Observation:* [EVIDENCE: measure_rtt("pcscf", "172.22.0.18") -> "3 packets transmitted, 3 received, 0% packet loss, time 2043ms
rtt min/avg/max/mdev = 0.087/0.204/0.404/0.141 ms"]
    - *Comment:* A low RTT to another host confirms the P-CSCF container's basic network connectivity is fine. This is consistent with an application-level fault, but does not prove it. The key finding remains the connection failures from the first probe.
- **container='pcscf'** ✓ CONSISTENT
    - *Tool:* `default_api.check_process_listeners(container='pcscf')`
    - *Observation:* [EVIDENCE: check_process_listeners("pcscf") -> "udp   UNCONN 0      0        172.22.0.21:5060       0.0.0.0:*    users:((\"kamailio\",pid=62,fd=8)...)"]
    - *Comment:* This confirms that the P-CSCF process is running and listening on its SIP port (5060), which is a necessary precondition for it to receive INVITEs and subsequently attempt to contact the PCF.

**Alternative suspects:** pcf


## Evidence Validation (Phase 6)

**Overall verdict:** clean
**Overall confidence:** high

**Per sub-Investigator:**

| Agent | Tool Calls | Citations | Verdict | Confidence |
|---|---|---|---|---|
| `InvestigatorAgent_h1` | 3 | 3/3 | clean | high |
| `InvestigatorAgent_h2` | 4 | 3/3 | clean | high |
| `InvestigatorAgent_h3` | 4 | 3/3 | clean | high |


## Ground Truth

**Failure domain:** data_layer  
**Protocol impact:** multiple  
**Affected components:** mongo  
**Severity:** degraded

## Agent Diagnosis

**Model:** v6-adk/gemini-2.5-flash+gemini-2.5-pro  
**Time to diagnosis:** 180.5s

### Prompt to RCA Agent

```
The 5G SA + IMS stack is experiencing issues. Investigate and diagnose the root cause.
```

**Diagnosis:**

> ### causes
> - **summary**: The IMS stack is down due to SIP call setup failures, caused by the PCF being unable to process QoS authorization requests because its backing MongoDB database has crashed.
> - **timeline**:
>     1. The `mongo` container, the backing database for the PCF, terminated unexpectedly.
>     2. The PCF network function, now unable to access its database, began failing to process incoming requests for QoS policy authorization.
>     3. The P-CSCF, upon receiving SIP INVITEs for new calls, attempted to contact the PCF. These connection attempts failed at the application layer.
>     4. Unable to get QoS authorization from the PCF, the P-CSCF could not set up the required dedicated bearers for voice traffic, leading to the rejection of SIP calls.
> - **root_cause**: The PCF is failing because its backing database (MongoDB) has crashed. The primary suspect network function directly impacted is the `pcf`.
> - **affected_components**:
>     - `mongo` (database container): Root Cause
>     - `pcf` (Policy Control Function): Secondary
>     - `pcscf` (Proxy-CSCF): Symptomatic
>     - `IMS Stack`: Symptomatic
> - **recommendation**: Verify the status and logs of the `mongo` container to understand why it terminated. Concurrently, inspect the logs of the `pcf` container for error messages related to database connectivity to confirm it cannot function without its database.
> - **confidence**: high
> - **explanation**: The investigation confirmed that the issue is a dependency failure, not a network or internal component fault. Hypothesis `h1` (PCF failure due to a crashed database) was proven correct by evidence showing the `mongo` container had `exited` and P-CSCF metrics indicated over 16,000 failed connection attempts to the PCF. This explains why the PCF is not servicing requests despite its process still listening on the network. Alternative hypotheses were disproven: a network partition (`h2`) was ruled out by `measure_rtt` probes showing perfect connectivity, and an internal P-CSCF fault (`h3`) was falsified by metrics proving the P-CSCF was actively trying to send requests. With competing theories eliminated, confidence in the root cause is high.

### Scoring Breakdown

**Overall score: 100%**

**Scorer assessment:** The agent provided a perfectly accurate and well-supported diagnosis, correctly identifying the root cause, affected components, severity, fault type, and layer, with appropriate confidence.

| Dimension | Result | Rationale |
|-----------|--------|-----------|
| Root cause correct | Yes | The agent correctly identified that the MongoDB database crashed, which is the direct cause of the observed failures. It also correctly linked this to the PCF's inability to function, leading to IMS issues. This aligns perfectly with the simulated failure mode of 'MongoDB Gone'. |
| Component overlap | 100% | The agent explicitly listed 'mongo' (database container) as the 'Root Cause' in its 'affected_components' list, which is the primary affected component according to the simulated failure. |
| Severity correct | Yes | The simulated failure resulted in the 'mongo' component being completely unreachable, leading to a complete outage for dependent services. The agent correctly diagnosed this as the 'IMS stack is down' and 'completely non-functional for call setup', indicating a full outage. |
| Fault type identified | Yes | The agent identified that the 'mongo' container had 'exited'/'crashed', which is the observable fault type for a component being 'completely unreachable' or 'not responding'. |
| Layer accuracy | Yes | The ground truth states that 'mongo' belongs to the 'infrastructure' layer. The agent's network analysis correctly rated the 'infrastructure' layer as 'red' with the evidence 'mongo' container has 'exited'. |
| Confidence calibrated | Yes | The agent stated 'high' confidence, which is appropriate given that its diagnosis is accurate across all evaluated dimensions and supported by clear evidence (mongo exited, P-CSCF connection failures). |

**Ranking position:** #1 — The agent provided a single, clear root cause in its final diagnosis, which was correct. Therefore, the correct cause is ranked first.


### Token Usage

| Metric | Count |
|--------|-------|
| Input tokens | 185,618 |
| Output tokens | 5,508 |
| Thinking tokens | 14,733 |
| **Total tokens** | **205,859** |

**Per-phase breakdown:**

| Phase | Tokens | Tool Calls | LLM Calls |
|-------|--------|------------|-----------|
| AnomalyScreener | 0 | 0 | 0 |
| EventAggregator | 0 | 0 | 0 |
| CorrelationAnalyzer | 0 | 0 | 0 |
| NetworkAnalystAgent | 34,993 | 4 | 3 |
| InstructionGeneratorAgent | 29,061 | 2 | 3 |
| InvestigatorAgent_h1 | 33,276 | 3 | 4 |
| InvestigatorAgent_h2 | 41,385 | 4 | 5 |
| InvestigatorAgent_h3 | 61,041 | 4 | 5 |
| EvidenceValidator | 0 | 0 | 0 |
| SynthesisAgent | 6,103 | 0 | 1 |


## Resolution

**Heal method:** scheduled  
**Recovery time:** 315.0s
