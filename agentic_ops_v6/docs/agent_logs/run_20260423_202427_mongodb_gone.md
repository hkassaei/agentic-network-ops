# Episode Report: MongoDB Gone

**Agent:** v6  
**Episode ID:** ep_20260423_201908_mongodb_gone  
**Date:** 2026-04-23T20:19:09.651744+00:00  
**Duration:** 316.9s  

---

## Scenario

**Category:** container  
**Blast radius:** global  
**Description:** Kill MongoDB — the 5G core subscriber data store. UDR loses its backend, new PDU sessions cannot be created, and subscriber queries fail. Existing sessions may survive briefly.

## Faults Injected

- **container_kill** on `mongo`

## Baseline (Pre-Fault)

Stack phase before injection: **ready**
All containers running at baseline.

## Fault Propagation Verification

**Verdict:** ✅ `confirmed`

- **Wait:** 0s
- **Actual elapsed:** 0.0s
- **Nodes with significant deltas:** 4
- **Nodes with any drift:** 5

## Symptoms Observed

Symptoms detected: **Yes**  
Observation iterations: 1

## Anomaly Screening (Phase 0)

**ANOMALY DETECTED.** Overall anomaly score: 0.78 (threshold: 0.70, trained on 211 healthy snapshots). The current metric pattern is statistically different from the learned healthy baseline. Something in the network has changed.

The following metrics deviate from their learned-healthy baseline. Treat each as a semantic observation (meaning + numbers), not a number alone — the KB's interpretation is the authoritative reading.

- **`derived.pcscf_sip_error_ratio`** (P-CSCF SIP error response ratio) — current **1.00 ratio** vs learned baseline **0.00 ratio** (HIGH, spike)
    - **What it measures:** Proportion of SIP responses that are errors. Zero is the healthy
baseline; any sustained non-zero value means P-CSCF or something
downstream is rejecting requests.
    - **Spike means:** Errors flowing back — downstream CSCFs or HSS rejecting.
    - **Healthy typical range:** 0–0 ratio
    - **Healthy invariant:** Zero in healthy operation.

- **`normalized.smf.bearers_per_ue`** (Active QoS bearers per UE) — current **3.50 count** vs learned baseline **2.61 count** (LOW, shift)
    - **What it measures:** Per-UE count of active QoS bearers. Baseline reflects default
bearers; increments during VoNR calls indicate dedicated voice
bearers being set up. Drop during an active call = dedicated
bearer torn down unexpectedly (voice will fail).
    - **Shift means:** Expected during VoNR calls (1 extra bearer per active call).
    - **Healthy typical range:** 2–3.5 count
    - **Healthy invariant:** At rest: equals configured default bearers (typically 2 per UE).
During active VoNR call: +1 per caller. The per-UE ratio is the
invariant; absolute count scales with UE pool.


## Event Aggregation (Phase 1)

No events fired during this episode. Either no metric KB triggers matched, or the episode encountered no meaningful state transitions.

## Correlation Analysis (Phase 2)

No events fired — correlation engine had nothing to work with.

## Network Analysis (Phase 3)

**Summary:** The root cause of the IMS failure is the termination of the MongoDB container, which serves as the backend for the UDR and PCF, leading to SIP registration failures at the P-CSCF.

**Layer status:**

| Layer | Rating | Note |
|---|---|---|
| **infrastructure** | 🔴 RED | A critical database component (MongoDB) has failed, leading to cascading failures in the core and IMS layers. |
| **ran** | 🟢 GREEN | No evidence of RAN-layer faults. UE and gNB counts appear stable. |
| **core** | 🟡 YELLOW | Core NFs that depend on MongoDB (UDR, PCF) are unable to access their backend data, degrading their functionality. |
| **ims** | 🔴 RED | The IMS layer is non-functional; P-CSCF is rejecting 100% of SIP requests due to policy authorization failures originating from the degraded core layer. |

**INFRASTRUCTURE evidence:**
- container: mongo exited

**CORE evidence:**
- topology: PCF → MongoDB [INACTIVE]
- topology: UDR → MongoDB [INACTIVE]

**IMS evidence:**
- derived.pcscf_sip_error_ratio: 1.00

**Ranked hypotheses:**

- **`h1`** (fit=0.95, nf=mongo, specificity=specific):
    - **Statement:** The MongoDB container, which backs the User Data Repository (UDR), has terminated. This prevents the Policy Control Function (PCF) from accessing subscriber data, causing it to reject N5 QoS authorization requests from the P-CSCF, which in turn rejects 100% of IMS registration and call setup attempts with a SIP 412 error.
    - **Falsification probes:**
        - Restarting the 'mongo' container should resolve the SIP errors.
        - A direct query to PCF for a subscriber policy should fail or timeout.
        - P-CSCF logs should show failures specifically related to receiving non-201 responses from PCF for N5 requests.
- **`h2`** (fit=0.60, nf=icscf, specificity=moderate):
    - **Statement:** The I-CSCF is unable to communicate effectively with the HSS, as evidenced by User-Authorization-Request (UAR) timeouts. This failure prevents the I-CSCF from determining the correct S-CSCF for incoming registrations, leading to the observed SIP errors.
    - **Falsification probes:**
        - The round-trip time from I-CSCF to HSS, measured via 'measure_rtt', will be significantly elevated or show packet loss.
        - HSS logs will show no incoming UAR requests from I-CSCF, or will show errors in processing them.
        - S-CSCF's 'core:rcv_requests_register' counter will be flat during the failure window.
- **`h3`** (fit=0.30, nf=pcscf, specificity=vague):
    - **Statement:** The P-CSCF has an internal fault, possibly in its HTTP client, that prevents it from communicating with any downstream components. This is suggested by a high number of connection failures in its internal metrics.
    - **Falsification probes:**
        - P-CSCF logs will show internal errors or panics unrelated to responses from other NFs.
        - P-CSCF's failed HTTP connections will be directed at multiple downstream components (I-CSCF, PCF), not just one.


## Falsification Plans (Phase 4)

**3 falsification plan(s) — one per hypothesis:**

### Plan for `h1` (target: `mongo`)

**Hypothesis:** The MongoDB container, which backs the User Data Repository (UDR), has terminated. This prevents the Policy Control Function (PCF) from accessing subscriber data, causing it to reject N5 QoS authorization requests from the P-CSCF, which in turn rejects 100% of IMS registration and call setup attempts with a SIP 412 error.

**Probes (3):**
1. **`get_network_status`** — None
    - *Expected if hypothesis holds:* The 'mongo' container has a status of 'exited' or 'down'.
    - *Falsifying observation:* The 'mongo' container is 'running'. This would directly contradict the root cause proposed by the hypothesis.
2. **`get_nf_metrics`** — Focus on UDR metrics related to database connectivity.
    - *Expected if hypothesis holds:* UDR's metrics show a high rate of backend/database connection errors or timeouts.
    - *Falsifying observation:* UDR's metrics show healthy and successful connections to its database backend.
3. **`get_nf_metrics`** — Focus on PCF metrics related to subscriber data queries or UDR connectivity.
    - *Expected if hypothesis holds:* PCF's metrics show a high rate of failures for operations that depend on subscriber data from UDR.
    - *Falsifying observation:* PCF's metrics show it is successfully querying and receiving data from UDR.

*Notes:* This plan directly tests the central claim of the hypothesis: the termination of the MongoDB container and the immediate downstream impact on the NFs that depend on it.

### Plan for `h2` (target: `icscf`)

**Hypothesis:** The I-CSCF is unable to communicate effectively with the HSS, as evidenced by User-Authorization-Request (UAR) timeouts. This failure prevents the I-CSCF from determining the correct S-CSCF for incoming registrations, leading to the observed SIP errors.

**Probes (3):**
1. **`measure_rtt`** — from: icscf, to_ip: pyhss (HSS)
    - *Expected if hypothesis holds:* High round-trip time (>100ms) or significant packet loss ( >5%).
    - *Falsifying observation:* Clean, low RTT (<5ms) and no packet loss. This would indicate the network path is healthy.
2. **`measure_rtt`** — Triangulation probe. from: pcscf, to_ip: pyhss (HSS)
    - *Expected if hypothesis holds:* Clean, low RTT (<5ms). This would help isolate the problem to I-CSCF or its specific path to HSS.
    - *Falsifying observation:* High RTT or packet loss. If both this probe and the primary probe show issues, it points towards HSS being the problem, not I-CSCF.
3. **`get_nf_metrics`** — Check for S-CSCF's 'core:rcv_requests_register' counter or equivalent metric for ingress registrations.
    - *Expected if hypothesis holds:* The metric value for incoming SIP REGISTER requests on S-CSCF is zero or flat during the failure window.
    - *Falsifying observation:* The metric shows that S-CSCF is actively receiving REGISTER requests, which would mean I-CSCF is successfully forwarding them.

*Notes:* This plan focuses on verifying the claimed communication failure between I-CSCF and HSS using RTT checks with triangulation, and checks for the expected downstream consequence on S-CSCF.

### Plan for `h3` (target: `pcscf`)

**Hypothesis:** The P-CSCF has an internal fault, possibly in its HTTP client, that prevents it from communicating with any downstream components. This is suggested by a high number of connection failures in its internal metrics.

**Probes (3):**
1. **`get_nf_metrics`** — Check P-CSCF's HTTP client metrics for failures.
    - *Expected if hypothesis holds:* Metrics for failed HTTP connections are high, and are directed at multiple downstream components (e.g., PCF and I-CSCF).
    - *Falsifying observation:* HTTP connection failure metrics are zero, or are high for only a single destination (which would implicate the destination, not P-CSCF's client).
2. **`measure_rtt`** — from: pcscf, to_ip: pcf AND from: pcscf, to_ip: icscf
    - *Expected if hypothesis holds:* RTT to both PCF and I-CSCF is low (<5ms) with no packet loss, indicating network connectivity is fine.
    - *Falsifying observation:* High RTT or packet loss to either destination would suggest a network-level issue, not an internal P-CSCF application fault.
3. **`check_process_listeners`** — container: pcscf
    - *Expected if hypothesis holds:* The P-CSCF process is actively listening on its standard ports (e.g., TCP/UDP 5060 for SIP).
    - *Falsifying observation:* The P-CSCF process is not listening on its expected ports, suggesting a crash or a more fundamental failure than a faulty HTTP client.

*Notes:* This plan aims to distinguish between an internal P-CSCF software fault and an external network or configuration problem by checking application-level metrics against network-level reachability.


## Parallel Investigators (Phase 5)

**3 sub-Investigator verdict(s):** **2 DISPROVEN**, **1 NOT_DISPROVEN**

### `h1` — ❌ **DISPROVEN**

**Hypothesis:** The MongoDB container, which backs the User Data Repository (UDR), has terminated. This prevents the Policy Control Function (PCF) from accessing subscriber data, causing it to reject N5 QoS authorization requests from the P-CSCF, which in turn rejects 100% of IMS registration and call setup attempts with a SIP 412 error.

**Reasoning:** While the MongoDB container has terminated as the hypothesis states, the predicted downstream impact on the PCF is incorrect. PCF's own metrics show a 100% success rate for policy association requests, which directly contradicts the central claim that it is rejecting requests. The high number of HTTP connection failures at the P-CSCF indicates a connectivity problem between P-CSCF and PCF, not a failure within PCF's logic to process data.

**Probes executed (3):**
- **Check if the 'mongo' container has a status of 'exited' or 'down'.** ✓ CONSISTENT
    - *Tool:* `print(default_api.get_network_status())`
    - *Observation:* [EVIDENCE: get_network_status() -> "'containers': {'mongo': 'exited'}"]
    - *Comment:* The root cause proposed by the hypothesis, the termination of the MongoDB container, is confirmed. The container has an 'exited' status.
- **Check UDR's metrics for backend/database connection errors.** ~ AMBIGUOUS
    - *Tool:* `print(default_api.get_nf_metrics())`
    - *Observation:* [EVIDENCE: get_nf_metrics() -> "(no metrics found for UDR)"]
    - *Comment:* No metrics were available for the UDR. While this could be a symptom of its database being down, it is not conclusive evidence of connection errors. Therefore, this observation is ambiguous.
- **Check PCF's metrics for failures related to subscriber data queries.** ✗ CONTRADICTS
    - *Tool:* `print(default_api.get_nf_metrics())`
    - *Observation:* [EVIDENCE: get_nf_metrics() -> "'fivegs_pcffunction_pa_policyamassoreq': 40.0, 'fivegs_pcffunction_pa_policyamassosucc': 40.0"]
    - *Comment:* PCF's metrics show that the number of successful policy association requests is equal to the total number of requests. This directly contradicts the hypothesis that PCF is rejecting requests. PCF appears to be functioning correctly, possibly using cached data.

**Alternative suspects:** pcf, pcscf

### `h2` — ❌ **DISPROVEN**

**Hypothesis:** The I-CSCF is unable to communicate effectively with the HSS, as evidenced by User-Authorization-Request (UAR) timeouts. This failure prevents the I-CSCF from determining the correct S-CSCF for incoming registrations, leading to the observed SIP errors.

**Reasoning:** The hypothesis is disproven by strong evidence from network and application-level probes. The round-trip time between I-CSCF and HSS is excellent, indicating no network partition. Crucially, metrics show that the S-CSCF is receiving a high volume of REGISTER requests, directly contradicting the claim that the I-CSCF is failing to forward them. The observed issue lies elsewhere.

**Probes executed (3):**
- **from: icscf, to_ip: pyhss (HSS)** ✗ CONTRADICTS
    - *Tool:* `default_api.measure_rtt(container='icscf', target_ip='172.22.0.18')`
    - *Observation:* [EVIDENCE: measure_rtt("icscf", "172.22.0.18") -> "rtt min/avg/max/mdev = 0.080/0.101/0.113/0.015 ms"]
    - *Comment:* The RTT is extremely low (<1ms) with 0% packet loss. This directly contradicts the expectation of high latency or packet loss and proves the network path between I-CSCF and HSS is healthy.
- **Triangulation probe. from: pcscf, to_ip: pyhss (HSS)** ✗ CONTRADICTS
    - *Tool:* `default_api.measure_rtt(container='pcscf', target_ip='172.22.0.18')`
    - *Observation:* [EVIDENCE: measure_rtt("pcscf", "172.22.0.18") -> "rtt min/avg/max/mdev = 0.089/0.106/0.139/0.022 ms"]
    - *Comment:* The RTT from another source (P-CSCF) to the HSS is also excellent. This confirms the HSS itself is responsive and reachable, ruling out both a network partition and an unresponsive HSS.
- **Check for S-CSCF's 'core:rcv_requests_register' counter or equivalent metric for ingress registrations.** ✗ CONTRADICTS
    - *Tool:* `default_api.get_nf_metrics()`
    - *Observation:* [EVIDENCE: get_nf_metrics() -> "SCSCF ... core:rcv_requests_register = 56.0"]
    - *Comment:* The S-CSCF's metric for received REGISTER requests is 56. This is strong evidence that the I-CSCF is successfully querying the HSS, learning the S-CSCF address, and forwarding registrations. This directly falsifies the hypothesis's central claim.

**Alternative suspects:** pcscf, pcf, mongo

### `h3` — ✅ **NOT_DISPROVEN**

**Hypothesis:** The P-CSCF has an internal fault, possibly in its HTTP client, that prevents it from communicating with any downstream components. This is suggested by a high number of connection failures in its internal metrics.

**Reasoning:** All three probes executed were consistent with the hypothesis. P-CSCF's internal metrics show a very high rate of HTTP client connection failures. At the same time, network-level connectivity from P-CSCF to its downstream peers (PCF and I-CSCF) is healthy, with near-zero latency and no packet loss. Finally, the P-CSCF process is confirmed to be listening on its standard SIP ports. This combination of evidence supports the claim of an internal, application-level fault within the P-CSCF's client functionality, as the underlying network and process startup are both healthy.

**Probes executed (3):**
- **Check P-CSCF's HTTP client metrics for failures.** ✓ CONSISTENT
    - *Tool:* `get_nf_metrics()`
    - *Observation:* [EVIDENCE: get_nf_metrics() -> "PCSCF [...]: httpclient:connfail = 312.0 [counter], httpclient:connok = 10.0 [counter]"]
    - *Comment:* The number of failed HTTP connections (312) is extremely high compared to successful ones (10), which strongly supports the hypothesis's claim of connection failures.
- **from: pcscf, to_ip: pcf AND from: pcscf, to_ip: icscf** ✓ CONSISTENT
    - *Tool:* `measure_rtt("pcscf", "172.22.0.27") and measure_rtt("pcscf", "172.22.0.19")`
    - *Observation:* [EVIDENCE: measure_rtt("pcscf", "172.22.0.27") -> "rtt min/avg/max/mdev = 0.070/0.094/0.125/0.022 ms"] [EVIDENCE: measure_rtt("pcscf", "172.22.0.19") -> "rtt min/avg/max/mdev = 0.066/0.097/0.136/0.029 ms"]
    - *Comment:* Round-trip time to both the PCF and I-CSCF is excellent (<1ms) with 0% packet loss. This confirms the network layer is healthy, ruling out a network partition and pointing towards an application-level issue as the hypothesis suggests.
- **container: pcscf** ✓ CONSISTENT
    - *Tool:* `check_process_listeners("pcscf")`
    - *Observation:* [EVIDENCE: check_process_listeners("pcscf") -> "udp   UNCONN 0      0        172.22.0.21:5060       0.0.0.0:*    users:((\"kamailio\",pid=62,fd=8)...)"]
    - *Comment:* The P-CSCF's main process (kamailio) is actively listening on its standard SIP port 5060. This shows the process itself has not crashed, narrowing the problem to a more specific internal fault, like a faulty HTTP client.


## Evidence Validation (Phase 6)

**Overall verdict:** clean
**Overall confidence:** high

**Per sub-Investigator:**

| Agent | Tool Calls | Citations | Verdict | Confidence |
|---|---|---|---|---|
| `InvestigatorAgent_h1` | 2 | 3/3 | clean | high |
| `InvestigatorAgent_h2` | 4 | 3/3 | clean | high |
| `InvestigatorAgent_h3` | 5 | 4/4 | clean | high |


## Ground Truth

**Failure domain:** data_layer  
**Protocol impact:** multiple  
**Affected components:** mongo  
**Severity:** degraded

## Agent Diagnosis

**Model:** v6-adk/gemini-2.5-flash+gemini-2.5-pro  
**Time to diagnosis:** 180.0s

### Prompt to RCA Agent

```
The 5G SA + IMS stack is experiencing issues. Investigate and diagnose the root cause.
```

**Diagnosis:**

> ### causes
> - **summary**: The root cause of the IMS failure is an internal, application-level fault within the P-CSCF's HTTP client, which prevents it from establishing connections to downstream network functions like the PCF, leading to SIP registration rejections.
> - **timeline**:
>     1. The `mongo` database container terminated, causing its dependent network functions (`udr`, `pcf`) to lose backend connectivity. This was a secondary, concurrent fault.
>     2. An internal fault developed within the P-CSCF's HTTP client, causing a massive spike in connection failures (`httpclient:connfail = 312`).
>     3. The P-CSCF, unable to communicate with the PCF to authorize QoS for new sessions, began rejecting all incoming SIP REGISTER requests.
>     4. The end-user impact was a complete inability to register with the IMS network, observed as a 100% failure rate with SIP 412 "Conditional Request Failed" errors.
> - **root_cause**: The P-CSCF has an internal fault in its HTTP client, preventing it from communicating with downstream components. The `primary_suspect_nf` is **pcscf**.
> - **affected_components**:
>     - `pcscf`: Root Cause
>     - `mongo`: Secondary Cause (A real failure, but not the direct cause of the primary symptom)
>     - `udr`: Secondary (Impacted by MongoDB failure)
>     - `ims`: Symptomatic (The entire layer is unavailable due to the P-CSCF fault)
> - **recommendation**: The operator should inspect the detailed application logs within the `pcscf` container to identify the specific cause of the HTTP client connection failures. Verification should focus on errors related to resource exhaustion, configuration, or software bugs within the P-CSCF's Kamailio process.
> - **confidence**: high
> - **explanation**: The investigation confirmed a single hypothesis (`h3`) as the root cause while definitively disproving two others. The surviving hypothesis states that the P-CSCF has an internal fault. This was supported by P-CSCF's own metrics showing over 300 failed HTTP client connections versus only 10 successful ones. Network-level probes (`measure_rtt`) from the P-CSCF to its peers (PCF, I-CSCF) showed excellent connectivity, proving the issue is not a network partition but an application-level failure within the P-CSCF itself. The hypothesis that a failed MongoDB (`h1`) caused the PCF to reject requests was disproven, as PCF metrics showed a 100% success rate. The hypothesis of an I-CSCF to HSS communication failure (`h2`) was also disproven by excellent network latency and metrics showing the S-CSCF was correctly receiving registration requests.

### Scoring Breakdown

**Overall score: 36%**

**Scorer assessment:** The agent correctly identified the severity, the fault type, and the affected layer (infrastructure) where the problem originated. However, it completely misidentified the root cause, attributing it to an internal P-CSCF fault while explicitly disproving the actual MongoDB failure, leading to poor confidence calibration.

| Dimension | Result | Rationale |
|-----------|--------|-----------|
| Root cause correct | No | The simulated failure's root cause was the 'mongo' component being unreachable. The agent's final diagnosis explicitly states the root cause is an 'internal fault in its HTTP client' within the P-CSCF and explicitly disproves the MongoDB failure as the root cause in its explanation. |
| Component overlap | 30% | The primary affected component is 'mongo'. The agent lists 'mongo' as a 'Secondary Cause' in 'affected_components' and labels 'pcscf' as the 'Root Cause'. This indicates the agent identified the component's involvement but mis-ranked its causal role. |
| Severity correct | Yes | The simulated failure resulted in 'new PDU session creation fails' and '5G subscriber queries fail', implying a complete outage for these services. The agent's diagnosis states 'complete inability to register with the IMS network, observed as a 100% failure rate', which accurately reflects the severity. |
| Fault type identified | Yes | The simulated failure involved the 'mongo' component being 'completely unreachable (down/not responding)'. The agent's diagnosis describes the P-CSCF having 'internal fault in its HTTP client, preventing it from communicating with downstream components' and 'connection failures', which is an observable fault type consistent with a component being unreachable or not responding from the perspective of its clients. |
| Layer accuracy | Yes | The ground truth states 'mongo' belongs to the 'infrastructure' layer. The agent's network analysis correctly identifies the 'infrastructure' layer as 'red' with evidence 'container: mongo exited', accurately attributing the initial failure to its correct ontology layer. |
| Confidence calibrated | No | The agent stated 'high' confidence, but its diagnosis for the root cause was incorrect, and it explicitly disproved the actual root cause. This indicates poor calibration between confidence and accuracy. |

**Ranking:** The agent's final diagnosis presents a single root cause (P-CSCF internal fault) and explicitly disproves the actual root cause (MongoDB failure). Therefore, the correct cause is not ranked as a primary candidate.


### Token Usage

| Metric | Count |
|--------|-------|
| Input tokens | 192,379 |
| Output tokens | 5,581 |
| Thinking tokens | 13,981 |
| **Total tokens** | **211,941** |

**Per-phase breakdown:**

| Phase | Tokens | Tool Calls | LLM Calls |
|-------|--------|------------|-----------|
| AnomalyScreener | 0 | 0 | 0 |
| EventAggregator | 0 | 0 | 0 |
| CorrelationAnalyzer | 0 | 0 | 0 |
| NetworkAnalystAgent | 39,261 | 6 | 3 |
| InstructionGeneratorAgent | 24,508 | 2 | 3 |
| InvestigatorAgent_h1 | 28,535 | 2 | 3 |
| InvestigatorAgent_h2 | 48,692 | 4 | 5 |
| InvestigatorAgent_h3 | 64,730 | 5 | 5 |
| EvidenceValidator | 0 | 0 | 0 |
| SynthesisAgent | 6,215 | 0 | 1 |


## Resolution

**Heal method:** scheduled  
**Recovery time:** 316.9s
