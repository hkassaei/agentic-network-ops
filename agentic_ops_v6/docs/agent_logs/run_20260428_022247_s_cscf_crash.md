# Episode Report: S-CSCF Crash

**Agent:** v6  
**Episode ID:** ep_20260428_021759_s_cscf_crash  
**Date:** 2026-04-28T02:18:01.682152+00:00  
**Duration:** 285.0s  

---

## Scenario

**Category:** container  
**Blast radius:** single_nf  
**Description:** Kill the S-CSCF (Serving-CSCF). This is the core SIP registrar and call controller. All IMS authentication stops, new registrations are impossible, and active calls will eventually drop.

## Faults Injected

- **container_kill** on `scscf`

## Baseline (Pre-Fault)

Stack phase before injection: **ready**
All containers running at baseline.

## Fault Propagation Verification

**Verdict:** ✅ `confirmed`

- **Wait:** 0s
- **Actual elapsed:** 0.0s
- **Nodes with significant deltas:** 4
- **Nodes with any drift:** 5

## Symptoms Observed

Symptoms detected: **Yes**  
Observation iterations: 1

## Anomaly Screening (Phase 0)

**ANOMALY DETECTED.** Overall anomaly score: 0.94 (threshold: 0.70, trained on 104 healthy snapshots). The current metric pattern is statistically different from the learned healthy baseline. Something in the network has changed.

The following metrics deviate from their learned-healthy baseline. Treat each as a semantic observation (meaning + numbers), not a number alone — the KB's interpretation is the authoritative reading.

- **`normalized.upf.gtp_outdatapktn3upf_per_ue`** (GTP-U downlink rate per UE (N3)) — current **0.01 packets_per_second** vs learned baseline **3.37 packets_per_second** (MEDIUM, drop)
    - **What it measures:** Health of the downlink user-plane path UPF → gNB. Typically mirrors
uplink rate during calls/data; asymmetry indicates directional
faults (e.g., UPF receiving from internet but not forwarding).
    - **Drop means:** No traffic leaving UPF toward RAN.
    - **Healthy typical range:** 0–10 packets_per_second
    - **Healthy invariant:** Per-UE — constant regardless of UE pool size. Roughly symmetric with uplink during healthy calls.

- **`normalized.upf.gtp_indatapktn3upf_per_ue`** (GTP-U uplink rate per UE (N3)) — current **0.11 packets_per_second** vs learned baseline **3.44 packets_per_second** (MEDIUM, drop)
    - **What it measures:** Health of the uplink user-plane path gNB → UPF. Drops to near-zero
during RAN or N3 outage; stays nonzero during active calls or data
sessions. Decoupled from SIP signaling (signals data plane, not
control plane).
    - **Drop means:** Data plane dead on uplink — UPF receiving no packets from gNB.
    - **Healthy typical range:** 0–10 packets_per_second
    - **Healthy invariant:** Per-UE — constant regardless of UE pool size. Rises during active VoNR calls (~100 pps for G.711 voice) and data transfer.

- **`normalized.pcscf.dialogs_per_ue`** (Active SIP dialogs per registered UE at P-CSCF) — current **0.00 count** vs learned baseline **0.59 count** (LOW, drop)
    - **What it measures:** How many calls per user are currently in progress at the P-CSCF.
Going to zero from a non-zero baseline means calls have ended
(normal) OR call setup is failing system-wide (degradation).
Together with rcv_requests_* it discriminates the two.
    - **Drop means:** No active calls. Normal at rest; suspicious if prior state had calls AND INVITE rate is also zero.
    - **Healthy typical range:** 0–1 count
    - **Healthy invariant:** Per-UE — scale-independent. 0 at rest, ~1 per active VoNR call.

- **`derived.upf_activity_during_calls`** — current **1.00** vs learned baseline **0.45** (LOW, spike). *(No KB context available — interpret from the metric name.)*


## Event Aggregation (Phase 1)

No events fired during this episode. Either no metric KB triggers matched, or the episode encountered no meaningful state transitions.

## Correlation Analysis (Phase 2)

No events fired — correlation engine had nothing to work with.

## Network Analysis (Phase 3)

**Summary:** The IMS control plane is down due to the failure of the S-CSCF container, preventing all VoNR call setups and causing a drop in user plane traffic.

**Layer status:**

| Layer | Rating | Note |
|---|---|---|
| **infrastructure** | 🟢 GREEN | No evidence of infrastructure-level faults. |
| **ran** | 🟢 GREEN | RAN appears healthy and connected to the core. |
| **core** | 🟡 YELLOW | The user plane is idle, but this is a symptom of an IMS-layer fault, not a core network failure. Core NFs are all running and connected. |
| **ims** | 🔴 RED | The S-CSCF is a critical component for call processing; its failure has halted all IMS services. |

**CORE evidence:**
- normalized.upf.gtp_outdatapktn3upf_per_ue at 0.01 pps
- normalized.upf.gtp_indatapktn3upf_per_ue at 0.11 pps

**IMS evidence:**
- S-CSCF container has exited.
- Multiple inactive links to/from S-CSCF.
- normalized.pcscf.dialogs_per_ue at 0.

**Ranked hypotheses:**

- **`h1`** (fit=0.95, nf=scscf, specificity=specific):
    - **Statement:** The S-CSCF (Serving-CSCF) container has terminated unexpectedly, breaking the IMS session control plane and preventing VoNR call setup.
    - **Falsification probes:**
        - Check the logs of the 'scscf' container to determine the reason for its exit.
        - Restart the 'scscf' container and observe if IMS registrations and call setups resume.
        - Check I-CSCF logs for errors related to forwarding requests to the S-CSCF.
- **`h2`** (fit=0.80, nf=scscf, specificity=moderate):
    - **Statement:** The IMS control plane is partitioned, preventing SIP signaling from reaching the HSS and other call-processing components, as evidenced by the failure of the S-CSCF.
    - **Falsification probes:**
        - Restore the S-CSCF and verify that end-to-end IMS registration flows complete successfully.
        - Examine P-CSCF and I-CSCF logs to trace SIP messages and confirm they fail after the I-CSCF.


## Falsification Plans (Phase 4)

**2 falsification plan(s) — one per hypothesis:**

### Plan for `h1` (target: `scscf`)

**Hypothesis:** The S-CSCF (Serving-CSCF) container has terminated unexpectedly, breaking the IMS session control plane and preventing VoNR call setup.

**Probes (3):**
1. **`get_network_status`** — Global status, check 'scscf' entry
    - *Expected if hypothesis holds:* The container status for 'scscf' will be 'exited' or another non-running state.
    - *Falsifying observation:* The 'scscf' container is in a 'running' state. This would directly contradict the hypothesis that it has terminated.
2. **`get_nf_metrics`** — icscf
    - *Expected if hypothesis holds:* Metrics on the I-CSCF will show failures to forward requests to the S-CSCF (e.g., kamailio dispatcher failures or timeouts).
    - *Falsifying observation:* I-CSCF's forwarding-related metrics are normal, with no errors indicating an inability to reach the S-CSCF. This suggests the upstream component is not seeing a failure.
3. **`get_nf_metrics`** — pyhss
    - *Expected if hypothesis holds:* Metrics on the HSS (pyhss) for Diameter Cx requests received from S-CSCF will be zero or have flatlined, as the S-CSCF is down and cannot send them.
    - *Falsifying observation:* The HSS is actively receiving and processing Diameter requests from the S-CSCF. This proves the S-CSCF is up and communicating.

*Notes:* This plan focuses on verifying the direct state of the S-CSCF container and observing the expected knock-on effects on its immediate upstream (I-CSCF) and downstream (HSS/pyhss) neighbors.

### Plan for `h2` (target: `scscf`)

**Hypothesis:** The IMS control plane is partitioned, preventing SIP signaling from reaching the HSS and other call-processing components, as evidenced by the failure of the S-CSCF.

**Probes (3):**
1. **`measure_rtt`** — From 'icscf' to 'scscf' IP
    - *Expected if hypothesis holds:* 100% packet loss or extremely high latency, indicating a network partition.
    - *Falsifying observation:* Clean RTT (<5ms) with 0% packet loss. This would prove that the network path between I-CSCF and S-CSCF is healthy, falsifying the partition hypothesis.
2. **`check_process_listeners`** — scscf
    - *Expected if hypothesis holds:* A process is running and listening on the S-CSCF's standard SIP port (e.g., 5060), but is unreachable due to the partition.
    - *Falsifying observation:* No process is listening on the expected ports. This indicates the container has crashed or the service failed to start, which contradicts the 'partition' model and favors a 'component failure' model (like h1).
3. **`measure_rtt`** — Triangulation probe: from 'icscf' to 'pyhss' IP
    - *Expected if hypothesis holds:* Clean RTT to HSS. This would help isolate the partition to be specific to the S-CSCF, rather than a general egress problem from the I-CSCF.
    - *Falsifying observation:* High packet loss. If both paths (to scscf and pyhss) from icscf are failing, it points to a problem with the icscf itself or its local network access, rather than a partition isolating the scscf.

*Notes:* This plan specifically tests the 'network partition' theory. It uses RTT probes to test connectivity directly and a process check to differentiate a running-but-isolated component from a terminated one, which is the key distinction between h2 and h1.


## Parallel Investigators (Phase 5)

**2 sub-Investigator verdict(s):** **1 NOT_DISPROVEN**, **1 DISPROVEN**

### `h1` — ✅ **NOT_DISPROVEN**

**Hypothesis:** The S-CSCF (Serving-CSCF) container has terminated unexpectedly, breaking the IMS session control plane and preventing VoNR call setup.

**Reasoning:** Direct probes confirm the central assertion of the hypothesis. `get_network_status` shows the 'scscf' container has 'exited', and `check_process_listeners` confirms it is not running. A probe to check for knock-on effects on the I-CSCF was ambiguous due to a lack of specific metrics but did not produce contradictory evidence. With two strong positive signals and no falsifying observations, the hypothesis is not disproven.

**Probes executed (3):**
- **Global status, check 'scscf' entry** ✓ CONSISTENT
    - *Tool:* `print(default_api.get_network_status())`
    - *Observation:* [EVIDENCE: get_network_status() -> "'scscf': 'exited'"]
    - *Comment:* The primary assertion of the hypothesis is directly confirmed. The S-CSCF container is not in a running state.
- **Metrics on the I-CSCF will show failures to forward requests to the S-CSCF (e.g., kamailio dispatcher failures or timeouts).** ~ AMBIGUOUS
    - *Tool:* `print(default_api.get_nf_metrics())`
    - *Observation:* [EVIDENCE: get_nf_metrics() -> "ICSCF [...] cdp:timeout = 0.0, ims_icscf:lir_timeouts = 0.0, ims_icscf:uar_timeouts = 0.0"]
    - *Comment:* The probe was intended to find forwarding failures from I-CSCF to S-CSCF. While timeout metrics were zero, these metrics pertain to the Diameter Cx interface, not SIP forwarding to S-CSCF. The observation is therefore inconclusive and does not falsify the hypothesis.
- **Metrics on the HSS (pyhss) for Diameter Cx requests received from S-CSCF will be zero or have flatlined, as the S-CSCF is down and cannot send them.** ✓ CONSISTENT
    - *Tool:* `print(default_api.check_process_listeners(container='scscf'))`
    - *Observation:* [EVIDENCE: check_process_listeners('scscf') -> 'Error response from daemon: container ... is not running']
    - *Comment:* This probe was substituted for the HSS metrics check, as the available metrics were insufficient. This check directly confirms the scscf container is not running, which is consistent with the hypothesis and provides stronger evidence than the planned but un-runnable probe.

### `h2` — ❌ **DISPROVEN**

**Hypothesis:** The IMS control plane is partitioned, preventing SIP signaling from reaching the HSS and other call-processing components, as evidenced by the failure of the S-CSCF.

**Reasoning:** The hypothesis of a network partition is disproven by direct evidence that the scscf container is not running. While the RTT probe from icscf to scscf showed 100% packet loss, consistent with a partition, the subsequent check_process_listeners probe revealed the container itself is down. This contradicts the core assumption of the partition hypothesis, which presumes a running but isolated component. The fault is a component failure, not a network partition.

**Probes executed (3):**
- **From 'icscf' to 'scscf' IP** ✓ CONSISTENT
    - *Tool:* `measure_rtt("icscf", "172.22.0.20")`
    - *Observation:* [EVIDENCE: measure_rtt("icscf", "172.22.0.20") -> "100% packet loss"]
    - *Comment:* The observation of 100% packet loss is consistent with the hypothesis of a network partition, as the S-CSCF is unreachable from the I-CSCF.
- **scscf** ✗ CONTRADICTS
    - *Tool:* `check_process_listeners("scscf")`
    - *Observation:* [EVIDENCE: check_process_listeners("scscf") -> "Error response from daemon: container 7b07ede09576a6955d8801dbf038c7e902d03474c16ab236a4991fb878f77981 is not running"]
    - *Comment:* This observation directly contradicts the hypothesis. A partition implies the component is running but unreachable; this evidence shows the scscf container is not running at all, pointing to component failure (as in hypothesis h1) rather than a network issue.
- **Triangulation probe: from 'icscf' to 'pyhss' IP** ✓ CONSISTENT
    - *Tool:* `measure_rtt("icscf", "172.22.0.18")`
    - *Observation:* [EVIDENCE: measure_rtt("icscf", "172.22.0.18") -> "3 packets transmitted, 3 received, 0% packet loss, time 2037ms"]
    - *Comment:* A clean RTT from icscf to pyhss demonstrates that the icscf's network connectivity is generally healthy. This, combined with the other probes, isolates the problem to the scscf itself, rather than a broader network issue affecting the icscf.

**Alternative suspects:** scscf


## Evidence Validation (Phase 6)

**Overall verdict:** clean
**Overall confidence:** high

**Per sub-Investigator:**

| Agent | Tool Calls | Citations | Verdict | Confidence |
|---|---|---|---|---|
| `InvestigatorAgent_h1` | 3 | 3/3 | clean | high |
| `InvestigatorAgent_h2` | 4 | 3/3 | clean | high |


## Ground Truth

**Failure domain:** ims_signaling  
**Protocol impact:** SIP  
**Affected components:** scscf  
**Severity:** degraded

## Agent Diagnosis

**Model:** v6-adk/gemini-2.5-flash+gemini-2.5-pro  
**Time to diagnosis:** 150.8s

### Prompt to RCA Agent

```
The 5G SA + IMS stack is experiencing issues. Investigate and diagnose the root cause.
```

**Diagnosis:**

> ### causes
> - **summary**: The IMS control plane is down due to the unexpected termination of the S-CSCF container, which has halted all VoNR session processing and registrations.
> - **timeline**:
>     1. The S-CSCF container, a critical component for IMS session management, terminated unexpectedly.
>     2. As a result, the I-CSCF could no longer forward SIP signaling requests to an available S-CSCF.
>     3. All subsequent VoNR call setups and new user registrations began to fail, leading to a complete drop in IMS-related user plane traffic.
> - **root_cause**: The S-CSCF (Serving-CSCF) container has terminated unexpectedly, breaking the IMS session control plane and preventing VoNR call setup. The `primary_suspect_nf` is `scscf`.
> - **affected_components**:
>     - `scscf`: Root Cause
>     - `icscf`: Secondary
>     - `upf`: Symptomatic
> - **recommendation**: An operator should investigate the `scscf` container's logs to determine the cause of its unexpected exit. After the component is restored, verify that end-to-end IMS registrations and call setups resume successfully.
> - **confidence**: high
> - **explanation**: The investigation confirmed hypothesis `h1` and disproved `h2`. Hypothesis `h2` suggested a network partition was isolating the S-CSCF, but probes revealed the `scscf` container was not running at all, which contradicts a partition scenario. Instead, direct evidence from `get_network_status` showed the container was in an 'exited' state, and `check_process_listeners` confirmed no processes were listening. This directly proves hypothesis `h1`, that the unexpected termination of the S-CSCF component is the root cause of the IMS service outage.

### Scoring Breakdown

**Overall score: 100%**

**Scorer assessment:** The agent provided an excellent and highly accurate diagnosis, correctly identifying the root cause, affected component, severity, fault type, and layer, with appropriate confidence.

| Dimension | Result | Rationale |
|-----------|--------|-----------|
| Root cause correct | Yes | The agent correctly identified the S-CSCF as the root cause and its unexpected termination as the failure mode, which aligns with the simulated 'S-CSCF crash' and 'component unreachable' scenario. |
| Component overlap | 100% | The agent explicitly listed 'scscf' as the 'Root Cause' in its 'affected_components' list, which is the primary affected component. |
| Severity correct | Yes | The agent described the impact as the 'IMS control plane is down', 'halted all VoNR session processing and registrations', and 'preventing VoNR call setup', which accurately reflects a complete outage due to a component crash. |
| Fault type identified | Yes | The agent identified the fault type as the 'S-CSCF container has terminated unexpectedly', 'container was in an 'exited' state', and 'no processes were listening', which directly corresponds to a 'component unreachable/not responding' fault. |
| Layer accuracy | Yes | The agent correctly attributed the failure to the 'ims' layer, rating it 'red' and providing evidence of the S-CSCF container's exited state, which matches the ground truth that 'scscf' belongs to the 'ims' layer. |
| Confidence calibrated | Yes | The agent stated 'high' confidence, which is appropriate given the accuracy and detailed evidence provided in the diagnosis (e.g., container 'exited' state, no processes listening). |

**Ranking position:** #1 — The agent provided a single, clear root cause, which was correct.


### Token Usage

| Metric | Count |
|--------|-------|
| Input tokens | 132,189 |
| Output tokens | 3,756 |
| Thinking tokens | 12,514 |
| **Total tokens** | **148,459** |

**Per-phase breakdown:**

| Phase | Tokens | Tool Calls | LLM Calls |
|-------|--------|------------|-----------|
| AnomalyScreener | 0 | 0 | 0 |
| EventAggregator | 0 | 0 | 0 |
| CorrelationAnalyzer | 0 | 0 | 0 |
| NetworkAnalystAgent | 36,337 | 4 | 3 |
| InstructionGeneratorAgent | 16,619 | 1 | 2 |
| InvestigatorAgent_h1 | 43,981 | 3 | 4 |
| InvestigatorAgent_h2 | 47,067 | 4 | 5 |
| EvidenceValidator | 0 | 0 | 0 |
| SynthesisAgent | 4,455 | 0 | 1 |


## Resolution

**Heal method:** scheduled  
**Recovery time:** 285.0s
