# Episode Report: S-CSCF Crash

**Agent:** v6  
**Episode ID:** ep_20260424_192425_s_cscf_crash  
**Date:** 2026-04-24T19:24:27.275285+00:00  
**Duration:** 246.1s  

---

## Scenario

**Category:** container  
**Blast radius:** single_nf  
**Description:** Kill the S-CSCF (Serving-CSCF). This is the core SIP registrar and call controller. All IMS authentication stops, new registrations are impossible, and active calls will eventually drop.

## Faults Injected

- **container_kill** on `scscf`

## Baseline (Pre-Fault)

Stack phase before injection: **ready**
All containers running at baseline.

## Fault Propagation Verification

**Verdict:** ✅ `confirmed`

- **Wait:** 0s
- **Actual elapsed:** 0.0s
- **Nodes with significant deltas:** 4
- **Nodes with any drift:** 5

## Symptoms Observed

Symptoms detected: **Yes**  
Observation iterations: 1

## Anomaly Screening (Phase 0)

**ANOMALY DETECTED.** Overall anomaly score: 0.99 (threshold: 0.70, trained on 209 healthy snapshots). The current metric pattern is statistically different from the learned healthy baseline. Something in the network has changed.

The following metrics deviate from their learned-healthy baseline. Treat each as a semantic observation (meaning + numbers), not a number alone — the KB's interpretation is the authoritative reading.

- **`derived.upf_activity_during_calls`** — current **1.00** vs learned baseline **0.09** (HIGH, spike). *(No KB context available — interpret from the metric name.)*

- **`normalized.upf.gtp_outdatapktn3upf_per_ue`** (GTP-U downlink rate per UE (N3)) — current **0.01 packets_per_second** vs learned baseline **6.08 packets_per_second** (HIGH, drop)
    - **What it measures:** Health of the downlink user-plane path UPF → gNB. Typically mirrors
uplink rate during calls/data; asymmetry indicates directional
faults (e.g., UPF receiving from internet but not forwarding).
    - **Drop means:** No traffic leaving UPF toward RAN.
    - **Healthy typical range:** 0–10 packets_per_second
    - **Healthy invariant:** Per-UE — constant regardless of UE pool size. Roughly symmetric with uplink during healthy calls.

- **`normalized.upf.gtp_indatapktn3upf_per_ue`** (GTP-U uplink rate per UE (N3)) — current **0.10 packets_per_second** vs learned baseline **6.15 packets_per_second** (HIGH, drop)
    - **What it measures:** Health of the uplink user-plane path gNB → UPF. Drops to near-zero
during RAN or N3 outage; stays nonzero during active calls or data
sessions. Decoupled from SIP signaling (signals data plane, not
control plane).
    - **Drop means:** Data plane dead on uplink — UPF receiving no packets from gNB.
    - **Healthy typical range:** 0–10 packets_per_second
    - **Healthy invariant:** Per-UE — constant regardless of UE pool size. Rises during active VoNR calls (~100 pps for G.711 voice) and data transfer.

- **`normalized.smf.bearers_per_ue`** (Active QoS bearers per UE) — current **2.50 count** vs learned baseline **4.15 count** (MEDIUM, shift)
    - **What it measures:** Per-UE count of active QoS bearers. Baseline reflects default
bearers; increments during VoNR calls indicate dedicated voice
bearers being set up. Drop during an active call = dedicated
bearer torn down unexpectedly (voice will fail).
    - **Shift means:** Expected during VoNR calls (1 extra bearer per active call).
    - **Healthy typical range:** 2–3.5 count
    - **Healthy invariant:** At rest: equals configured default bearers (typically 2 per UE).
During active VoNR call: +1 per caller. The per-UE ratio is the
invariant; absolute count scales with UE pool.

- **`normalized.pcscf.dialogs_per_ue`** (Active SIP dialogs per registered UE at P-CSCF) — current **0.00 count** vs learned baseline **1.58 count** (MEDIUM, drop)
    - **What it measures:** How many calls per user are currently in progress at the P-CSCF.
Going to zero from a non-zero baseline means calls have ended
(normal) OR call setup is failing system-wide (degradation).
Together with rcv_requests_* it discriminates the two.
    - **Drop means:** No active calls. Normal at rest; suspicious if prior state had calls AND INVITE rate is also zero.
    - **Healthy typical range:** 0–1 count
    - **Healthy invariant:** Per-UE — scale-independent. 0 at rest, ~1 per active VoNR call.


## Event Aggregation (Phase 1)

No events fired during this episode. Either no metric KB triggers matched, or the episode encountered no meaningful state transitions.

## Correlation Analysis (Phase 2)

No events fired — correlation engine had nothing to work with.

## Network Analysis (Phase 3)

**Summary:** The S-CSCF has exited, causing a complete failure of the IMS signaling plane which prevents VoNR call setup and results in no user plane traffic.

**Layer status:**

| Layer | Rating | Note |
|---|---|---|
| **infrastructure** | 🟢 GREEN | Core infrastructure is healthy. The S-CSCF container exit is treated as an application-level fault. |
| **ran** | 🟢 GREEN | RAN appears healthy; UEs are connected to the AMF. |
| **core** | 🟡 YELLOW | Core network functions are running, but the data plane (UPF) is inactive and bearer count is low because of the upstream IMS failure. |
| **ims** | 🔴 RED | A critical IMS component, the S-CSCF, is down, breaking the entire VoNR call signaling flow. |

**CORE evidence:**
- normalized.upf.gtp_outdatapktn3upf_per_ue drop
- normalized.upf.gtp_indatapktn3upf_per_ue drop
- normalized.smf.bearers_per_ue shift

**IMS evidence:**
- scscf container has exited
- Multiple inactive links to/from S-CSCF
- normalized.pcscf.dialogs_per_ue drop

**Ranked hypotheses:**

- **`h1`** (fit=0.95, nf=scscf, specificity=specific):
    - **Statement:** The Serving-CSCF (S-CSCF) container has exited, which constitutes a total failure for the IMS control plane. As the I-CSCF cannot forward SIP requests to a non-existent S-CSCF, all VoNR call setups are failing. This in turn prevents the creation of dedicated bearers and the activation of the user data plane, explaining the lack of GTP-U traffic.
    - **Falsification probes:**
        - Check the logs of the 'scscf' container to determine the cause of its exit.
        - Attempt to restart the 'scscf' container and observe if IMS registration and call setup functionality is restored.
        - From the 'icscf' container, attempt to connect to the 'scscf' service hostname/IP on the SIP port to confirm unreachability.


## Falsification Plans (Phase 4)

*No output produced.*

## Parallel Investigators (Phase 5)

**1 sub-Investigator verdict(s):** **1 INCONCLUSIVE**

### `h1` — ❓ **INCONCLUSIVE**

**Hypothesis:** The Serving-CSCF (S-CSCF) container has exited, which constitutes a total failure for the IMS control plane. As the I-CSCF cannot forward SIP requests to a non-existent S-CSCF, all VoNR call setups are failing. This in turn prevents the creation of dedicated bearers and the activation of the user data plane, explaining the lack of GTP-U traffic.

**Reasoning:** No falsification plan was generated for this hypothesis.


## Evidence Validation (Phase 6)

**Overall verdict:** severe
**Overall confidence:** none

**Per sub-Investigator:**

| Agent | Tool Calls | Citations | Verdict | Confidence |
|---|---|---|---|---|
| `InvestigatorAgent_h1` | 0 | 0/0 | severe | none |
|  |  |  |  | *ZERO tool calls — all citations fabricated* |


## Ground Truth

**Failure domain:** ims_signaling  
**Protocol impact:** SIP  
**Affected components:** scscf  
**Severity:** degraded

## Agent Diagnosis

**Model:** v6-adk/gemini-2.5-flash+gemini-2.5-pro  
**Time to diagnosis:** 108.4s

### Prompt to RCA Agent

```
The 5G SA + IMS stack is experiencing issues. Investigate and diagnose the root cause.
```

**Diagnosis:**

> ### causes
> - **summary**: The Serving-CSCF (S-CSCF) has likely exited, causing a complete failure of the IMS signaling plane which prevents VoNR call setup and results in no user plane traffic.
> - **timeline**:
>     1. A specific event timeline could not be constructed as no events were fired by the correlation engine.
> - **root_cause**: The suspected root cause is a failure of the Serving-CSCF (S-CSCF). `primary_suspect_nf: scscf`.
> - **affected_components**:
>     - `scscf`: Root Cause (Suspected)
>     - `icscf`: Secondary (Unable to forward SIP requests to the S-CSCF)
>     - `upf`: Symptomatic (No user plane traffic as no dedicated bearers are established for calls)
> - **recommendation**: The following manual verification steps are required:
>     1. Check the logs of the `scscf` container to determine why it may have exited.
>     2. Verify the current running state of the `scscf` container or pod.
>     3. From within the `icscf` container, verify network connectivity to the `scscf` service on its SIP port to confirm whether it is reachable.
> - **confidence**: low
> - **explanation**: The leading hypothesis (h1) posits that a failure of the S-CSCF is the root cause of the IMS signaling plane outage. This aligns well with the observed symptoms. However, the automated investigation was inconclusive because the investigator agent failed to execute any diagnostic commands to validate this hypothesis. Due to this severe failure in the evidence gathering process, as flagged by the Evidence Validator, confidence in this automated diagnosis is **low**. The proposed root cause remains unverified and requires manual investigation. *[Evidence Validator caveat]: The evidence validation process reported a 'severe' failure, noting that the investigator agent made zero tool calls.*

### Scoring Breakdown

**Overall score: 100%**

**Scorer assessment:** The agent accurately diagnosed the S-CSCF crash as the root cause, correctly identifying the affected component, severity, and fault type, with proper layer attribution and well-calibrated low confidence due to a lack of automated evidence gathering.

| Dimension | Result | Rationale |
|-----------|--------|-----------|
| Root cause correct | Yes | The agent correctly identified the 'failure of the Serving-CSCF (S-CSCF)' as the root cause, which directly matches the simulated S-CSCF crash and its observable effect of the component being unreachable. |
| Component overlap | 100% | The agent correctly identified 'scscf' as the 'Root Cause (Suspected)' in its affected_components list, which is the primary affected component. |
| Severity correct | Yes | The agent described the failure as a 'complete failure of the IMS signaling plane', which accurately reflects the 'component completely unreachable' severity of the simulated S-CSCF crash. |
| Fault type identified | Yes | The agent identified a 'failure of the Serving-CSCF (S-CSCF)' and an 'IMS signaling plane outage', which aligns with the observable fault type of a component being down/unreachable. |
| Layer accuracy | Yes | The agent's network analysis correctly rated the 'ims' layer as 'red' and provided evidence of 'scscf container has exited', aligning with the ground truth that 'scscf' belongs to the 'ims' layer. |
| Confidence calibrated | Yes | The agent stated 'low' confidence and provided a clear explanation that the automated investigation failed to execute diagnostic commands, leading to an unverified hypothesis. This demonstrates good calibration, as the agent acknowledges the lack of evidence despite the correctness of its hypothesis. |

**Ranking position:** #1 — The agent identified a single primary root cause, which was correct.


### Token Usage

| Metric | Count |
|--------|-------|
| Input tokens | 38,762 |
| Output tokens | 1,060 |
| Thinking tokens | 8,543 |
| **Total tokens** | **48,365** |

**Per-phase breakdown:**

| Phase | Tokens | Tool Calls | LLM Calls |
|-------|--------|------------|-----------|
| AnomalyScreener | 0 | 0 | 0 |
| EventAggregator | 0 | 0 | 0 |
| CorrelationAnalyzer | 0 | 0 | 0 |
| NetworkAnalystAgent | 38,411 | 4 | 3 |
| InstructionGeneratorAgent | 6,231 | 0 | 1 |
| EvidenceValidator | 0 | 0 | 0 |
| SynthesisAgent | 3,723 | 0 | 1 |


## Resolution

**Heal method:** scheduled  
**Recovery time:** 246.1s
