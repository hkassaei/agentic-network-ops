# Episode Report: MongoDB Gone

**Agent:** v7  
**Episode ID:** ep_20260510_193011_mongodb_gone  
**Date:** 2026-05-10T19:30:12.654557+00:00  
**Duration:** 449.9s  

---

## Scenario

**Category:** container  
**Blast radius:** global  
**Description:** Kill MongoDB — the 5G core subscriber data store. UDR loses its backend, new PDU sessions cannot be created, and subscriber queries fail. Existing sessions may survive briefly.

## Faults Injected

- **container_kill** on `mongo`

## Baseline (Pre-Fault)

Stack phase before injection: **ready**
All containers running at baseline.

## Fault Propagation Verification

**Verdict:** ✅ `confirmed`

- **Wait:** 0s
- **Actual elapsed:** 0.0s
- **Nodes with significant deltas:** 3
- **Nodes with any drift:** 4

## Symptoms Observed

Symptoms detected: **Yes**  
Observation iterations: 1

## Anomaly Screening (Phase 0)

**ANOMALY DETECTED.** Overall anomaly score: 39.99 (per-bucket threshold: 26.31, context bucket (0, 1), trained on 323 healthy snapshots). The current metric pattern is statistically different from the learned healthy baseline. Something in the network has changed.

The following metrics deviate from their learned-healthy baseline. Treat each as a semantic observation (meaning + numbers), not a number alone — the KB's interpretation is the authoritative reading.

- **`derived.pcscf_sip_error_ratio`** (P-CSCF SIP error response ratio) — current **0.33 ratio** vs learned baseline **0.00 ratio** (MEDIUM, spike)
    - **What it measures:** Proportion of SIP responses that are errors. Zero is the healthy
baseline; any sustained non-zero value means P-CSCF or something
downstream is rejecting requests.
    - **Spike means:** Errors flowing back — downstream CSCFs or HSS rejecting.
    - **Healthy typical range:** 0–0 ratio
    - **Healthy invariant:** Zero in healthy operation.

- **`normalized.icscf.cdp_replies_per_ue`** (I-CSCF Diameter reply rate per UE) — current **0.00 replies_per_second_per_ue** vs learned baseline **0.03 replies_per_second_per_ue** (MEDIUM, drop)
    - **What it measures:** Liveness of the I-CSCF↔HSS Cx path. Drops to 0 when HSS is unreachable OR when no signaling is occurring at the I-CSCF (idle or upstream P-CSCF partitioned).
    - **Drop means:** No Cx replies in the window. Could be healthy idle OR a Cx-path fault.
    - **Healthy typical range:** 0–1 replies_per_second_per_ue

- **`normalized.icscf.core:rcv_requests_register_per_ue`** (SIP REGISTER rate per UE at I-CSCF) — current **0.02 requests_per_second** vs learned baseline **0.06 requests_per_second** (MEDIUM, drop)
    - **What it measures:** Health of the P-CSCF → I-CSCF forwarding path (Mw interface). When
this drops to zero while P-CSCF REGISTER rate is still non-zero,
it's the SIGNATURE of an IMS partition between P-CSCF and I-CSCF.
    - **Drop means:** Either UEs not registering at all, or P-CSCF isolated from I-CSCF.
    - **Healthy typical range:** 0–0.5 requests_per_second
    - **Healthy invariant:** Per-UE rate. Should closely track ims.pcscf.rcv_requests_register_per_ue.

- **`normalized.pcscf.core:rcv_requests_invite_per_ue`** (SIP INVITE rate per UE at P-CSCF) — current **0.02 requests_per_second** vs learned baseline **0.00 requests_per_second** (MEDIUM, spike)
    - **What it measures:** Call attempt rate from registered UEs. Unlike REGISTER (periodic),
INVITEs only fire when UEs place calls. Zero is normal during
quiet periods; nonzero INVITE with zero dialogs is the signature
of call setup failure.
    - **Spike means:** Fewer call attempts.
    - **Healthy typical range:** 0–0.2 requests_per_second
    - **Healthy invariant:** Per-UE rate.

- **`normalized.pcscf.core:rcv_requests_register_per_ue`** (SIP REGISTER rate per UE at P-CSCF) — current **0.02 requests_per_second** vs learned baseline **0.06 requests_per_second** (MEDIUM, drop)
    - **What it measures:** How actively UEs are refreshing their IMS registrations with the
P-CSCF. REGISTERs arrive periodically (re-registration timer) plus
at attach. Sustained zero means UEs cannot reach P-CSCF OR the
UE-to-network SIP path is broken.
    - **Drop means:** No REGISTERs flowing. Unusual unless UEs are all deregistered.
    - **Healthy typical range:** 0–0.5 requests_per_second
    - **Healthy invariant:** Per-UE rate — same value at any deployment scale.

- **`normalized.scscf.core:rcv_requests_register_per_ue`** (SIP REGISTER rate per UE at S-CSCF) — current **0.02 requests_per_second** vs learned baseline **0.06 requests_per_second** (MEDIUM, drop)
    - **What it measures:** Health of the I-CSCF → S-CSCF forwarding path. Drop to zero while
I-CSCF is receiving REGISTERs = S-CSCF-side issue (crashed, or
I-CSCF → S-CSCF path broken).
    - **Drop means:** S-CSCF isolated or not running.
    - **Healthy typical range:** 0–0.5 requests_per_second
    - **Healthy invariant:** Per-UE rate. Tracks icscf.register rate.

- **`normalized.smf.bearers_per_ue`** (Active QoS bearers per UE) — current **2.50 count** vs learned baseline **2.48 count** (LOW, shift)
    - **What it measures:** Per-UE count of active QoS bearers. Baseline reflects default
bearers; increments during VoNR calls indicate dedicated voice
bearers being set up. Drop during an active call = dedicated
bearer torn down unexpectedly (voice will fail).
    - **Shift means:** Expected during VoNR calls (1 extra bearer per active call).
    - **Healthy typical range:** 2–3.5 count
    - **Healthy invariant:** At rest: equals configured default bearers (typically 2 per UE).
During active VoNR call: +1 per caller. The per-UE ratio is the
invariant; absolute count scales with UE pool.

- **`normalized.upf.gtp_outdatapktn3upf_per_ue`** (GTP-U downlink rate per UE (N3)) — current **0.10 packets_per_second** vs learned baseline **1.45 packets_per_second** (LOW, drop)
    - **What it measures:** Health of the downlink user-plane path UPF → gNB. Drops to
near-zero during a downlink-affecting RAN, N3, or UPF
outage; stays nonzero during active calls or data sessions.
Decoupled from SIP signaling. The cross-direction
relationship to uplink (gtp_indatapktn3upf_per_ue) reflects
current traffic profile, NOT data-plane health — voice
with NULL_AUDIO, signaling-only chatter, idle UEs, and
asymmetric data sessions all produce persistent in/out
imbalance under healthy operation. Use this metric for
same-direction collapse detection (a known-active downlink
collapsing toward zero); do NOT infer loss from
uplink-vs-downlink asymmetry — see stack rule
upf_counters_are_directional.
    - **Drop means:** No traffic leaving UPF toward RAN.
    - **Healthy typical range:** 0–10 packets_per_second
    - **Healthy invariant:** Per-UE rate. Constant regardless of UE pool size. Uplink
(gtp_indatapktn3upf_per_ue) and downlink are independent
directions whose ratio is determined entirely by the current
traffic profile — voice with NULL_AUDIO (this testbed),
signaling-only chatter, idle UEs, and asymmetric data
sessions all produce persistent in/out imbalance under
healthy operation. Asymmetry between uplink and downlink
rates is NEVER, by itself, evidence of packet loss — not
at any magnitude, not under any traffic mix. To detect
actual loss, use the methods listed in stack rule
`upf_counters_are_directional` (same-direction rate
comparison, RTCP loss_ratio at RTPEngine, or tc qdisc drop
counters). Rate-based metrics like this one are usually
MORE informative than the underlying lifetime cumulative
counter for current-state failure detection.

- **`normalized.scscf.cdp_replies_per_ue`** (S-CSCF CDP Diameter replies per UE) — current **0.03 replies_per_second_per_ue** vs learned baseline **0.06 replies_per_second_per_ue** (LOW, shift)
    - **What it measures:** Active S-CSCF Diameter traffic with HSS. Near-zero when registrations idle OR HSS partition.
    - **Shift means:** Diameter peering loss with HSS.
    - **Healthy typical range:** 0–1 replies_per_second_per_ue
    - **Healthy invariant:** Per-UE rate; varies with registration/auth load.

- **`normalized.upf.gtp_indatapktn3upf_per_ue`** (GTP-U uplink rate per UE (N3)) — current **0.15 packets_per_second** vs learned baseline **1.45 packets_per_second** (LOW, drop)
    - **What it measures:** Health of the uplink user-plane path gNB → UPF. Drops to near-zero
during RAN or N3 outage; stays nonzero during active calls or data
sessions. Decoupled from SIP signaling (signals data plane, not
control plane).
    - **Drop means:** Data plane dead on uplink — UPF receiving no packets from gNB.
    - **Healthy typical range:** 0–10 packets_per_second
    - **Healthy invariant:** Per-UE rate. Constant regardless of UE pool size. Rises during
active VoNR calls (~100 pps for G.711 voice) and data transfer.
Uplink and downlink (gtp_outdatapktn3upf_per_ue) are independent
directions whose ratio is determined entirely by the current
traffic profile — voice with NULL_AUDIO (this testbed),
signaling-only chatter, idle UEs, and asymmetric data sessions
all produce persistent in/out imbalance under healthy operation.
Asymmetry between uplink and downlink rates is NEVER, by itself,
evidence of packet loss — not at any magnitude, not under any
traffic mix. To detect actual loss, use the methods listed in
stack rule `upf_counters_are_directional` (same-direction rate
comparison, RTCP loss_ratio at RTPEngine, or tc qdisc drop
counters). Rate-based metrics like this one are usually MORE
informative than the underlying lifetime cumulative counter for
current-state failure detection.


## Symptom Classifier (Phase 0.5)

**Label:** `mixed`  
**Flag counts:** transport=2, application=2, ambiguous=6

### Transport-bucket flags (2)

| Metric | Direction | Score | KB-driven reason |
|---|---|---:|---|
| `normalized.upf.gtp_outdatapktn3upf_per_ue` | drop | 3.50 | KB-labeled transport: core.upf.gtp_outdatapktn3upf_per_ue (drop, score=3.50) |
| `normalized.upf.gtp_indatapktn3upf_per_ue` | drop | 2.03 | KB-labeled transport: core.upf.gtp_indatapktn3upf_per_ue (drop, score=2.03) |

### Application-bucket flags (2)

| Metric | Direction | Score | KB-driven reason |
|---|---|---:|---|
| `derived.pcscf_sip_error_ratio` | spike | 4.59 | KB-labeled application: ims.pcscf.sip_error_ratio (spike, score=4.59) |
| `normalized.smf.bearers_per_ue` | shift | 3.90 | KB-labeled application: core.smf.bearers_per_ue (shift, score=3.90) |

### Ambiguous-bucket flags (6)

| Metric | Direction | Score | KB-driven reason |
|---|---|---:|---|
| `normalized.icscf.cdp_replies_per_ue` | drop | 4.59 | KB-labeled mixed: ims.icscf.cdp_replies_per_ue (drop, score=4.59) |
| `normalized.icscf.core:rcv_requests_register_per_ue` | drop | 4.59 | KB-labeled mixed: ims.icscf.rcv_requests_register_per_ue (drop, score=4.59) |
| `normalized.pcscf.core:rcv_requests_invite_per_ue` | spike | 4.59 | KB-labeled mixed: ims.pcscf.rcv_requests_invite_per_ue (spike, score=4.59) |
| `normalized.pcscf.core:rcv_requests_register_per_ue` | drop | 4.59 | KB-labeled mixed: ims.pcscf.rcv_requests_register_per_ue (drop, score=4.59) |
| `normalized.scscf.core:rcv_requests_register_per_ue` | drop | 4.59 | KB-labeled mixed: ims.scscf.rcv_requests_register_per_ue (drop, score=4.59) |
| `normalized.scscf.cdp_replies_per_ue` | shift | 2.99 | KB-labeled mixed: ims.scscf.cdp_replies_per_ue (shift, score=2.99) |

**Rationale:**

```
label=mixed. Both transport-layer (2) and application-layer (2) signals are load-bearing. Path walk runs first; falls through to the application-layer pipeline if the walk produces null localization.

Transport signals: normalized.upf.gtp_outdatapktn3upf_per_ue (drop, score=3.50) — KB-labeled transport: core.upf.gtp_outdatapktn3upf_per_ue (drop, score=3.50); normalized.upf.gtp_indatapktn3upf_per_ue (drop, score=2.03) — KB-labeled transport: core.upf.gtp_indatapktn3upf_per_ue (drop, score=2.03)

Application signals: derived.pcscf_sip_error_ratio (spike, score=4.59) — KB-labeled application: ims.pcscf.sip_error_ratio (spike, score=4.59); normalized.smf.bearers_per_ue (shift, score=3.90) — KB-labeled application: core.smf.bearers_per_ue (shift, score=3.90)

Ambiguous signals: normalized.icscf.cdp_replies_per_ue (drop, score=4.59) — KB-labeled mixed: ims.icscf.cdp_replies_per_ue (drop, score=4.59); normalized.icscf.core:rcv_requests_register_per_ue (drop, score=4.59) — KB-labeled mixed: ims.icscf.rcv_requests_register_per_ue (drop, score=4.59); normalized.pcscf.core:rcv_requests_invite_per_ue (spike, score=4.59) — KB-labeled mixed: ims.pcscf.rcv_requests_invite_per_ue (spike, score=4.59); normalized.pcscf.core:rcv_requests_register_per_ue (drop, score=4.59) — KB-labeled mixed: ims.pcscf.rcv_requests_register_per_ue (drop, score=4.59); normalized.scscf.core:rcv_requests_register_per_ue (drop, score=4.59) — KB-labeled mixed: ims.scscf.rcv_requests_register_per_ue (drop, score=4.59) [+1 more]
```

## Transport-Layer Route (Phase 0.6)

### Resolver

**Flow:** `data_pdu_session_user_traffic` (Data PDU Session — User Traffic)  
**Direction:** both  
**Hop count:** 11

**Candidates considered:**

| Flow | Score |
|---|---:|
| `data_pdu_session_user_traffic` ← chosen | 13 |
| `vonr_media` | 13 |
| `ims_registration` | 4 |
| `vonr_call_teardown` | 4 |
| `vonr_call_setup` | 4 |

**Rationale:**

```
Resolved transport path to flow `data_pdu_session_user_traffic` (score=13, 11 hops on the walk). Load-bearing components: ['icscf', 'pcscf', 'scscf', 'smf', 'upf']. Other candidate flows considered: vonr_media=13, ims_registration=4, vonr_call_teardown=4, vonr_call_setup=4.
```

### Walker

**Status:** ⚠️ **null localization**
**Window:** 5s  
**Walked flow:** `data_pdu_session_user_traffic`

**Per-hop results:**

| # | Node | Kind | Iface | Attribution | Detail |
|---:|---|---|---|---|---|
| 0 | `e2e_ue1` | container | `eth0` | `clean` | _clean_ |
| 1 | `docker_open5gs_default` | docker_bridge | `br-docker_open5gs` | `clean` | _clean_ |
| 2 | `nr_gnb` | container | `eth0` | `clean` | _clean_ |
| 3 | `docker_open5gs_default` | docker_bridge | `br-docker_open5gs` | `clean` | _clean_ |
| 4 | `upf` | container | `eth0` | `clean` | _clean_ |
| 5 | `internet` | external_network | `eth0` | `inconclusive` | _no_prober_registered_: "no HopProber registered for kind='external_network'; registered kinds: ['contai |
| 6 | `upf` | container | `eth0` | `clean` | _clean_ |
| 7 | `docker_open5gs_default` | docker_bridge | `br-docker_open5gs` | `clean` | _clean_ |
| 8 | `nr_gnb` | container | `eth0` | `clean` | _clean_ |
| 9 | `docker_open5gs_default` | docker_bridge | `br-docker_open5gs` | `clean` | _clean_ |
| 10 | `e2e_ue1` | container | `eth0` | `clean` | _clean_ |

### Localized Synthesis

*Walker found no hop with attribution. Phase 0.6 returned None and the orchestrator fell through to the application-layer pipeline (Phases 1-7) below — the diagnosis you see in `Agent Diagnosis` came from that fallback path, not from Phase 0.6.*

## Event Aggregation (Phase 1)

No events fired during this episode. Either no metric KB triggers matched, or the episode encountered no meaningful state transitions.

## Correlation Analysis (Phase 2)

No events fired — correlation engine had nothing to work with.

## Network Analysis (Phase 3)

**Summary:** The IMS stack is failing at the P-CSCF due to a downstream dependency failure in the PCF, which has lost its database backend (MongoDB).

**Layer status:**

| Layer | Rating | Note |
|---|---|---|
| **infrastructure** | 🔴 RED | The MongoDB container, a critical database dependency for several NFs, is down. This is the likely root cause. |
| **ran** | 🟢 GREEN | No evidence of RAN-layer issues. UEs are attempting to register. |
| **core** | 🔴 RED | The PCF is unable to function correctly due to the loss of its MongoDB backend. This is causing it to reject policy authorization requests from the IMS layer. |
| **ims** | 🔴 RED | The IMS layer is symptomatic. P-CSCF is receiving errors from the core (PCF) and propagating them as SIP errors. Downstream IMS components are starved of traffic. |

**INFRASTRUCTURE evidence:**
- MongoDB container has exited.

**CORE evidence:**
- PCF has an inactive link to the exited MongoDB.
- The causal chain 'subscriber_data_store_unavailable' links MongoDB failure to PCF failure.

**IMS evidence:**
- derived.pcscf_sip_error_ratio was 0.33, indicating SIP-level failures.
- normalized.icscf.cdp_replies_per_ue is 0, indicating a breakdown in IMS registration flow.
- normalized.icscf.core:rcv_requests_register_per_ue is 0, indicating no registrations are reaching the I-CSCF.

**Ranked hypotheses:**

- **`h1`** (fit=0.90, nf=pcf, specificity=specific):
    - **Statement:** The PCF is the source of the anomaly; its failure to access the unavailable MongoDB is causing it to reject Rx requests from the P-CSCF, leading to SIP registration and call setup failures.
    - **Supporting events:** `derived.pcscf_sip_error_ratio`, `mongo container exited`
    - **Falsification probes:**
        - Check PCF logs for errors related to MongoDB connectivity.
        - Restart the MongoDB container and observe if SIP errors at the P-CSCF cease and registration rates normalize.
        - Check if PCF is responding to its HTTP health check endpoint.
- **`h2`** (fit=0.40, nf=pyhss, specificity=moderate):
    - **Statement:** The HSS is the source of the anomaly; it is unresponsive to Diameter Cx requests from the I-CSCF, causing a breakdown in the IMS registration and authentication flow.
    - **Supporting events:** `normalized.icscf.cdp_replies_per_ue`
    - **Falsification probes:**
        - Measure RTT and packet loss from the I-CSCF to the HSS.
        - Check HSS logs for any error messages or signs of overload.
        - Confirm that I-CSCF is sending Diameter requests to HSS using tcpdump.


## Falsification Plans (Phase 4)

**2 falsification plan(s) — one per hypothesis:**

### Plan for `h1` (target: `pcf`)

**Hypothesis:** The PCF is the source of the anomaly; its failure to access the unavailable MongoDB is causing it to reject Rx requests from the P-CSCF, leading to SIP registration and call setup failures.

**Probes (3):**
1. **`get_network_status`** — all
    - *Expected if hypothesis holds:* The 'pcf' container is in a running state, while the 'mongo' container is in an exited or otherwise non-running state.
    - *Falsifying observation:* The 'pcf' container is not in a running state, or the 'mongo' container is running correctly.
2. **`check_process_listeners`** — pcf
    - *Expected if hypothesis holds:* The PCF process is listening on its Rx (HTTP/2) interface port.
    - *Falsifying observation:* The PCF process is not listening on its Rx interface port.
3. **`get_diagnostic_metrics`** — pcf
    - *Expected if hypothesis holds:* PCF metrics show a high rate of errors related to database connectivity or failures in processing Rx requests.
    - *Falsifying observation:* PCF metrics show no database-related errors and a normal rate of successfully processed Rx requests.

*Notes:* This plan tests the primary hypothesis from the Network Analyst. The core idea is that PCF is running but is unhealthy due to its backend MongoDB dependency being down, causing it to reject policy requests from the P-CSCF.

### Plan for `h2` (target: `pyhss`)

**Hypothesis:** The HSS is the source of the anomaly; it is unresponsive to Diameter Cx requests from the I-CSCF, causing a breakdown in the IMS registration and authentication flow.

**Probes (3):**
1. **`measure_rtt`** — from: icscf, to: pyhss
    - *Expected if hypothesis holds:* High RTT or packet loss is observed.
    - *Falsifying observation:* Normal RTT and no packet loss is observed. This observation is only meaningful when compared with the partner probe.
2. **`measure_rtt`** — from: icscf, to: scscf
    - *Expected if hypothesis holds:* Normal RTT and no packet loss is observed.
    - *Falsifying observation:* High RTT or packet loss is also observed on this path, suggesting the issue is likely with the source 'icscf' or a shared network segment, not with 'pyhss'.
3. **`get_network_status`** — pyhss
    - *Expected if hypothesis holds:* The 'pyhss' container is in a running state.
    - *Falsifying observation:* The 'pyhss' container is not in a running state.

*Notes:* This plan addresses the secondary, lower-confidence hypothesis. It correctly uses a compositional probe (measure_rtt) with a partner probe for disambiguation, as required by the resample feedback, to isolate whether pyhss is the source of unresponsiveness or if the cause lies elsewhere (e.g., icscf or the network).


## Parallel Investigators (Phase 5)

**2 sub-Investigator verdict(s):** **2 DISPROVEN**

### `h1` — ❌ **DISPROVEN**

**Hypothesis:** The PCF is the source of the anomaly; its failure to access the unavailable MongoDB is causing it to reject Rx requests from the P-CSCF, leading to SIP registration and call setup failures.

**Reasoning:** [Multi-shot consensus — both shots returned DISPROVEN.]

Shot 1: Probe 3 revealed a direct contradiction to the hypothesis. Diagnostic metrics for the PCF at the time of the anomaly show that the number of policy association requests equals the number of successful policy associations. This 100% success rate falsifies the core claim of the hypothesis that the PCF was rejecting requests. While the MongoDB container was indeed down (Probe 1) and the PCF process was listening (Probe 2), the metrics prove it was not causing request rejections.

Shot 2: The hypothesis was disproven by the diagnostic metrics from the PCF. While the PCF container is running and listening on its service ports, and its MongoDB dependency is confirmed to be down, the metrics do not show any evidence of request rejections or processing failures. Instead, the available counters for policy association requests show a 100% success rate. This directly contradicts the hypothesis's central claim that the PCF is rejecting requests.

**Probes executed (3):**
- **Check the running status of network containers, particularly 'pcf' and 'mongo'.** ✓ CONSISTENT
    - *Tool:* `print(default_api.get_network_status())`
    - *Observation:* [EVIDENCE: get_network_status() -> "{'mongo': 'exited', 'pcf': 'running'}"]
    - *Comment:* The observation that the 'pcf' container is running while the 'mongo' container has exited is consistent with the prerequisite condition of the hypothesis.
- **Check if the PCF process is listening on its service ports.** ✓ CONSISTENT
    - *Tool:* `print(default_api.check_process_listeners(container='pcf'))`
    - *Observation:* [EVIDENCE: check_process_listeners("pcf") -> "tcp   LISTEN 0      5        172.22.0.27:7777       0.0.0.0:*    users:(('open5gs-pcfd',pid=8,fd=12))\ntcp   LISTEN 0      4096     172.22.0.27:9091       0.0.0.0:*    users:(('open5gs-pcfd',pid=8,fd=10))"]
    - *Comment:* The PCF process is listening on its network ports. This is consistent with the hypothesis that the PCF is running but is unhealthy internally.
- **Check PCF diagnostic metrics for signs of database errors or request processing failures.** ✗ CONTRADICTS
    - *Tool:* `print(default_api.get_diagnostic_metrics(at_time_ts=1778441490.4587011, nfs=['pcf']))`
    - *Observation:* [EVIDENCE: get_diagnostic_metrics(at_time_ts=1778441490.4587011, nfs=['pcf']) -> "fivegs_pcffunction_pa_policyamassoreq = 16 [counter]... fivegs_pcffunction_pa_policyamassosucc = 16 [counter]"]
    - *Comment:* The metrics show that the number of policy association requests (16) perfectly matches the number of successful associations (16). This contradicts the hypothesis that the PCF is rejecting requests.

**Alternative suspects:** pcscf

### `h2` — ❌ **DISPROVEN**

**Hypothesis:** The HSS is the source of the anomaly; it is unresponsive to Diameter Cx requests from the I-CSCF, causing a breakdown in the IMS registration and authentication flow.

**Reasoning:** [Multi-shot consensus — both shots returned DISPROVEN.]

Shot 1: The hypothesis that the HSS is unresponsive was contradicted by network-level probes. RTT measurements from the I-CSCF to both the HSS (pyhss) and a control target (S-CSCF) showed healthy, low-latency connectivity with zero packet loss. This indicates that the HSS is responsive at the transport layer, and the problem does not lie with network connectivity to the HSS, nor with the I-CSCF's networking stack.

Shot 2: The hypothesis is disproven by direct network transport probes. The HSS (pyhss) was expected to be unresponsive, but measurements from the I-CSCF to the pyhss show a healthy network path with no packet loss and very low latency. A partner probe from the I-CSCF to the S-CSCF confirms the I-CSCF's networking is not the issue. Furthermore, the pyhss container is confirmed to be in a running state. There is no evidence of network-level unresponsiveness, contradicting the core of the hypothesis.

**Probes executed (3):**
- **measure RTT from: icscf, to: pyhss** ✗ CONTRADICTS
    - *Tool:* `measure_rtt(container='icscf', target_ip='172.22.0.18')`
    - *Observation:* [EVIDENCE: measure_rtt("icscf", "172.22.0.18") -> "66 packets transmitted, 66 received, 0% packet loss, time 6777ms\nrtt min/avg/max/mdev = 0.049/0.111/0.212/0.031 ms"]
    - *Comment:* The RTT to the HSS is low and there is no packet loss. This contradicts the hypothesis that the HSS is unresponsive. This probe's result is confirmed by the partner probe.
- **measure RTT from: icscf, to: scscf** ✓ CONSISTENT
    - *Tool:* `measure_rtt(container='icscf', target_ip='172.22.0.20')`
    - *Observation:* [EVIDENCE: measure_rtt("icscf", "172.22.0.20") -> "66 packets transmitted, 66 received, 0% packet loss, time 6808ms\nrtt min/avg/max/mdev = 0.063/0.116/0.238/0.025 ms"]
    - *Comment:* This partner probe shows the I-CSCF has healthy network connectivity to other NFs. This, combined with the first probe's result, indicates the network path to the HSS is not the issue, and the HSS is responsive at the network layer.
- **get network status of pyhss** ✓ CONSISTENT
    - *Tool:* `get_network_status()`
    - *Observation:* [EVIDENCE: get_network_status() -> "'pyhss': 'running'"]
    - *Comment:* The pyhss container is running, which is consistent with a hypothesis about application-level unresponsiveness rather than a crash. However, the transport-level probes already disproved the 'unresponsive' claim.

**Alternative suspects:** pcf, mongo


## Evidence Validation (Phase 6)

**Overall verdict:** clean
**Overall confidence:** high

**Per sub-Investigator:**

| Agent | Tool Calls | Citations | Verdict | Confidence |
|---|---|---|---|---|
| `InvestigatorAgent_h1` | 6 | 3/3 | clean | high |
| `InvestigatorAgent_h2` | 8 | 3/3 | clean | high |


## Ground Truth

**Failure domain:** data_layer  
**Protocol impact:** multiple  
**Affected components:** mongo  
**Severity:** degraded

## Agent Diagnosis

**Model:** v7-adk/gemini-2.5-flash+gemini-2.5-pro  
**Time to diagnosis:** 315.3s

### Prompt to RCA Agent

```
The 5G SA + IMS stack is experiencing issues. Investigate and diagnose the root cause.
```

**Diagnosis:**

> ### causes
> - **summary**: Initial hypotheses targeting the PCF and HSS as the root cause of IMS failures were disproven, leaving the diagnosis inconclusive.
> - **timeline**:
>     1. The 'mongo' container, a database dependency for the PCF, entered an 'exited' state.
>     2. The P-CSCF began experiencing a high ratio of SIP errors.
>     3. The I-CSCF experienced a deviation in its rate of CDP replies per UE.
> - **root_cause**: INCONCLUSIVE
> - **affected_components**:
>     - `mongo`: Secondary
>     - `pcf`: Symptomatic
>     - `pcscf`: Symptomatic
> - **recommendation**: The initial hypotheses were incorrect. Further investigation should focus on the alternative suspects identified: check P-CSCF logs for error details, and re-evaluate the impact of the offline MongoDB container on the PCF, as the observed behavior (100% success rate despite DB failure) is anomalous.
> - **confidence**: low
> - **verdict_kind**: inconclusive
> - **explanation**: The two primary hypotheses were investigated and decisively disproven. The hypothesis that the PCF was rejecting requests (h1) was falsified by PCF's own diagnostic metrics, which showed a 100% success rate for policy association requests despite its MongoDB dependency being down. The hypothesis that the HSS was unresponsive (h2) was falsified by network probes showing healthy, low-latency connectivity from the I-CSCF. Because both initial lines of investigation failed and no single alternative suspect was corroborated enough to warrant promotion, the root cause remains unknown. The investigation did surface P-CSCF, PCF, and Mongo as leads for manual follow-up.

### Scoring Breakdown

**Overall score: 46%**

**Scorer assessment:** The agent correctly identified the severity and fault type, and accurately placed the affected component in its ontology layer. Its low confidence was well-calibrated given its failure to conclude a root cause in the final diagnosis, despite intermediate reasoning pointing to it. The agent listed MongoDB as an affected component but mis-ranked its causal role.

| Dimension | Result | Rationale |
|-----------|--------|-----------|
| Root cause correct | No | The agent's final diagnosis explicitly states 'INCONCLUSIVE' for the root cause. While the intermediate network analysis correctly identified MongoDB as the likely root cause, the final, authoritative diagnosis failed to conclude this. |
| Component overlap | 30% | The primary affected component, 'mongo', is listed in 'affected_components' but is labeled 'Secondary', not 'Root Cause'. This indicates the agent recognized its involvement but mis-ranked its causal role in the final diagnosis. |
| Severity correct | Yes | The simulated failure is a complete unreachability/down state for MongoDB. The agent's diagnosis mentions 'MongoDB container...being down' and 'exited' in its explanation and intermediate reasoning, correctly identifying a complete outage. |
| Fault type identified | Yes | The simulated failure mode is 'component completely unreachable (down/not responding)'. The agent's diagnosis and reasoning consistently refer to the 'mongo' container as 'exited' and 'down', which correctly identifies the fault type as a component being unreachable/down. |
| Layer accuracy | Yes | The ground truth states 'mongo' belongs to the 'infrastructure' layer. The agent's network analysis correctly rates the 'infrastructure' layer as 'red' with evidence 'MongoDB container has exited' and notes it as the 'likely root cause'. |
| Confidence calibrated | Yes | The agent's final diagnosis is 'INCONCLUSIVE' and its 'root_cause_correct' score is False. Given this, a 'low' confidence level is appropriate and well-calibrated. |

**Ranking:** The agent's final diagnosis explicitly states 'INCONCLUSIVE' and does not provide a ranked list of root cause candidates.


### Token Usage

| Metric | Count |
|--------|-------|
| Input tokens | 386,613 |
| Output tokens | 6,865 |
| Thinking tokens | 25,353 |
| **Total tokens** | **418,831** |

**Per-phase breakdown:**

| Phase | Tokens | Tool Calls | LLM Calls |
|-------|--------|------------|-----------|
| AnomalyScreener | 0 | 0 | 0 |
| SymptomClassifier | 0 | 0 | 0 |
| PathResolver | 0 | 0 | 0 |
| PathWalkInvestigator | 0 | 0 | 0 |
| EventAggregator | 0 | 0 | 0 |
| CorrelationAnalyzer | 0 | 0 | 0 |
| NetworkAnalystAgent | 80,460 | 4 | 5 |
| Phase 3 NetworkAnalyst__guardrail | 0 | 0 | 0 |
| InstructionGeneratorAgent | 10,447 | 0 | 1 |
| InstructionGeneratorAgent | 38,561 | 2 | 3 |
| Phase 4 InstructionGenerator__guardrail | 0 | 0 | 0 |
| InstructionGeneratorAgent | 22,473 | 1 | 2 |
| Phase 4 InstructionGenerator__guardrail | 0 | 0 | 0 |
| Phase 4 InstructionGenerator__guardrail | 0 | 0 | 0 |
| InvestigatorAgent_h1 | 49,598 | 3 | 4 |
| InvestigatorAgent_h1 | 49,148 | 3 | 4 |
| InvestigatorAgent_h1__reconciliation | 0 | 0 | 0 |
| InvestigatorAgent_h2 | 78,458 | 4 | 5 |
| InvestigatorAgent_h2 | 80,075 | 4 | 5 |
| InvestigatorAgent_h2__reconciliation | 0 | 0 | 0 |
| Phase5FanOutAudit | 0 | 0 | 0 |
| Phase6.5CandidatePool | 0 | 0 | 0 |
| EvidenceValidator | 0 | 0 | 0 |
| SynthesisAgent | 9,611 | 0 | 1 |
| Phase 7 Synthesis__guardrail | 0 | 0 | 0 |


## Resolution

**Heal method:** scheduled  
**Recovery time:** 449.9s
