# Episode Report: MongoDB Gone

**Agent:** v6  
**Episode ID:** ep_20260429_163805_mongodb_gone  
**Date:** 2026-04-29T16:38:07.099337+00:00  
**Duration:** 301.9s  

---

## Scenario

**Category:** container  
**Blast radius:** global  
**Description:** Kill MongoDB — the 5G core subscriber data store. UDR loses its backend, new PDU sessions cannot be created, and subscriber queries fail. Existing sessions may survive briefly.

## Faults Injected

- **container_kill** on `mongo`

## Baseline (Pre-Fault)

Stack phase before injection: **ready**
All containers running at baseline.

## Fault Propagation Verification

**Verdict:** ✅ `confirmed`

- **Wait:** 0s
- **Actual elapsed:** 0.0s
- **Nodes with significant deltas:** 3
- **Nodes with any drift:** 4

## Symptoms Observed

Symptoms detected: **Yes**  
Observation iterations: 1

## Anomaly Screening (Phase 0)

**ANOMALY DETECTED.** Overall anomaly score: 36.77 (per-bucket threshold: 26.31, context bucket (0, 1), trained on 323 healthy snapshots). The current metric pattern is statistically different from the learned healthy baseline. Something in the network has changed.

The following metrics deviate from their learned-healthy baseline. Treat each as a semantic observation (meaning + numbers), not a number alone — the KB's interpretation is the authoritative reading.

- **`context.cx_active`** — current **0.00** vs learned baseline **0.59** (MEDIUM, drop). *(No KB context available — interpret from the metric name.)*

- **`normalized.icscf.cdp_replies_per_ue`** (I-CSCF Diameter reply rate per UE) — current **0.00 replies_per_second_per_ue** vs learned baseline **0.03 replies_per_second_per_ue** (MEDIUM, drop)
    - **What it measures:** Liveness of the I-CSCF↔HSS Cx path. Drops to 0 when HSS is unreachable OR when no signaling is occurring at the I-CSCF (idle or upstream P-CSCF partitioned).
    - **Drop means:** No Cx replies in the window. Could be healthy idle OR a Cx-path fault.
    - **Healthy typical range:** 0–1 replies_per_second_per_ue

- **`normalized.icscf.core:rcv_requests_register_per_ue`** (SIP REGISTER rate per UE at I-CSCF) — current **0.02 requests_per_second** vs learned baseline **0.06 requests_per_second** (MEDIUM, drop)
    - **What it measures:** Health of the P-CSCF → I-CSCF forwarding path (Mw interface). When
this drops to zero while P-CSCF REGISTER rate is still non-zero,
it's the SIGNATURE of an IMS partition between P-CSCF and I-CSCF.
    - **Drop means:** Either UEs not registering at all, or P-CSCF isolated from I-CSCF.
    - **Healthy typical range:** 0–0.5 requests_per_second
    - **Healthy invariant:** Per-UE rate. Should closely track ims.pcscf.rcv_requests_register_per_ue.

- **`normalized.pcscf.core:rcv_requests_register_per_ue`** (SIP REGISTER rate per UE at P-CSCF) — current **0.02 requests_per_second** vs learned baseline **0.06 requests_per_second** (MEDIUM, drop)
    - **What it measures:** How actively UEs are refreshing their IMS registrations with the
P-CSCF. REGISTERs arrive periodically (re-registration timer) plus
at attach. Sustained zero means UEs cannot reach P-CSCF OR the
UE-to-network SIP path is broken.
    - **Drop means:** No REGISTERs flowing. Unusual unless UEs are all deregistered.
    - **Healthy typical range:** 0–0.5 requests_per_second
    - **Healthy invariant:** Per-UE rate — same value at any deployment scale.

- **`normalized.scscf.cdp_replies_per_ue`** (S-CSCF CDP Diameter replies per UE) — current **0.00 replies_per_second_per_ue** vs learned baseline **0.06 replies_per_second_per_ue** (MEDIUM, drop)
    - **What it measures:** Active S-CSCF Diameter traffic with HSS. Near-zero when registrations idle OR HSS partition.
    - **Drop means:** No active S-CSCF Diameter exchanges (idle or partitioned).
    - **Healthy typical range:** 0–1 replies_per_second_per_ue
    - **Healthy invariant:** Per-UE rate; varies with registration/auth load.

- **`normalized.scscf.core:rcv_requests_register_per_ue`** (SIP REGISTER rate per UE at S-CSCF) — current **0.00 requests_per_second** vs learned baseline **0.06 requests_per_second** (MEDIUM, drop)
    - **What it measures:** Health of the I-CSCF → S-CSCF forwarding path. Drop to zero while
I-CSCF is receiving REGISTERs = S-CSCF-side issue (crashed, or
I-CSCF → S-CSCF path broken).
    - **Drop means:** S-CSCF isolated or not running.
    - **Healthy typical range:** 0–0.5 requests_per_second
    - **Healthy invariant:** Per-UE rate. Tracks icscf.register rate.

- **`normalized.upf.gtp_indatapktn3upf_per_ue`** (GTP-U uplink rate per UE (N3)) — current **0.08 packets_per_second** vs learned baseline **1.45 packets_per_second** (MEDIUM, drop)
    - **What it measures:** Health of the uplink user-plane path gNB → UPF. Drops to near-zero
during RAN or N3 outage; stays nonzero during active calls or data
sessions. Decoupled from SIP signaling (signals data plane, not
control plane).
    - **Drop means:** Data plane dead on uplink — UPF receiving no packets from gNB.
    - **Healthy typical range:** 0–10 packets_per_second
    - **Healthy invariant:** Per-UE — constant regardless of UE pool size. Rises during active VoNR calls (~100 pps for G.711 voice) and data transfer.

- **`normalized.upf.gtp_outdatapktn3upf_per_ue`** (GTP-U downlink rate per UE (N3)) — current **0.00 packets_per_second** vs learned baseline **1.45 packets_per_second** (MEDIUM, drop)
    - **What it measures:** Health of the downlink user-plane path UPF → gNB. Typically mirrors
uplink rate during calls/data; asymmetry indicates directional
faults (e.g., UPF receiving from internet but not forwarding).
    - **Drop means:** No traffic leaving UPF toward RAN.
    - **Healthy typical range:** 0–10 packets_per_second
    - **Healthy invariant:** Per-UE — constant regardless of UE pool size. Roughly symmetric with uplink during healthy calls.

- **`normalized.smf.bearers_per_ue`** (Active QoS bearers per UE) — current **2.00 count** vs learned baseline **2.48 count** (LOW, shift)
    - **What it measures:** Per-UE count of active QoS bearers. Baseline reflects default
bearers; increments during VoNR calls indicate dedicated voice
bearers being set up. Drop during an active call = dedicated
bearer torn down unexpectedly (voice will fail).
    - **Shift means:** Expected during VoNR calls (1 extra bearer per active call).
    - **Healthy typical range:** 2–3.5 count
    - **Healthy invariant:** At rest: equals configured default bearers (typically 2 per UE).
During active VoNR call: +1 per caller. The per-UE ratio is the
invariant; absolute count scales with UE pool.


## Event Aggregation (Phase 1)

No events fired during this episode. Either no metric KB triggers matched, or the episode encountered no meaningful state transitions.

## Correlation Analysis (Phase 2)

No events fired — correlation engine had nothing to work with.

## Network Analysis (Phase 3)

**Summary:** The root cause of the outage is the failure of the MongoDB container, which has caused a cascading failure in the 5G core's data management tier (UDR), preventing UE registration and PDU session establishment.

**Ranked hypotheses:**

- **`h1`** (fit=0.90, nf=udr, specificity=specific):
    - **Statement:** The 'mongo' database container has exited. This has caused the UDR (Unified Data Repository) and PCF to fail, as they cannot connect to their database. The UDR failure prevents the AMF from retrieving subscriber data, which blocks UE core network registration and PDU session establishment, resulting in a complete failure of the control and data planes.
    - **Falsification probes:**
        - Restart the 'mongo' container and observe if UDR connectivity is restored.
        - Check the logs for the 'udr' container to confirm it is failing to connect to MongoDB.
        - Check the logs for the 'amf' container for errors related to fetching subscriber data from the UDM/UDR.
- **`h2`** (fit=0.60, nf=pyhss, specificity=moderate):
    - **Statement:** The HSS ('pyhss') is unresponsive to Diameter requests from the I-CSCF and S-CSCF, as evidenced by consistent timeouts on the Cx interface. This failure blocks all IMS-related procedures, including registration and authentication.
    - **Falsification probes:**
        - Check the 'pyhss' container logs to determine if it is receiving and processing Diameter requests from the CSCFs.
        - Measure the round-trip time from the 'icscf' container to the 'pyhss' container to rule out a network partition.
        - Directly query the HSS's underlying MySQL database to verify its health and responsiveness.


## Falsification Plans (Phase 4)

**2 falsification plan(s) — one per hypothesis:**

### Plan for `h1` (target: `mongo`)

**Hypothesis:** The 'mongo' database container has exited. This has caused the UDR (Unified Data Repository) and PCF to fail, as they cannot connect to their database. The UDR failure prevents the AMF from retrieving subscriber data, which blocks UE core network registration and PDU session establishment, resulting in a complete failure of the control and data planes.

**Probes (3):**
1. **`get_network_status`** — all containers
    - *Expected if hypothesis holds:* The 'mongo' container has a status of 'exited' or is not in the running list.
    - *Falsifying observation:* The 'mongo' container status is 'running'.
2. **`get_network_status`** — focus on udr, pcf
    - *Expected if hypothesis holds:* The 'udr' and 'pcf' containers are in a 'restarting' (crash loop) or 'exited' state.
    - *Falsifying observation:* Both 'udr' and 'pcf' containers are 'running' normally.
3. **`get_nf_metrics`** — focus on amf
    - *Expected if hypothesis holds:* AMF metrics show a high rate of failures for outgoing requests to the UDM/UDR (e.g., HTTP 5xx error codes or timeouts on the N10 interface).
    - *Falsifying observation:* AMF metrics for UDM/UDR communication are normal (e.g., high rate of HTTP 2xx success codes, low/zero errors).

*Notes:* This hypothesis posits a clear causal chain starting from a container failure. The probes are designed to verify each step of this chain: the trigger (mongo down), the immediate effect (UDR/PCF down), and the cascading effect (AMF errors).

### Plan for `h2` (target: `pyhss`)

**Hypothesis:** The HSS ('pyhss') is unresponsive to Diameter requests from the I-CSCF and S-CSCF, as evidenced by consistent timeouts on the Cx interface. This failure blocks all IMS-related procedures, including registration and authentication.

**Probes (3):**
1. **`measure_rtt`** — from 'icscf' to 'pyhss' IP
    - *Expected if hypothesis holds:* High RTT or packet loss, indicating a network partition or heavy congestion.
    - *Falsifying observation:* Low RTT (<5ms) and 0% packet loss. This would rule out a network partition as the cause of unresponsiveness.
2. **`query_subscriber`** — any valid test IMSI, e.g. 'imsi-208930000000001'
    - *Expected if hypothesis holds:* The tool call fails, times out, or returns an error, indicating the HSS service is unhealthy and cannot access its data store.
    - *Falsifying observation:* The tool successfully retrieves and displays the subscriber's profile. This proves the HSS service and its database backend are fundamentally working.
3. **`check_process_listeners`** — on the 'pyhss' container, for Diameter port 3868
    - *Expected if hypothesis holds:* No process is listening on TCP port 3868, meaning the Diameter server is not running.
    - *Falsifying observation:* A process is actively listening on TCP port 3868. This shows the HSS is ready for Diameter traffic, suggesting the issue is in processing, not listening.

*Notes:* This hypothesis claims unresponsiveness. The probes are designed to isolate the failure domain: network path (`measure_rtt`), service health (`query_subscriber`), and process state (`check_process_listeners`). This provides triangulation to pinpoint why 'pyhss' might be unresponsive.


## Parallel Investigators (Phase 5)

**2 sub-Investigator verdict(s):** **2 DISPROVEN**

### `h1` — ❌ **DISPROVEN**

**Hypothesis:** The 'mongo' database container has exited. This has caused the UDR (Unified Data Repository) and PCF to fail, as they cannot connect to their database. The UDR failure prevents the AMF from retrieving subscriber data, which blocks UE core network registration and PDU session establishment, resulting in a complete failure of the control and data planes.

**Reasoning:** The hypothesis is disproven because a key consequence it predicts is demonstrably false. While the 'mongo' container has exited as predicted, the PCF container is not only running but its metrics show a 100% success rate for policy association requests. This contradicts the claim that the PCF would fail due to the database outage.

**Probes executed (2):**
- **Check the status of all containers to see if the 'mongo' container has exited and if 'udr' and 'pcf' are failing.** ✗ CONTRADICTS
    - *Tool:* `print(default_api.get_network_status())`
    - *Observation:* [EVIDENCE: get_network_status() -> "'containers': {'mongo': 'exited', 'udr': 'running', 'pcf': 'running'}"]
    - *Comment:* The observation that the 'mongo' container has exited is consistent with the hypothesis. However, the observation that the 'udr' and 'pcf' containers are 'running' contradicts the expectation that they would be in a 'restarting' or 'exited' state.
- **Check AMF and PCF metrics to see if they show failures.** ✗ CONTRADICTS
    - *Tool:* `print(default_api.get_nf_metrics())`
    - *Observation:* [EVIDENCE: get_nf_metrics() -> "fivegs_pcffunction_pa_policyamassoreq = 10.0, fivegs_pcffunction_pa_policyamassosucc = 10.0"]
    - *Comment:* The PCF metrics show that requests for policy association are 100% successful. This directly contradicts the hypothesis's claim that the PCF would fail. If it were unable to connect to its database, it should not be able to process these requests successfully.

**Alternative suspects:** pcf

### `h2` — ❌ **DISPROVEN**

**Hypothesis:** The HSS ('pyhss') is unresponsive to Diameter requests from the I-CSCF and S-CSCF, as evidenced by consistent timeouts on the Cx interface. This failure blocks all IMS-related procedures, including registration and authentication.

**Reasoning:** The hypothesis claims the HSS is unresponsive. Probes show this is false. The RTT from the I-CSCF to the HSS is low (<1ms) with no packet loss, ruling out a network partition. Furthermore, a direct query to the HSS for subscriber data completed successfully, returning a result without timing out. This proves the HSS service is responsive at both the network and application service layer. The timeouts experienced by the CSCFs must stem from a more complex issue than a simple lack of response from the HSS.

**Probes executed (3):**
- **from 'icscf' to 'pyhss' IP** ✗ CONTRADICTS
    - *Tool:* `default_api.measure_rtt(container='icscf', target_ip='172.22.0.18')`
    - *Observation:* [EVIDENCE: measure_rtt("icscf", "172.22.0.18") -> "rtt min/avg/max/mdev = 0.077/0.093/0.116/0.016 ms"]
    - *Comment:* The RTT is extremely low and there is 0% packet loss. This contradicts the expectation of high RTT or packet loss, falsifying the idea of a network partition or congestion causing the unresponsiveness.
- **any valid test IMSI, e.g. 'imsi-208930000000001'** ✗ CONTRADICTS
    - *Tool:* `default_api.query_subscriber(domain='ims', imsi='imsi-208930000000001')`
    - *Observation:* [EVIDENCE: query_subscriber("ims", "imsi-208930000000001") -> "{\n  \"ims_subscriber\": null\n}"]
    - *Comment:* The tool successfully received a reply from the HSS service, proving it is responsive. The hypothesis expected the tool to time out or fail. While the result is 'null' (subscriber not found), the service itself did not time out and is clearly running and queryable, which directly contradicts the claim of unresponsiveness.
- **on the 'pyhss' container, for Diameter port 3868** ~ AMBIGUOUS
    - *Tool:* `default_api.check_process_listeners(container='pyhss')`
    - *Observation:* [EVIDENCE: check_process_listeners("pyhss") -> "Neither ss nor netstat available in pyhss. Output: OCI runtime exec failed: exec failed: unable to start container process: exec: \"netstat\": executable file not found in $PATH: unknown"]
    - *Comment:* The probe failed because the required tool (`netstat` or `ss`) was not available in the container. No conclusion can be drawn from this probe.

**Alternative suspects:** icscf, scscf


## Evidence Validation (Phase 6)

**Overall verdict:** clean
**Overall confidence:** high

**Per sub-Investigator:**

| Agent | Tool Calls | Citations | Verdict | Confidence |
|---|---|---|---|---|
| `InvestigatorAgent_h1` | 2 | 2/2 | clean | high |
| `InvestigatorAgent_h2` | 4 | 3/3 | clean | high |


## Ground Truth

**Failure domain:** data_layer  
**Protocol impact:** multiple  
**Affected components:** mongo  
**Severity:** degraded

## Agent Diagnosis

**Model:** v6-adk/gemini-2.5-flash+gemini-2.5-pro  
**Time to diagnosis:** 165.1s

### Prompt to RCA Agent

```
The 5G SA + IMS stack is experiencing issues. Investigate and diagnose the root cause.
```

**Diagnosis:**

> ### causes
> - **summary**: The initial hypotheses regarding a MongoDB failure causing a 5G core cascade and an unresponsive HSS blocking IMS have both been disproven, pointing towards an unconfirmed issue within the PCF, I-CSCF, or S-CSCF.
> - **timeline**:
>     1. No correlated events were available to construct an event timeline.
> - **root_cause**: The root cause could not be determined. The initial set of hypotheses from the Network Analyst was incorrect. The investigation suggests new primary suspects are the Policy Control Function (`pcf`), the Interrogating-CSCF (`icscf`), and the Serving-CSCF (`scscf`).
> - **affected_components**:
>     - `pcf`: Suspected Cause (needs investigation)
>     - `icscf`: Suspected Cause (needs investigation)
>     - `scscf`: Suspected Cause (needs investigation)
>     - `mongo`: Symptomatic (Container has exited, but this was proven not to be the root cause of the end-to-end service failure.)
> - **recommendation**: Since the primary hypotheses were disproven, manual investigation is required. Focus on the `pcf`, `icscf`, and `scscf` components. Check their internal logs for errors and verify their configurations, particularly how they handle upstream service failures (e.g., database connectivity for PCF, Diameter timeouts for CSCFs).
> - **confidence**: low
> - **explanation**: The two primary hypotheses were systematically tested and disproven. The theory of a cascading failure due to the `mongo` database container exiting was falsified by evidence showing the PCF container operating with 100% success on policy requests; this should not be possible if it could not reach its database. The second theory, that the HSS (`pyhss`) was unresponsive, was falsified by direct network and application probes that confirmed the HSS was reachable and responding to queries. As both initial hypotheses are invalid, confidence in a specific root cause is low. The investigation now shifts to the components that showed unexpected behavior: the PCF, I-CSCF, and S-CSCF.

### Scoring Breakdown

**Overall score: 46%**

**Scorer assessment:** The agent correctly identified the severity and the type of fault for MongoDB, and its confidence was calibrated. However, it incorrectly dismissed MongoDB as the root cause and failed to identify the correct primary affected components, instead pointing to other NFs.

| Dimension | Result | Rationale |
|-----------|--------|-----------|
| Root cause correct | No | The simulated failure was the 'mongo' component being completely unreachable. The agent's final diagnosis explicitly states, 'The root cause could not be determined. The initial set of hypotheses from the Network Analyst was incorrect.' It then suggests PCF, I-CSCF, and S-CSCF as new primary suspects, and states that the MongoDB failure hypothesis 'was falsified by evidence'. This is incorrect; MongoDB was indeed the root cause. |
| Component overlap | 30% | The primary affected component was 'mongo'. The agent lists 'mongo' in its 'affected_components' but labels it as 'Symptomatic' and explicitly states it 'was proven not to be the root cause'. It incorrectly identifies 'pcf', 'icscf', and 'scscf' as 'Suspected Cause'. Therefore, it identified the component but mis-ranked its causal role. |
| Severity correct | Yes | The simulated failure implies a complete outage ('Component completely unreachable'). The agent's explanation refers to an 'end-to-end service failure', which aligns with a complete outage. |
| Fault type identified | Yes | The simulated failure was 'mongo' component completely unreachable. The agent correctly observed that the 'mongo' container 'has exited', which is a clear identification of a 'component unreachable' fault type, even though it dismissed its causal role. |
| Layer accuracy | Yes | The 'AGENT NETWORK ANALYSIS' section, which is the only source for layer status, has an empty `layer_status` object. Since no layer status information is available, no misattribution can be detected. |
| Confidence calibrated | Yes | The agent's diagnosis was incorrect regarding the root cause and primary affected components. Its stated confidence level of 'low' is appropriate given the inaccuracy of its conclusion. |

**Ranking:** The agent explicitly states 'The root cause could not be determined' and dismisses 'mongo' as the root cause, labeling it 'Symptomatic'. It then lists PCF, I-CSCF, and S-CSCF as 'new primary suspects'. The correct cause is not ranked as a primary suspect or root cause.


### Token Usage

| Metric | Count |
|--------|-------|
| Input tokens | 136,854 |
| Output tokens | 3,519 |
| Thinking tokens | 12,713 |
| **Total tokens** | **153,086** |

**Per-phase breakdown:**

| Phase | Tokens | Tool Calls | LLM Calls |
|-------|--------|------------|-----------|
| AnomalyScreener | 0 | 0 | 0 |
| EventAggregator | 0 | 0 | 0 |
| CorrelationAnalyzer | 0 | 0 | 0 |
| NetworkAnalystAgent | 55,620 | 5 | 4 |
| InstructionGeneratorAgent | 17,592 | 2 | 2 |
| InvestigatorAgent_h1 | 29,640 | 2 | 3 |
| InvestigatorAgent_h2 | 45,187 | 4 | 5 |
| EvidenceValidator | 0 | 0 | 0 |
| SynthesisAgent | 5,047 | 0 | 1 |


## Resolution

**Heal method:** scheduled  
**Recovery time:** 301.9s
