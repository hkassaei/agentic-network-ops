# Episode Report: S-CSCF Crash

**Agent:** v6  
**Episode ID:** ep_20260421_023627_s_cscf_crash  
**Date:** 2026-04-21T02:36:28.970925+00:00  
**Duration:** 249.0s  

---

## Scenario

**Category:** container  
**Blast radius:** single_nf  
**Description:** Kill the S-CSCF (Serving-CSCF). This is the core SIP registrar and call controller. All IMS authentication stops, new registrations are impossible, and active calls will eventually drop.

## Faults Injected

- **container_kill** on `scscf`

## Baseline (Pre-Fault)

Stack phase before injection: **ready**
All containers running at baseline.

## Fault Propagation Verification

**Verdict:** ✅ `confirmed`

- **Wait:** 0s
- **Actual elapsed:** 0.0s
- **Nodes with significant deltas:** 3
- **Nodes with any drift:** 4

## Symptoms Observed

Symptoms detected: **Yes**  
Observation iterations: 1

## Anomaly Screening (Phase 0)

**ANOMALY DETECTED.** Overall anomaly score: 0.90 (threshold: 0.70, trained on 211 healthy snapshots). The current metric pattern is statistically different from the learned healthy baseline. Something in the network has changed.

The following metrics deviate from their learned-healthy baseline. Treat each as a semantic observation (meaning + numbers), not a number alone — the KB's interpretation is the authoritative reading.

- **`normalized.upf.gtp_outdatapktn3upf_per_ue`** (GTP-U downlink rate per UE (N3)) — current **0.02 packets_per_second** vs learned baseline **3.34 packets_per_second** (MEDIUM, drop)
    - **What it measures:** Health of the downlink user-plane path UPF → gNB. Typically mirrors
uplink rate during calls/data; asymmetry indicates directional
faults (e.g., UPF receiving from internet but not forwarding).
    - **Drop means:** No traffic leaving UPF toward RAN.
    - **Healthy typical range:** 0–10 packets_per_second
    - **Healthy invariant:** Per-UE — constant regardless of UE pool size. Roughly symmetric with uplink during healthy calls.

- **`normalized.upf.gtp_indatapktn3upf_per_ue`** (GTP-U uplink rate per UE (N3)) — current **0.11 packets_per_second** vs learned baseline **3.42 packets_per_second** (MEDIUM, drop)
    - **What it measures:** Health of the uplink user-plane path gNB → UPF. Drops to near-zero
during RAN or N3 outage; stays nonzero during active calls or data
sessions. Decoupled from SIP signaling (signals data plane, not
control plane).
    - **Drop means:** Data plane dead on uplink — UPF receiving no packets from gNB.
    - **Healthy typical range:** 0–10 packets_per_second
    - **Healthy invariant:** Per-UE — constant regardless of UE pool size. Rises during active VoNR calls (~100 pps for G.711 voice) and data transfer.

- **`normalized.pcscf.dialogs_per_ue`** (Active SIP dialogs per registered UE at P-CSCF) — current **0.00 count** vs learned baseline **0.57 count** (LOW, drop)
    - **What it measures:** How many calls per user are currently in progress at the P-CSCF.
Going to zero from a non-zero baseline means calls have ended
(normal) OR call setup is failing system-wide (degradation).
Together with rcv_requests_* it discriminates the two.
    - **Drop means:** No active calls. Normal at rest; suspicious if prior state had calls AND INVITE rate is also zero.
    - **Healthy typical range:** 0–1 count
    - **Healthy invariant:** Per-UE — scale-independent. 0 at rest, ~1 per active VoNR call.

- **`derived.upf_activity_during_calls`** — current **1.00** vs learned baseline **0.47** (LOW, spike). *(No KB context available — interpret from the metric name.)*


## Event Aggregation (Phase 1)

No events fired during this episode. Either no metric KB triggers matched, or the episode encountered no meaningful state transitions.

## Correlation Analysis (Phase 2)

No events fired — correlation engine had nothing to work with.

## Network Analysis (Phase 3)

**Summary:** The S-CSCF, a critical IMS component, has crashed, leading to a complete failure of VoNR call setup and a corresponding lack of data plane traffic.

**Layer status:**

| Layer | Rating | Note |
|---|---|---|
| **infrastructure** | 🟢 GREEN | The underlying container orchestration appears to be stable. |
| **ran** | 🟢 GREEN | The RAN appears to be functioning correctly and is connected to the core. |
| **core** | 🟡 YELLOW | The core data plane is idle, which is a primary symptom. However, the root cause is not in the core NFs themselves, but an upstream IMS failure preventing call setup, meaning no data plane is ever requested. |
| **ims** | 🔴 RED | The IMS layer is non-functional due to the failure of the S-CSCF, which is essential for SIP session routing and control. |

**CORE evidence:**
- UPF GTP-U traffic is near-zero for both uplink and downlink
- UPF container is running and N4 link from SMF is active

**IMS evidence:**
- S-CSCF container has 'exited'
- Active SIP dialogs at P-CSCF is zero
- Multiple IMS-layer network links are inactive due to S-CSCF being down

**Ranked hypotheses:**

- **`h1`** (fit=1.00, nf=scscf, specificity=specific):
    - **Statement:** The S-CSCF container has exited, which breaks the IMS call setup signaling chain (P-CSCF → I-CSCF → S-CSCF) and prevents VoNR sessions from being established. The observed zero data-plane traffic is a direct consequence, as no GTP tunnels are created if calls do not connect.
    - **Falsification probes:**
        - Check the docker logs for the 'scscf' container to find the reason for its exit.
        - Restarting the 'scscf' container should immediately restore VoNR call setup capabilities.


## Falsification Plans (Phase 4)

**1 falsification plan(s) — one per hypothesis:**

### Plan for `h1` (target: `scscf`)

**Hypothesis:** The S-CSCF container has exited, which breaks the IMS call setup signaling chain (P-CSCF → I-CSCF → S-CSCF) and prevents VoNR sessions from being established. The observed zero data-plane traffic is a direct consequence, as no GTP tunnels are created if calls do not connect.

**Probes (3):**
1. **`get_network_status`** — Check the running status of the 'scscf' container.
    - *Expected if hypothesis holds:* The 'scscf' container will be reported as 'exited' or 'down'.
    - *Falsifying observation:* The 'scscf' container is reported as 'running' and healthy.
2. **`check_process_listeners`** — Check which ports are actively listening on the 'scscf' container.
    - *Expected if hypothesis holds:* No SIP-related ports (e.g., 5060 UDP/TCP, 5061 TLS) will be found listening on the 'scscf' container, or the tool will report an error due to the container being inaccessible.
    - *Falsifying observation:* SIP-related ports (e.g., 5060 UDP/TCP, 5061 TLS) are found actively listening on the 'scscf' container, indicating the S-CSCF application is running and responsive.
3. **`get_nf_metrics`** — Retrieve metrics from the 'icscf' that indicate attempts, successes, or failures to communicate with the S-CSCF. Specifically look for counters related to S-CSCF reachability or session setup attempts towards S-CSCF.
    - *Expected if hypothesis holds:* I-CSCF metrics will show an absence of successful session setups towards S-CSCF, and/or an increase in errors/timeouts when attempting to route to S-CSCF (e.g., 'scscf_unreachable_count' or 'sip_5xx_responses_from_scscf').
    - *Falsifying observation:* I-CSCF metrics show successful session setups towards S-CSCF and no significant errors or timeouts related to S-CSCF communication, implying I-CSCF can successfully interact with S-CSCF.

*Notes:* The probes focus on verifying the S-CSCF's liveness at the container and application level, and how its upstream dependency (I-CSCF) observes its status, without duplicating the NA's suggested log checks.


## Parallel Investigators (Phase 5)

**1 sub-Investigator verdict(s):** **1 NOT_DISPROVEN**

### `h1` — ✅ **NOT_DISPROVEN**

**Hypothesis:** The S-CSCF container has exited, which breaks the IMS call setup signaling chain (P-CSCF → I-CSCF → S-CSCF) and prevents VoNR sessions from being established. The observed zero data-plane traffic is a direct consequence, as no GTP tunnels are created if calls do not connect.

**Reasoning:** All executed probes produced evidence consistent with the hypothesis. The first probe directly confirmed the central claim that the S-CSCF container has exited. Subsequent probes confirmed the logical consequences of this state, such as the absence of listening processes, and found no contradictory evidence in the metrics of adjacent network functions.

**Probes executed (3):**
- **Check the running status of the 'scscf' container.** ✓ CONSISTENT
    - *Tool:* `print(default_api.get_network_status())`
    - *Observation:* [EVIDENCE: get_network_status() -> "'containers': {'scscf': 'exited'}"]
    - *Comment:* The tool output confirms that the scscf container has an 'exited' status, which directly aligns with the hypothesis's primary assertion.
- **Check which ports are actively listening on the 'scscf' container.** ✓ CONSISTENT
    - *Tool:* `print(default_api.check_process_listeners(container='scscf'))`
    - *Observation:* [EVIDENCE: check_process_listeners("scscf") -> "Error response from daemon: container cfb808a9bc67ca46a2355b05bea28407d83285ff1bd77561080e32db686f7b62 is not running"]
    - *Comment:* The tool failed because the container is not running. This is the expected outcome if the container has exited and therefore cannot have any listening application ports.
- **Retrieve metrics from the 'icscf' that indicate attempts, successes, or failures to communicate with the S-CSCF.** ✓ CONSISTENT
    - *Tool:* `print(default_api.get_nf_metrics())`
    - *Observation:* [EVIDENCE: get_nf_metrics() -> "ICSCF [8 req] (via kamcmd):\n  core:rcv_requests_invite = 0.0"]
    - *Comment:* ICSCF metrics show it has received zero INVITE requests, which is consistent with a broken call-setup chain. While this doesn't explicitly show I-CSCF failing to reach S-CSCF, it doesn't contradict the hypothesis. The lack of incoming call attempts at I-CSCF is an expected symptom if the upstream P-CSCF knows the S-CSCF is unavailable, or if the failure is total.


## Evidence Validation (Phase 6)

**Overall verdict:** clean
**Overall confidence:** high

**Per sub-Investigator:**

| Agent | Tool Calls | Citations | Verdict | Confidence |
|---|---|---|---|---|
| `InvestigatorAgent_h1` | 3 | 3/3 | clean | high |


## Ground Truth

**Failure domain:** ims_signaling  
**Protocol impact:** SIP  
**Affected components:** scscf  
**Severity:** degraded

## Agent Diagnosis

**Model:** v6-adk/gemini-2.5-flash+gemini-2.5-pro  
**Time to diagnosis:** 117.4s

### Prompt to RCA Agent

```
The 5G SA + IMS stack is experiencing issues. Investigate and diagnose the root cause.
```

**Diagnosis:**

> ### causes
> - **summary**: The Serving-CSCF (S-CSCF) has crashed, leading to a complete failure of Voice over New Radio (VoNR) call setup.
> - **timeline**:
>     1. The S-CSCF container, a critical component of the IMS system, unexpectedly exited.
>     2. This breaks the mandatory SIP signaling path required for session establishment.
>     3. All subsequent VoNR call attempts failed during the signaling phase, as they could not be routed by the S-CSCF.
>     4. As a direct consequence, the 5G Core's data plane remains idle, as no requests to establish data bearers for voice calls are ever received.
> - **root_cause**: The `scscf` container has exited. The investigation confirmed its status as 'exited', directly breaking the IMS signaling chain. `primary_suspect_nf`: `scscf`.
> - **affected_components**:
>     - `scscf`: Root Cause
>     - `ims`: Secondary (The entire IMS layer is non-functional without a working S-CSCF)
>     - `core`: Symptomatic (The core's data plane is idle as a symptom, not a cause, of the IMS failure)
> - **recommendation**: The operator should investigate the S-CSCF container logs to determine the reason for its unexpected exit.
> - **confidence**: high
> - **explanation**: The investigation confirmed with high confidence that the root cause is a crash of the S-CSCF container. Probes verified that the container is in an 'exited' state, which is a total failure for this critical IMS component. This single failure fully explains the observed symptoms: without a functional S-CSCF, the SIP signaling chain is broken, preventing any VoNR calls from being established. The resulting lack of data plane traffic in the 5G core is a direct and expected consequence of no calls successfully connecting.

### Scoring Breakdown

**Overall score: 100%**

**Scorer assessment:** The agent provided a perfect diagnosis, accurately identifying the S-CSCF crash as the root cause, its severity, fault type, and affected layer, with appropriate high confidence.

| Dimension | Result | Rationale |
|-----------|--------|-----------|
| Root cause correct | Yes | The agent correctly identified the S-CSCF crash/exit as the root cause, which directly corresponds to the simulated failure mode of the component being unreachable. |
| Component overlap | 100% | The agent explicitly named 'scscf' as the root cause and primary suspect. Listing 'ims' and 'core' as secondary/symptomatic is appropriate and does not detract from the primary component identification. |
| Severity correct | Yes | The agent described the failure as a 'crash', 'exited' state, and 'complete failure' of VoNR, which accurately reflects the complete unreachability and outage simulated. |
| Fault type identified | Yes | The agent identified the fault as a component crash/exit, leading to unreachability, which is a correct observable class of failure. |
| Layer accuracy | Yes | The agent correctly attributed the S-CSCF failure to the 'ims' layer, rating it 'red' with the S-CSCF container status as evidence, aligning with the component's ontology layer. |
| Confidence calibrated | Yes | The agent's diagnosis is highly accurate, specific, and well-supported by evidence (S-CSCF container 'exited' status), justifying the 'high' confidence level. |

**Ranking position:** #1 — The correct cause was the only and primary candidate listed by the agent.


### Token Usage

| Metric | Count |
|--------|-------|
| Input tokens | 63,310 |
| Output tokens | 2,368 |
| Thinking tokens | 9,455 |
| **Total tokens** | **75,133** |

**Per-phase breakdown:**

| Phase | Tokens | Tool Calls | LLM Calls |
|-------|--------|------------|-----------|
| AnomalyScreener | 0 | 0 | 0 |
| EventAggregator | 0 | 0 | 0 |
| CorrelationAnalyzer | 0 | 0 | 0 |
| NetworkAnalystAgent | 34,760 | 3 | 4 |
| InstructionGeneratorAgent | 10,870 | 1 | 2 |
| InvestigatorAgent_h1 | 25,517 | 3 | 4 |
| EvidenceValidator | 0 | 0 | 0 |
| SynthesisAgent | 3,986 | 0 | 1 |


## Resolution

**Heal method:** scheduled  
**Recovery time:** 249.0s
